---
day: 396
phase: 6
title_en: "Rendering Lighting Information from the Game"
title_zh: "从游戏渲染光照信息"
type: coding
difficulty: 4
duration: "1h"
hh_url: "https://guide.handmadehero.org/code/day396/"
domains: [game, graphics, rust]
prereqs: ["day395"]
---

# Day 396 · 从游戏渲染光照信息

> V-cycle 算出了体素网格上的光照。今天 Casey 把这个体素光照**采样到屏幕**,让玩家**看到**间接光的效果。这是从"算法空间"(3D 体素)回到"渲染空间"(2D 屏幕)的桥接。

## 0 · 为什么要有这一天

Day 394-395 的 V-cycle 算出**体素光照场**(64³ 网格,每体素 RGB 光照)。但这是个 3D 数据结构,屏幕是 2D 的——玩家看不到。

**桥接**:每像素的 fragment shader,**采样**该像素位置在体素网格中的光照值,加到 Phong 光照之上。具体:

1. G-buffer 告诉你每像素的 world position
2. 用 position 采样体素 3D 纹理:`vec3 gi_light = texture(voxel_light_texture, position / grid_extent)`
3. 加到 Phong 结果:`final = phong + gi_light * albedo`

**关键技术点**:

1. **3D 纹理采样**:GPU 用三线性插值(`texture(sampler3D, p)`)
2. **世界坐标到纹理坐标**:world position → normalized [0, 1]
3. **多级网格选择**:粗细网格采样(类似 mipmap)
4. **debug 视图**:切换显示直接光 / 间接光 / 合成

**今天的产出**:游戏场景里能看到**间接光**——红色墙壁附近的白墙微微泛红,角落比开阔地暗,这些真实光照效果。

心理锚点:看完今天,你能(1)用 GLSL 采样 3D 纹理;(2)把 world position 转为 texture coordinate;(3)把直接光和间接光合成;(4)用 debug 视图验证每步。

## 1 · Casey 今天做了什么(HH 脉络)

Casey 流程:

1. **回顾 Day 395**:V-cycle 算出 64³ 体素光照。
2. **桥接设计**:fragment shader 采样 3D 纹理。
3. **GLSL 实现**:
   - `uniform sampler3D voxel_light_texture`
   - `vec3 gi = texture(voxel_light_texture, (world_pos - grid_min) / grid_extent).rgb`
   - `final = ambient + diffuse + specular + gi * albedo`
4. **debug 视图**:F1 切换 "Phong only" / "GI only" / "Phong + GI"
5. **跑 demo**:看到红色墙壁把光反射到白墙,白墙泛红

这集是**算法落地**——把数学成果变成玩家可见的视觉效果。

## 2 · 心智模型

### 类比:医生看 MRI

想象医生看 MRI 扫描——MRI 是 3D 体数据(类似体素网格),医生**切片**看 2D 图像。

- 切片位置 = 像素的 world position
- 切片值 = 该位置的体素光照
- 显示 = 切片值映射到颜色

GPU 上的 3D 纹理采样就是这个过程——给定 3D 坐标,GPU 自动三线性插值 8 个邻居,返回该点的"光照值"。

### 严谨原理:3D 纹理采样

GLSL 的 `sampler3D`:

```glsl
uniform sampler3D voxel_light;  // 64³ RGBA32F

vec3 sample_gi(vec3 world_pos) {
    // 把 world_pos 归一化到 [0, 1]
    vec3 uvw = (world_pos - grid_min) / grid_extent;
    // 三线性采样(自动)
    vec4 sample = texture(voxel_light, uvw);
    return sample.rgb;
}
```

**关键**:

- `grid_min`:网格最小角的世界坐标
- `grid_extent`:网格的总尺寸(如 (3.2, 3.2, 3.2) for 64³ × 0.05)
- `uvw`:归一化坐标 ∈ [0, 1]³
- `texture(sampler3D, uvw)`:GPU 自动三线性插值

**纹理创建**(CPU 端):

```rust
let voxel_data: Vec<f32> = vec![0.0; 64 * 64 * 64 * 4];  // RGBA32F

let voxel_texture = glium::texture::Texture3D::new(
    &display,
    glium::texture::RawImage3d {
        data: std::borrow::Cow::Owned(voxel_data),
        width: 64, height: 64, depth: 64,
        format: glium::texture::ClientFormat::F32F32F32F32,
    },
    glium::texture::MipmapsOption::NoMipmap,
).unwrap();
```

**上传 V-cycle 结果到纹理**:

```rust
// V-cycle 计算后,把体素数据复制到 voxel_data
// 然后 upload 到 GPU 纹理
let main_texture = voxel_texture.main_level();
let writer = main_texture.write();
writer.write(glium::texture::RawImage3d {
    data: std::borrow::Cow::Owned(voxel_data),
    width: 64, height: 64, depth: 64,
    format: glium::texture::ClientFormat::F32F32F32F32,
});
```

### 完整 fragment shader(Phong + GI)

```glsl
#version 330

in vec3 v_world_position;
in vec3 v_world_normal;
in vec2 v_uv;

uniform sampler3D voxel_light;       // 64³ RGBA32F
uniform vec3 grid_min;
uniform vec3 grid_extent;

uniform sampler2D albedo_tex;
uniform vec3 light_dir;
uniform vec3 light_color;
uniform vec3 ambient_color;
uniform vec3 camera_pos;
uniform vec3 specular_color;
uniform float shininess;

uniform int debug_view;  // 0=phong+gi, 1=phong only, 2=gi only

out vec4 frag;

void main() {
    // Phong(Day 389-390)
    vec3 albedo = texture(albedo_tex, v_uv).rgb;
    vec3 N = normalize(v_world_normal);
    vec3 L = normalize(light_dir);
    vec3 V = normalize(camera_pos - v_world_position);
    vec3 R = reflect(-L, N);
    float diff = max(0.0, dot(N, L));
    float spec = pow(max(0.0, dot(R, V)), shininess);
    vec3 ambient = albedo * ambient_color;
    vec3 diffuse = albedo * light_color * diff;
    vec3 specular = specular_color * light_color * spec;
    vec3 phong = ambient + diffuse + specular;

    // GI(从体素网格采样)
    vec3 uvw = (v_world_position - grid_min) / grid_extent;
    vec3 gi_light = texture(voxel_light, uvw).rgb;
    vec3 gi = albedo * gi_light;  // 间接光照亮物体表面

    vec3 final_color;
    if (debug_view == 0) {
        final_color = phong + gi;
    } else if (debug_view == 1) {
        final_color = phong;
    } else {
        final_color = gi;
    }

    // Tone map + sRGB
    final_color = final_color / (final_color + vec3(1.0));
    frag = vec4(final_color, 1.0);
}
```

### World position → texture coordinate 的边界

如果 world_pos 在网格外,`uvw` 越界(< 0 或 > 1)。OpenGL 默认 `GL_REPEAT` 模式,纹理会"环绕",导致错误。

**修法**:用 `GL_CLAMP_TO_EDGE`(UV 越界时取边缘值):

```rust
use glium::texture::SamplerBehavior;
let sampler = SamplerBehavior {
    wrap_function: (
        glium::texture::SamplerWrapFunction::ClampToEdge,
        glium::texture::SamplerWrapFunction::ClampToEdge,
        glium::texture::SamplerWrapFunction::ClampToEdge,
    ),
    ..Default::default()
};

let sampled = glium::uniforms::Sampler(&voxel_light, sampler);
```

或 fragment shader 里手动 clamp:

```glsl
vec3 uvw = clamp((v_world_position - grid_min) / grid_extent, 0.0, 1.0);
```

### Debug 视图的重要性

多网格 GI 是复杂算法,**调试必须分步**:

- Phong only:验证 Phong 光照正确
- GI only:验证体素光照有合理分布(角落暗、光源处亮)
- Phong + GI:验证合成正确

每个 debug view 独立测试,确保各组件正确。

## 3 · 四域深入

### 3.1 · 🎮 游戏编程视角

工业界 GI 采样:

- **Light probes**:不采样 3D 纹理,采样**球谐系数**(spherical harmonics),记录"该位置的光照分布"
- **DDGI**:类似 light probes,但动态更新
- **VXGI**:cone tracing 而非简单采样

Casey 的"采样 3D 纹理"是最简单方案,适合教学。

### 3.2 · 🎨 图形学视角

**3D 纹理采样的细节**:

- **三线性插值**:8 邻居加权(默认)
- **三线性 + mipmap**:不同距离用不同 LOD(自动)
- **各向异性过滤**:斜视角时拉长采样核(各向异性 = 方向性)
- **立方体采样**:`samplerCube` 用于环境贴图

**性能考虑**:3D 纹理采样比 2D 慢(8 邻居 vs 4),但 GPU 内置硬件加速,实际差距小。

### 3.3 · 🐧 Linux 系统编程视角

3D 纹理在 Linux GPU 的支持:

```bash
# 检查 3D 纹理支持
glxinfo | grep "GL_TEXTURE_3D"
# 应该全支持

# 最大 3D 纹理尺寸
glxinfo | grep "Max 3D texture size"
# 典型 2048(意味着最大 2048³,但显存限制让 64³-256³ 实用)

# Mesa 实现
# 全驱动支持硬件 3D 纹理
```

### 3.4 · 🦀 Rust 生态视角

Rust + glium 的 3D 纹理:

```rust
use glium::texture::{Texture3D, UncompressedFloatFormat, MipmapsOption};

let voxel_texture = Texture3D::empty_with_format(
    &display,
    UncompressedFloatFormat::F32F32F32F32,
    MipmapsOption::NoMipmap,
    64, 64, 64,
)?;

// 写入数据
let writer = voxel_texture.main_level().write();
writer.write(glium::texture::RawImage3d::from_raw(
    glium::texture::ClientFormat::F32F32F32F32,
    glium::texture::PixelValue::F32F32F32F32,
    (64, 64, 64),
    voxel_data,
));

// 在 shader 里使用
let uniforms = uniform! {
    voxel_light: &voxel_texture,
    // ...
};
```

## 4 · 认知地图

### 4.1 上级

- **GI 渲染**(GI rendering)— 显示间接光的过程
- **3D 纹理采样** — GPU 内置功能
- **空间映射**(Space mapping)— world space ↔ texture space

### 4.2 同级

| GI 显示方法 | 实现 | 质量 |
|---|---|---|
| **3D 纹理采样** | texture(sampler3D, uvw) | 中(Casey 用) |
| Light probes | 球谐系数采样 | 高(工业界) |
| Cone tracing | 沿圆锥采样 3D 纹理 | 高 |
| Path tracing | 每像素随机反射 | 最高 |

### 4.3 下级

- **sampler3D** — GLSL 3D 纹理 sampler
- **Trilinear filtering** — 3D 三线性插值
- **Grid min / extent** — 网格在世界坐标的范围
- **Debug view** — 切换显示组件

## 5 · 对照与变奏

### 同一问题在不同语境下的变体

**问题**:把 3D 数据显示到 2D 屏幕。

| 语境 | 方法 |
|---|---|
| GI 渲染 | 3D 纹理采样 |
| MRI / CT 可视化 | 切片 + 三平面重建 |
| 体积渲染(烟雾) | Ray marching |
| 地形高度图 | displacement mapping |

**核心模式**:**采样 + 投影**(3D → 2D)。

### 历史演化

- **1990s**:3D 纹理引入(OpenGL 1.2)
- **2000s**:Volume rendering 流行(MRI / 烟雾)
- **2010s**:Real-time GI 用 3D 纹理(LPV)
- **2020s**:RTX 直接采样 radiance,3D 纹理用于缓存

## 6 · 关联 Day

- **铺垫**:
  - [day395](day395.md) — V-cycle 完成
  - [day392](day392.md) — G-buffer(position 用于采样)
- **当天**:day396 — 显示 GI
- **后续**:
  - [day397](day397.md) — 深度剥离 → 光照数据
  - [day398](day398.md) — 把光照应用到深度剥离

## 7 · 变式训练

### Lv1 · 概念辨析

**题**:为什么 GI 采样用 world position 而非 screen UV?

**参考解答**:

GI 是世界空间的属性——某点的间接光强度只取决于该点在场景中的位置,和相机视角无关。所以采样必须用 **world position** 转换为体素网格坐标。

screen UV 只在屏幕空间有意义——同一 world position 在不同相机视角下 screen UV 不同。如果用 screen UV,相机移动时 GI 会"漂移",不真实。

GI 采样是 view-independent(与视角无关),这是 GI 算法的核心特性。

### Lv2 · 动手实践

**题**:在 Day 392 G-buffer demo 基础上加 GI 采样。给定一个 32³ 的 voxel light texture(预先填充渐变数据),fragment shader 采样它并加到 Phong 之上。

完成标准:画面有 GI 贡献,debug 视图切换工作。

**参考解答**:

```rust
use glium::texture::Texture3D;

// 创建 32³ 体素纹理(预填充:中心 1.0,边缘 0.0)
let mut voxel_data = vec![0.0f32; 32 * 32 * 32 * 4];
for k in 0..32 {
    for j in 0..32 {
        for i in 0..32 {
            let dx = i as f32 - 16.0;
            let dy = j as f32 - 16.0;
            let dz = k as f32 - 16.0;
            let dist = (dx*dx + dy*dy + dz*dz).sqrt();
            let val = (1.0 - dist / 16.0).max(0.0);
            let idx = (k * 32 * 32 + j * 32 + i) * 4;
            voxel_data[idx] = val * 1.0;  // R
            voxel_data[idx + 1] = val * 0.3;  // G
            voxel_data[idx + 2] = val * 0.3;  // B
            voxel_data[idx + 3] = 1.0;  // A
        }
    }
}

let voxel_texture = Texture3D::with_format(
    &display,
    glium::texture::RawImage3d {
        data: std::borrow::Cow::Owned(voxel_data),
        width: 32, height: 32, depth: 32,
        format: glium::texture::ClientFormat::F32F32F32F32,
    },
    glium::texture::UncompressedFloatFormat::F32F32F32F32,
    glium::texture::MipmapsOption::NoMipmap,
).unwrap();

// 在 lighting shader 里加:
let uniforms = uniform! {
    // ...
    voxel_light: &voxel_texture,
    grid_min: [-1.6f32, -1.6, -1.6],
    grid_extent: [3.2f32, 3.2, 3.2],
    debug_view: 0,  // 0=phong+gi, 1=phong, 2=gi
};
```

Fragment shader:

```glsl
uniform sampler3D voxel_light;
uniform vec3 grid_min;
uniform vec3 grid_extent;
uniform int debug_view;
// ...

void main() {
    // Phong...
    
    // GI
    vec3 uvw = (v_world_position - grid_min) / grid_extent;
    vec3 gi = texture(voxel_light, uvw).rgb * albedo;
    
    vec3 final_color;
    if (debug_view == 0) final_color = phong + gi;
    else if (debug_view == 1) final_color = phong;
    else final_color = gi;
    
    // ...
}
```

### Lv3 · 迁移设计

**题**:3D 纹理采样迁移到**音频混响**——你想根据玩家位置采样房间的混响特性(混响时间 / 高频衰减)。

- 房间是个 3D 空间,每个位置有不同混响特性
- 用 3D 纹理存混响参数
- 玩家位置 → 3D 纹理坐标 → 采样混响参数 → 应用到音频

写下设计。

### Lv4 · 开源贡献

**题**:`bevy` 的 3D 纹理使用,GitHub: https://github.com/bevyengine/bevy

1. clone 仓库,看 3D 纹理相关:
   ```bash
   gh repo clone bevyengine/bevy
   cd bevy
   grep -rn "Texture3d\|Image3d" crates/bevy_render/src/ | head
   ```
2. 看 bevy 怎么用 3D 纹理(可能用于 volume / GI)。
3. **可能的贡献方向**:
   - 补 doc 关于 3D 纹理
   - 加 3D 纹理 example
   - 实现简单 GI
4. 写下 PR 描述。

## 8 · Rust / Arch 落地代码

### 完整 GI 采样 demo

```toml
[package]
name = "gi_sampling"
version = "0.1.0"
edition = "2021"
[dependencies]
glium = "0.34"
glam = "0.29"
```

```rust
// src/main.rs(基于 Day 392 G-buffer + 体素纹理)
use glium::{glutin, Surface, implement_vertex, program, index, texture, framebuffer, uniform};
use glam::Vec3;
use std::cell::Cell;

// (省略 G-buffer 创建,见 Day 392)

#[derive(Copy, Clone, Debug)]
enum DebugView { Combined, PhongOnly, GiOnly }

fn make_voxel_texture(display: &glium::Display) -> texture::Texture3D {
    let mut data = vec![0.0f32; 32 * 32 * 32 * 4];
    for k in 0..32 {
        for j in 0..32 {
            for i in 0..32 {
                let dx = i as f32 - 16.0;
                let dy = j as f32 - 16.0;
                let dz = k as f32 - 16.0;
                let dist = (dx*dx + dy*dy + dz*dz).sqrt();
                let val = (1.0 - dist / 16.0).max(0.0) * 2.0;  // 放大效果
                let idx = (k * 32 * 32 + j * 32 + i) * 4;
                data[idx] = val;
                data[idx + 1] = val * 0.5;
                data[idx + 2] = val * 0.5;
                data[idx + 3] = 1.0;
            }
        }
    }

    texture::Texture3D::with_format(
        display,
        texture::RawImage3d {
            data: std::borrow::Cow::Owned(data),
            width: 32, height: 32, depth: 32,
            format: texture::ClientFormat::F32F32F32F32,
        },
        texture::UncompressedFloatFormat::F32F32F32F32,
        texture::MipmapsOption::NoMipmap,
    ).unwrap()
}

// fragment shader 用上面 Lv2 的代码
```

### Arch Linux 工具链命令

```bash
# 1. 跑 demo
cargo run --release

# 2. 切换 debug view
# F1:Combined
# F2:Phong only(看到没有 GI 时的画面)
# F3:GI only(看到 GI 体素分布)

# 3. 用 RenderDoc 看 3D 纹理
renderdoc &
# 抓帧 → Texture Viewer → 看到 32³ 体素纹理(可旋转查看)

# 4. 性能
sudo pacman -S perf
perf stat -e instructions ./target/release/gi_sampling

# 5. 调试 uvw 坐标
# fragment shader 加:frag = vec4(uvw, 1.0);
# 看到 RGB = 归一化 world position,验证坐标转换正确
```


### 深度推导:本日相关数学与算法

要真正掌握今天的内容,我们必须把背后的数学推导展开。这一节是"为什么这么做"的论证。

**算法复杂度的精确分析**。今天的代码涉及到对实体集合排序,基准复杂度:

- 最优比较排序下界:O(N log N)(决策树理论)
- Rust `sort_by`:TimSort,平均 O(N log N),最好 O(N)(已部分有序时)
- 拓扑排序:O(V + E),V 是节点数,E 是边数
- Radix sort:O(N × k),k 是 sort key 位数——非比较排序,可突破 O(N log N)

对一个 1000 sprite 的场景,TimSort 大约 10000 次比较,每次比较 5-10 个 cycle(分支少),总 50000-100000 cycle = 20-40 μs。在 60 FPS(16666 μs 预算)占 0.2%——可忽略。100K sprite 时升到 2 ms,开始有压力。Minecraft 这种规模的游戏必须用 radix sort 或 GPU sort。

**内存访问模式**。sort 是**非线性内存访问**的典型——每次比较都可能跳到 Vec 不同位置。CPU prefetcher 抓不到模式,cache miss 频繁。优化:

1. **Sort by index**:先 sort indices(u32 数组,4 字节/项,cache 友好),再 reorder 数据。indices 1000 个 = 4 KB,L1 cache 装得下。
2. **Batch reorder**:indices 排好后,顺序遍历 indices,顺序访问原数据(虽然是间接但 prefetcher 跟得上)。

```rust
let mut indices: Vec<u32> = (0..entities.len() as u32).collect();
indices.sort_by(|&a, &b| sort_rule(&entities[a as usize], &entities[b as usize]));
let sorted: Vec<&Entity> = indices.iter().map(|&i| &entities[i as usize]).collect();
```

100K sprite 的 sort 用这个技巧能加速 30%。

**位运算打包 sort key**。今天 sort_key 是 u64,可以塞进多个字段:

```
bits 63-56: layer (8 bits, 256 layers)
bits 55-40: bottom_y (16 bits, 65536 levels)
bits 39-24: x (16 bits)
bits 23-8: sub_layer (16 bits)
bits 7-0: spawn_id (8 bits, 256 entries per slot)
```

打包:`key = (layer << 56) | (bottom_y << 40) | (x << 24) | (sub << 8) | spawn`。一次 u64 比较,无分支,Rust `sort_by_key` 自动 inline。

**部分序的图论模型**。Day 304 会用拓扑排序处理真正的部分序。每个 sprite 是图节点,如果 A 应该在 B 前面,加边 A→B。拓扑排序给出节点的合法顺序(满足所有约束)。

拓扑排序算法:

1. **Kahn 算法**:每次找入度为 0 的节点,加入结果,删掉它的出边,重复。
2. **DFS 后序**:DFS 遍历,记录后序(post-order),反转得到拓扑序。

如果图有环(循环依赖),拓扑排序失败——意味着 sprite 排序无解,必须**人为拆解**(切开某个 sprite)。

**浮点数的稳定性**。Rust 的 `f32::partial_cmp` 处理 NaN 返回 `None`,sort 会 panic。所以游戏代码里要么保证所有 f32 都不是 NaN(数学保证),要么 `partial_cmp(...).unwrap_or(Equal)`(默认相等)。今天 sort_rule 用后者。

更稳妥的方法:**所有位置用整数**(像素单位)。`pos_y: i32` 没有 NaN 问题,比较无分支。但缩放和旋转要用浮点,所以游戏用 i32 + f32 混合。

### 真实代码片段

Casey 在 HH 视频里实际写的代码风格(简化伪代码):

```c
internal int32
SortRule(entity *A, entity *B)
{
    int32 Result = 0;

    // Layer 比较
    if (A->Layer != B->Layer) {
        Result = (int32)A->Layer - (int32)B->Layer;
    } else {
        // Bounds overlap 检查
        rectangle2 BoundsA = GetEntityBounds(A);
        rectangle2 BoundsB = GetEntityBounds(B);
        if (RectanglesOverlap(BoundsA, BoundsB)) {
            // Bottom y 比较
            real32 BottomA = BoundsA.Max.Y;
            real32 BottomB = BoundsB.Max.Y;
            if (BottomA != BottomB) {
                Result = (BottomA < BottomB) ? -1 : 1;
            } else {
                // x 比较
                Result = SignOf32(A->P.X - B->P.X);
            }
        } else {
            // 不重叠:任意顺序
            Result = A->SpawnID - B->SpawnID;
        }
    }

    return Result;
}
```

注意 Casey 的 C 风格:

- 直接返回 int32(而非 enum Ordering)
- 用 `SignOf32`(返回 -1/0/1)简化比较
- if-else 链而非 Rust 的 `then_with`

Rust 等价:

```rust
fn sort_rule(a: &Entity, b: &Entity) -> Ordering {
    a.layer.cmp(&b.layer)
        .then_with(|| {
            if bounds_overlap(a, b) {
                let ba = bounds(a).max.y;
                let bb = bounds(b).max.y;
                ba.partial_cmp(&bb).unwrap_or(Ordering::Equal)
                    .then(a.pos.x.partial_cmp(&b.pos.x).unwrap_or(Ordering::Equal))
            } else {
                a.spawn_id.cmp(&b.spawn_id)
            }
        })
}
```

### 跨学科联结:从游戏到数据库

sort_rule 的本质是"复杂比较函数",这在很多领域都出现:

- **数据库 ORDER BY**:SQL 的 `ORDER BY layer, y, x` 是同样的 multi-key sort,但 SQL 是声明式(数据库优化器选 sort 算法),Casey 是命令式(手写 sort_by)。
- **React diff**:React 用 sort + key 比较 virtual DOM,决定哪些 component 重渲染。Multi-key sort 决定 diff 顺序。
- **Linux kernel CFS**(Completely Fair Scheduler):按 vruntime 排序进程,选最小 vruntime 的运行。本质是 sort_rule。
- **Git log --sort**:Git commit 按 committer date + author date + subject 排序。

Casey 在 HH 反复说"很多算法本质相同,只是命名不同"。掌握一个领域的 sort_rule,就掌握所有领域。

### 阅读路径建议

如果你被今天的概念绊倒,按这个顺序补:

1. **不熟 Ordering** → 读 Rust 文档 `std::cmp::Ord`、`std::cmp::Ordering`
2. **不熟闭包** → 读 Rust Book ch.13 Closures
3. **不熟 sort_by** → 读 `slice::sort_by` 文档,自己写 benchmark
4. **不熟 sort_key** → 读 `slice::sort_by_key`,理解 cache 行为
5. **不熟 bounds** → 读 bevy 的 sprite.rs,Aabb 实现
6. **不熟拓扑排序** → 读 Introduction to Algorithms (CLRS) ch.22 graph algorithms

每一步都跑代码验证,不要只读理论。


### 进一步展开:从概念到落地的完整路径

要把今天讨论的抽象概念变成可运行的代码,我们必须经历几个具体阶段。这一节展开每一步的工程细节。

**阶段一:定义接口**。Casey 在动手实现前,先想清楚 API。Rust 的 trait 是定义接口的工具:

```rust
trait Sortable {
    fn sort_key(&self) -> u64;
    fn bounds(&self) -> Aabb;
    fn layer(&self) -> u8;
}
```

trait 让代码可测试、可替换。Casey 经常说"先写 trait,再写 impl"——这强迫你思考抽象边界。

**阶段二:单元测试**。每个 sort_rule 分支都应有对应测试:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_basic_ordering() {
        let a = TestEntity { layer: 1, y: 100.0 };
        let b = TestEntity { layer: 1, y: 200.0 };
        assert_eq!(a.sort_key() < b.sort_key(), true);
    }

    #[test]
    fn test_layer_priority() {
        let a = TestEntity { layer: 2, y: 100.0 };
        let b = TestEntity { layer: 1, y: 200.0 };
        // layer 1 优先(a 在 b 后面)
        assert_eq!(a.sort_key() > b.sort_key(), true);
    }
}
```

跑 `cargo test`,所有测试必须通过才能进入下一阶段。

**阶段三:集成测试**。单元测试覆盖单个函数,集成测试覆盖整个系统。游戏里的集成测试通常是"加载场景,跑一帧,截图,对比期望截图"——visual regression test。这是 Casey debug 流程的标准化版本。

**阶段四:性能剖析**。用 `cargo flamegraph` 或 `perf record` 看 CPU 时间花在哪。如果 sort 占 0.5%,不用优化。占 20%,必优化。

```bash
sudo pacman -S flamegraph
cargo install flamegraph
cargo flamegraph --bin game
# 生成的 SVG 在浏览器打开,看热路径
```

**阶段五:跨平台验证**。Linux 测试用 `vulkaninfo | grep "Device name"` 看 GPU;Windows 测试用 dxdiag。同一代码在 Intel / AMD / NVIDIA / Apple Silicon GPU 上可能有微妙差异——精度、driver bug、性能特性。

### 深度概念:数据导向设计

Casey Muratori 的标志性观点是 **Data-Oriented Design**(DOD,数据导向设计),与传统的 Object-Oriented Programming(OOP)对立。今天的代码隐含 DOD 思想。

OOP 把数据和方法绑定:

```rust
// OOP 风格
struct Player {
    pos: Vec3,
    inventory: Vec<Item>,
    fn update(&mut self, dt: f32) { ... }
    fn render(&self, canvas: &mut Canvas) { ... }
}
```

每 Player 是个"对象",有数据有方法。看起来自然,但 cache 不友好——Player 的 pos、inventory、方法指针散在不同内存区域,CPU 访问要跳来跳去。

DOP 把数据按"访问模式"分组:

```rust
// DOP 风格
struct World {
    positions: Vec<Vec3>,      // 所有实体的位置
    velocities: Vec<Vec3>,     // 所有实体的速度
    inventories: Vec<Vec<Item>>,  // 所有实体的库存
}

fn update_positions(world: &mut World, dt: f32) {
    for (pos, vel) in world.positions.iter_mut().zip(&world.velocities) {
        *pos += *vel * dt;
    }
}
```

所有 positions 在一段连续内存,CPU prefetcher 跟得上,cache 命中率 99%+。100 万实体的 update 在现代 CPU 上 < 1 ms。

Casey 在 HH day 305+ 大量用 DOD,memory arena 是基础设施。今天的 sort_rule 也隐含 DOD——按 layer + bottom_y 排序,本质上是把"位置数据"和"渲染顺序"分开。

**与 ECS 的关系**。Entity-Component-System(ECS)是 DOD 的极致化:数据完全按 Component 分(Component = 数据,无方法),System 是函数(Component → 行为)。bevy / Unity DOTS / Unreal Mass 都用 ECS。

学完 HH 你能直接读懂 bevy 源码——它的 `Query<(&Transform, &mut Velocity)>` 就是 Casey 的"positions + velocities"。

### 渲染管线的完整链路

今天讨论的 sort_rule 是渲染管线的一小步。完整的渲染流程:

```
1. 游戏逻辑更新(物理、AI、输入)
2. 收集渲染数据(可见性裁剪、LOD 选择)
3. 构建 sort_rule(今天的重点)
4. 排序所有可见 sprite
5. 提交到 GPU(或软渲染)
6. GPU 顶点 shader
7. 光栅化
8. 片段 shader(光照、纹理)
9. 后处理(色调映射、bloom、AA)
10. 显示
```

步骤 3-5 是 CPU 渲染,6-9 是 GPU 渲染。今天的 sort_rule 在步骤 3——决定绘制顺序。Phase 8 切 GPU 后,步骤 4-5 改为 batch + indirect draw,但 sort 仍在 CPU(因为 GPU sort 复杂,且通常不是瓶颈)。

工业级渲染引擎(Unreal Lumen、Unity HDRP)在每一步都做了大量优化:

- 步骤 2:用 BVH 加速可见性
- 步骤 4:用 radix sort + SIMD
- 步骤 5:用 indirect draw + MultiDrawIndirect
- 步骤 8:用 Bindless resources 减少 descriptor 更新

这些优化在 Casey HH 后期逐步引入。

### 常见 bug 与解决方案

**Bug 1: Sprite flicker**(闪烁)。两 sprite sort key 相同,每帧顺序不同。解决:加 spawn_id tie-breaker,保证稳定。

**Bug 2: Z-fighting**(深度冲突)。3D 渲染时两个 polygon 在同一深度,GPU depth buffer 精度不够,出现条纹。解决:用 reverse-Z(Phase 8)或调整 near plane。

**Bug 3: Sprite 在帧间跳跃**。物理引擎和渲染不同步,显示的 sprite 位置和实际位置差一帧。解决:固定时间步 + 插值渲染(Game Loop Pattern)。

**Bug 4: NaN 在 sort_key**。某 sprite 的 pos 计算出错产生 NaN,sort_key 是 NaN,sort 顺序不确定。解决:`partial_cmp(...).unwrap_or(Equal)`,且 debug 时检测 NaN 并 panic。

**Bug 5: 排序逆方向**。`sort_by(|a, b| ...)` 写反了 a 和 b,导致排序颠倒。解决:加单元测试覆盖每个分支,且跑 visual test 验证。

每个 bug 都是 Casey 在 HH 里真实遇到并解决的——HH 的价值就在这些"踩坑教学"。

## 9 · 延伸阅读



本仓库本地:
- [day395](day395.md) — V-cycle 完成
- [day392](day392.md) — G-buffer 创建
- [day397](day397.md) — 深度剥离 → 光照数据
- [days/reference/hh-slices/phase-6.json](../reference/hh-slices/phase-6.json) — day396 lesson 数据

外部稳定 URL(可选):
- LearnOpenGL 3D textures: https://learnopengl.com/Advanced-OpenGL/Depth-testing
- OpenGL sampler3D: https://www.khronos.org/opengl/wiki/Sampler_(GLSL)#Sampler_types
- glium Texture3D: https://docs.rs/glium/latest/glium/texture/struct.Texture3D.html

真实开源源码:
- `bevy` Image / 3D texture: https://github.com/bevyengine/bevy/blob/main/crates/bevy_render/src/texture/image.rs
- Casey HH 原版 C 代码 day396: https://github.com/HandmadeHero/handmade-hero/tree/main/code/day396
