
# Day 304 · 构建与遍历图

> 昨天 Y/Z 分离失败。今天 Casey 启用**图论**武器——把每个 sprite 当节点,遮挡关系当边,构建有向图。然后用**拓扑排序**遍历图,得到正确的渲染顺序。这是 HH 最"算法密集"的一天。

## 0 · 为什么要有这一天

day 302 证明 sprite 排序本质是**部分序**——某些 sprite 配置下,无法线性排。day 303 的 Y/Z 分离失败。今天用图论解决。

图论的核心抽象:**节点(node)+ 边(edge)**。把 sprite 当节点,"A 应该在 B 之前画"当边,构建有向图。然后拓扑排序这个图,得到一个"满足所有约束的顺序"。

具体来说:

- 节点 = sprite
- 边 (A → B) = "A 应该在 B 之前画"(B 遮挡 A,或 A 在 B 后)
- 拓扑排序 = 找到节点顺序,使所有边的方向一致

拓扑排序的算法:**Kahn 算法**或**DFS**。两者的核心都是:

1. 找到入度为 0 的节点(无前置依赖)
2. 输出该节点,移除其出边
3. 重复

如果中途找不到入度 0 的节点但还有节点未输出,**有环**——图不是 DAG,拓扑排序失败。

今天的代码量:加图数据结构、加拓扑排序算法、改 render 调用。是 day 300-303 的"终极方案"。

心理锚点:看完今天,你应该理解拓扑排序的算法,能在其他场景(任务依赖、import 顺序)复用。

## 1 · Casey 今天做了什么

Casey 今天的实现:

1. **设计图数据结构**——节点是 entity index,边是遮挡关系
2. **构建图**——遍历所有 entity 对,判断遮挡关系,加边
3. **拓扑排序**——Kahn 算法,从入度 0 开始
4. **环检测**——如果拓扑排序失败,有环,加 tie-breaker
5. **替换 render 主循环**——按拓扑顺序画 sprite

今天的核心是**算法实现**。Casey 在 C 里手写,我们用 Rust 同样实现。

## 2 · 心智模型

### 类比:大学选课

把课程想象成 sprite,课程依赖是边:

- 数学 101 → 物理 101(先修数学才能学物理)
- 物理 101 → 量子力学
- 数学 101 → 微积分

构建图后,拓扑排序给出"合理的修课顺序":数学 101 → 物理 101 → 量子力学;数学 101 → 微积分。

如果有**循环依赖**(课程 A 是 B 的先修,B 是 A 的先修),无法修——拓扑排序检测到环,报警。

游戏 sprite 排序完全一样:

- 玩家 → 树(树遮挡玩家,树后画)
- 树 → 地面 tile(地面在最远,先画)
- 玩家 → 怪物(谁前?)

构建图 + 拓扑排序 = 正确渲染顺序。

### 2.1 图数据结构

```rust
struct OcclusionGraph {
    nodes: usize,  // 节点数(等于 entity 数)
    edges: Vec<(usize, usize)>,  // 边列表:(a, b) 表示 a 在 b 前
    adj_out: Vec<Vec<usize>>,    // 邻接表:a -> [b1, b2, ...]
    adj_in: Vec<Vec<usize>>,     // 反向:b -> [a1, a2, ...](谁指向 b)
}
```

**adj_out** 是"出边邻接表"——每个节点指向哪些节点。
**adj_in** 是"入边邻接表"——每个节点被哪些节点指向。

Kahn 算法用 adj_in(算入度)。

### 2.2 构建图

```rust
fn build_occlusion_graph(entities: &[Entity]) -> OcclusionGraph {
    let n = entities.len();
    let mut graph = OcclusionGraph {
        nodes: n,
        edges: vec![],
        adj_out: vec![vec![]; n],
        adj_in: vec![vec![]; n],
    };

    for i in 0..n {
        for j in 0..n {
            if i == j { continue; }
            let a = &entities[i];
            let b = &entities[j];
            // a 被 b 遮挡 当:b bounds 与 a 重叠,且 b.bottom > a.bottom
            if a.bounds.overlaps(&b.bounds) && b.bounds.bottom() > a.bounds.bottom() {
                graph.edges.push((i, j));  // i 在 j 前
                graph.adj_out[i].push(j);
                graph.adj_in[j].push(i);
            }
        }
    }

    graph
}
```

O(n²) 构建——对每个 entity 对判断一次。n=100 时是 10000 次,可接受。n=10000 时是 1 亿次,要优化(day 309 的 grid)。

### 2.3 Kahn 算法拓扑排序

```rust
fn topo_sort(graph: &OcclusionGraph) -> Result<Vec<usize>, Vec<usize>> {
    let n = graph.nodes;
    let mut in_degree: Vec<usize> = (0..n).map(|i| graph.adj_in[i].len()).collect();
    let mut queue: Vec<usize> = (0..n).filter(|&i| in_degree[i] == 0).collect();
    let mut order = vec![];

    while let Some(node) = queue.pop() {
        order.push(node);
        for &next in &graph.adj_out[node] {
            in_degree[next] -= 1;
            if in_degree[next] == 0 {
                queue.push(next);
            }
        }
    }

    if order.len() == n {
        Ok(order)
    } else {
        // 有环,返回环涉及的节点
        let cycle_nodes: Vec<usize> = (0..n).filter(|&i| in_degree[i] > 0).collect();
        Err(cycle_nodes)
    }
}
```

Kahn 算法步骤:

1. 算每个节点入度
2. 入度 0 的入队列
3. 出队一个节点,加入 order,把它所有出边"消去"(对端入度 -1,变成 0 入队)
4. 重复

如果队列空时还有节点未处理,这些节点都在环里——环的入度永远 > 0。

### 2.4 渲染主循环

```rust
fn render_topo(entities: &[Entity]) {
    let graph = build_occlusion_graph(entities);
    match topo_sort(&graph) {
        Ok(order) => {
            for idx in order {
                draw(&entities[idx]);
            }
        }
        Err(cycle_nodes) => {
            // 环:加 tie-breaker(spawn_id)
            println!("Cycle detected among: {:?}", cycle_nodes);
            // 简化:用 spawn_id 排环内节点
            // 实际实现更复杂
            for e in entities {
                draw(e);
            }
        }
    }
}
```

拓扑排序成功时,按顺序画就是正确渲染。失败时(环),退化到简化方案。

### 2.5 性能分析

| 步骤 | 时间 | 空间 |
|---|---|---|
| 构建图 | O(n²) | O(n + E) |
| 拓扑排序 | O(n + E) | O(n) |
| 总 | O(n²) | O(n²) |

n=100 时,O(10000) ≈ 微秒级。可接受。

n=1000 时,O(1M) ≈ 毫秒级。开始压力。

n=10000+ 时,要空间优化(day 309 的 grid)。

---

**总结**:今天的核心是**图论**——节点 + 边表达遮挡关系,Kahn 算法拓扑排序。处理 day 302 的"没有全序"问题。环检测是 day 308 的主题。

## 3 · 多域深入

### 3.1 · 🎮 游戏编程视角

游戏行业用拓扑排序的场景:

- **Sprite 排序**(今天)
- **Task graph**:游戏引擎的 frame graph,任务间有依赖
- **Material sorting**:shader 编译顺序(依赖其他 shader)
- **Asset loading**:资产依赖(mesh 依赖 texture)

商业引擎(Bevy、Unreal)都用拓扑排序处理这些。Casey 在 HH 教学手写,生产代码用 `petgraph` 或引擎内置。

### 3.2 · 🐧 Linux 系统编程视角

Linux 内核用拓扑排序的场景:

- **Module init order**:内核模块有依赖(`MODULE_DEPEND`),拓扑排序决定加载顺序
- **kworker tasks**:工作队列有依赖
- **device probe order**:PCI 设备拓扑排序

```bash
# 看模块依赖
modinfo ext4
# 输出:depends: mbcache, jbd2
# ext4 依赖这两个,加载顺序:mbcache → jbd2 → ext4
```

内核的 modprobe 工具内部用拓扑排序决定加载顺序。

### 3.3 · 🦀 Rust 生态视角

Rust 的 `petgraph` crate:

```rust
use petgraph::graph::DiGraph;
use petgraph::algo::toposort;

let mut g = DiGraph::new();
let a = g.add_node("A");
let b = g.add_node("B");
let c = g.add_node("C");
g.add_edge(a, b, ());
g.add_edge(b, c, ());

match toposort(&g, None) {
    Ok(order) => println!("{:?}", order),
    Err(cycle) => println!("Cycle: {:?}", cycle),
}
```

工业代码用 petgraph,教学代码手写(理解算法)。

**Kahn vs DFS 拓扑排序**:

- **Kahn**:BFS 风格,反复找入度 0
- **DFS**:递归访问,访问完后加入 order(逆序输出)

两者结果都正确,Kahn 更直观(模拟"逐步消去"),DFS 更适合递归友好场景。

### 3.4 · 🎨 图形学视角

图形学的"图"对应 **scene graph**——3D 场景里的物体按父子关系组织:

- Root → World
  - Root → Camera
  - Root → Lights
  - Root → Models
    - Model → Mesh
    - Model → Material

Scene graph 的遍历是树的 DFS(特殊图)。Unity、Unreal、Godot 都用 scene graph。

游戏 sprite 排序的"图"是另一种——基于遮挡关系,不是父子关系。两种图不同用途。

## 4 · 认知地图

### 4.1 上级

- **图论** — 节点 + 边的数学结构
- **拓扑排序** — DAG 的线性化
- **Kahn 算法** — BFS 风格拓扑排序
- **环检测** — DAG 的失败模式

### 4.2 同级

| 方案 | 关键差别 | 何时用 | 本项目选了哪个 |
|---|---|---|---|
| 拓扑排序(今天) | 处理部分序 | 复杂 | ✅ day 304 |
| Sort rule | 全序强制 | 简单 | day 300-301 |
| Y/Z 分离 | 失败 | 不用 | day 303 |
| Depth peeling | 像素级 | 高质量 | Phase 8+ |

### 4.3 下级

- **OcclusionGraph 结构** — 节点 + 边 + 邻接表
- **build 函数** — O(n²) 遍历对
- **Kahn 算法** — 入度 + 队列
- **环检测** — order.len() < n

## 5 · 对照与变奏

### 不同拓扑排序算法

| 算法 | 风格 | 时间 |
|---|---|---|
| Kahn | BFS,入度 | O(V+E) |
| DFS | 递归,post-order | O(V+E) |
| Tarjan SCC | 强连通分量 | O(V+E) |
| Lexicographic topo | 字典序最小 | O(V+E) |

Kahn 简单直观。Tarjan 处理环更优雅。Lexicographical 适合"有多个合法序,选最小"的场景。

### 图的不同表示

| 表示 | 空间 | 查询 |
|---|---|---|
| Edge list | O(E) | O(E) 找邻居 |
| Adjacency list | O(V+E) | O(1) 找邻居 |
| Adjacency matrix | O(V²) | O(1) 查 (i,j) 是否有边 |
| Adjacency map | O(V+E) | O(1) 查 + 删 |

HH 用 adjacency list——平衡空间和查询。

### 历史演化

- **1960**:Kahn 算法发表
- **1962**:Tarjan 出生(后续贡献 SCC 算法)
- **1970s**:拓扑排序在编译器(语句依赖)
- **1990s**:游戏引擎用 scene graph
- **2010s**:Bevy 等用 ECS 替代 scene graph
- **2020s**:并行拓扑排序(大数据)

## 6 · 关联 Day

- **铺垫**:
  - [day302](day302.md) — 确认没有全序
  - [day303](day303.md) — Y/Z 分离失败
- **当天**:day304(本日)
- **后续**:
  - [day305](day305.md) — 在平台层用 arena
  - [day306](day306.md) — 调试图排序
  - [day308](day308.md) — 环检测

## 7 · 变式训练

### Lv1 · 概念辨析

**题**:拓扑排序的复杂度为什么是 O(V+E) 而不是 O(V²)?具体哪个步骤避免了平方?

**参考解答**:Kahn 算法的步骤:

1. 算入度:遍历所有边,O(E)
2. 找入度 0:遍历所有节点,O(V)
3. 处理节点:每节点处理一次,每边访问一次(消去入度),O(V+E)

总 O(V+E)。

如果用邻接矩阵表示,步骤 1 变成 O(V²)(遍历矩阵),所以邻接矩阵下整体 O(V²)。邻接表的 O(V+E) 更高效。

### Lv2 · 动手实践

**题**:实现 Kahn 拓扑排序,处理 5 个 entity 的遮挡关系。验证顺序正确。

完成标准:给 5 个 entity 加几条边,输出拓扑顺序满足所有边方向。

**参考解答**:

```rust
#[derive(Debug)]
struct Graph {
    n: usize,
    adj_out: Vec<Vec<usize>>,
    adj_in: Vec<Vec<usize>>,
}

impl Graph {
    fn new(n: usize) -> Self {
        Self {
            n,
            adj_out: vec![vec![]; n],
            adj_in: vec![vec![]; n],
        }
    }

    fn add_edge(&mut self, from: usize, to: usize) {
        // from 在 to 前(to 遮挡 from,所以 from 先画)
        self.adj_out[from].push(to);
        self.adj_in[to].push(from);
    }
}

fn kahn_topo_sort(g: &Graph) -> Result<Vec<usize>, Vec<usize>> {
    let n = g.n;
    let mut in_degree: Vec<usize> = (0..n).map(|i| g.adj_in[i].len()).collect();
    let mut queue: Vec<usize> = (0..n).filter(|&i| in_degree[i] == 0).collect();
    let mut order = vec![];

    while let Some(node) = queue.pop() {
        order.push(node);
        // 复制出边避免借用问题
        let outs: Vec<usize> = g.adj_out[node].clone();
        for next in outs {
            in_degree[next] -= 1;
            if in_degree[next] == 0 {
                queue.push(next);
            }
        }
    }

    if order.len() == n {
        Ok(order)
    } else {
        let cycle: Vec<usize> = (0..n).filter(|&i| in_degree[i] > 0).collect();
        Err(cycle)
    }
}

fn main() {
    // 5 个 entity,遮挡关系:
    // 0(ground) 在 1(player) 前
    // 0 在 2(monster) 前
    // 1 在 3(glove) 前
    // 2 在 3 前
    // 4(tree) 在 1 前
    let mut g = Graph::new(5);
    g.add_edge(0, 1);
    g.add_edge(0, 2);
    g.add_edge(1, 3);
    g.add_edge(2, 3);
    g.add_edge(4, 1);

    match kahn_topo_sort(&g) {
        Ok(order) => {
            println!("Topo order: {:?}", order);
            // 一种可能:0, 4, 2, 1, 3
            // 验证所有边方向
            let pos: std::collections::HashMap<usize, usize> = order.iter()
                .enumerate().map(|(i, &n)| (n, i)).collect();
            for from in 0..5 {
                for &to in &g.adj_out[from] {
                    assert!(pos[&from] < pos[&to],
                        "Edge {} -> {} violated", from, to);
                }
            }
            println!("All edges satisfied!");
        }
        Err(cycle) => println!("Cycle: {:?}", cycle),
    }
}
```

**每行解释**:
- `add_edge(from, to)` — 添加 from -> to 边
- `in_degree` — 入度数组,初始化为 adj_in[i].len()
- `queue.pop()` — 出队一个入度 0 的节点
- `outs.clone()` — 复制出边避免借用 g.adj_out[next] 时同时改 in_degree
- 验证:所有边的 from 必须在 to 之前(order 中索引小)

### Lv3 · 迁移设计

**题**:把拓扑排序迁移到**JS 打包工具**(如 webpack)。模块有 import 依赖,A imports B 表示 B 必须先加载。设计 bundle 算法:

- 解析所有模块的 import
- 构建依赖图
- 拓扑排序
- 按序拼接 bundle

### Lv4 · 开源贡献

**题**:`petgraph` 的 toposort 实现。

1. clone petgraph
2. 看 `src/algo/toposort.rs`
3. **可能的贡献方向**:
   - 文档缺环检测的例子
   - 缺测试覆盖

写 PR 描述。

## 8 · Rust / Arch 落地代码

### Rust 完整可跑示例

§7 Lv2 完整可跑。这里加 debug 输出:

```rust
fn debug_print_graph(g: &Graph, entities: &[Entity]) {
    println!("Occlusion graph:");
    for i in 0..g.n {
        let targets: Vec<String> = g.adj_out[i].iter()
            .map(|&j| entities[j].name.to_string()).collect();
        if !targets.is_empty() {
            println!("  {} -> [{}]", entities[i].name, targets.join(", "));
        }
    }
}

fn main() {
    let entities = vec![
        Entity { pos: Vec2::new(0.0, 0.0), z: 0, bounds: Aabb::from_center(Vec2::new(0.0, 0.0), 100.0, 100.0), name: "Ground" },
        Entity { pos: Vec2::new(0.0, 0.0), z: 1, bounds: Aabb::from_center(Vec2::new(0.0, 0.0), 50.0, 50.0), name: "Player" },
    ];
    let g = build_occlusion_graph(&entities);
    debug_print_graph(&g, &entities);
}
```

### Arch Linux 工具链验证

```bash
cargo new --bin topo_sort
cd topo_sort
cargo run --release

# 验证:拓扑顺序满足所有边
# 用 graphviz 可视化图
sudo pacman -S graphviz
# 在 main 加 dot format 输出:
# println!("digraph G {{");
# for i in 0..g.n { for &j in &g.adj_out[i] { println!("  {} -> {};", i, j); } }
# println!("}}");
# 重定向到 graph.dot
# dot -Tpng graph.dot -o graph.png
```

**Troubleshooting**:

- 拓扑顺序不唯一 —— 多个入度 0 时顺序取决于 queue.pop(),用 BTreeSet 保证字典序
- 性能 —— n=1000 时构建图 O(n²) 慢,day 309 优化
- 环 —— day 308 处理


### 工程实现细节的展开

要把今天的代码真正跑通,我们必须回答几个具体工程问题。

**第一,内存布局**。所有数据结构都要考虑 cache locality。Rust 的 `#[repr(C)]` 保证字段顺序,CPU prefetcher 才能预测下一个访问。对 hot loop 里的结构体,经常用 SoA(Structure of Arrays)而非 AoS(Array of Structures):

```rust
// AoS(Array of Structures)—— 简单但 cache 差
struct Entities { items: Vec<Entity> }  // 每实体 64 字节

// SoA(Structure of Arrays)—— 复杂但 cache 友好
struct Entities {
    positions: Vec<Vec3>,    // 16 字节 × N
    normals: Vec<Vec3>,      // 16 字节 × N
    layers: Vec<u8>,         // 1 字节 × N
}
```

SoA 让 sort 时只读 positions(16 字节/实体),不读其他字段——cache 命中率高 4 倍。Casey 在 HH 后期会切换到 SoA。

**第二,错误处理**。Rust 的 `Result<T, E>` 强制处理错误,但游戏代码经常 `unwrap()`——这是有意的。游戏里的"不可能错"用 unwrap,失败直接 panic,debug 时立刻知道。但**加载资源**(纹理、网格)不能 unwrap——加载失败应该 fallback 到默认资源。

```rust
// 加载纹理——失败 fallback
let tex = load_texture("player.png").unwrap_or_else(|_| {
    eprintln!("Missing player.png, using default");
    default_texture()
});

// 数学计算——不可能失败,unwrap 合理
let normal = (cross(v1, v2)).normalize().unwrap();
```

**第三,并发**。游戏循环经常是单线程逻辑 + 多线程渲染。Rust 的 `Send + Sync` 编译时检查——只要数据没 `Rc` / `RefCell`,就可以跨线程。Casey 的 HH 在 day 305+ 用 memory arena(单线程,无锁,极快);工业级引擎(bevy、Fyrox)用 ECS + rayon 多线程。

**第四,跨平台**。Linux 用 X11 / Wayland,Windows 用 Win32,macOS 用 Cocoa。Casey 的 HH 用 platform layer 抽象——`platform_get_time()`、`platform_swap_buffers()` 等,每平台独立实现。Rust 生态的 `winit` crate 做这事,bevy / wgpu 都基于它。

### 数学严谨化的推导

本节内容相关的核心数学推导:

**向量点积**:a · b = |a||b|cos θ。在排序的语境里,两个 sprite 的"前后关系"可以看作向量在世界空间的点积——投影到视线方向。但 2D 游戏里视线固定(z 方向),所以退化为 y 比较。

**叉积判断**:a × b 的符号判断 b 在 a 的左侧还是右侧。Casey 在 sort_rule 里用叉积决定 sprite 在玩家的哪侧——决定是玩家遮挡 NPC 还是反过来。

**重心坐标**:三角形内任一点 P = αA + βB + γC,α+β+γ=1。fragment shader 接收的 attribute 是顶点 attribute 的重心插值。今天 sort_rule 不涉及 fragment shader,但理解重心坐标对 Phase 8 GPU 渲染至关重要。

**IEEE 754 float**:f32 的位模式不是单调的——负数比正数位模式大。所以 sort key 用 u64 时,f32 要"重新编码"成单调 u32:

```rust
fn f32_to_monotonic_u32(x: f32) -> u32 {
    let bits = x.to_bits();
    if bits & 0x8000_0000 != 0 {
        !bits  // 负数:全部翻转
    } else {
        bits ^ 0x8000_0000  // 正数:翻转符号位
    }
}
```

这样 -1.0 → 0x00000000,0.0 → 0x80000000,+1.0 → 0xBF800000...^0x80000000 = 0x3F800000,单调递增。

### 历史脉络与工业实践

1990 年代 id Software 的 Doom(1993)用 BSP tree + painter's algorithm——排序在编译期完成,运行时 O(N) 遍历。Quake(1996)首次用 Z-buffer(GPU 硬件),彻底解决排序问题。

2D 游戏排序的演化:

- **1985-1990**:无 sort,固定 layer(Super Mario Bros)
- **1990-1995**:Y-sort 在 JRPG 普及(最终幻想 6、超时空之轮)
- **1995-2005**:Multi-layer Y-sort(星际争霸、暗黑破坏神 2)
- **2005-2015**:引擎内置 sort(Unity、GameMaker)
- **2015-2025**:GPU + sort 混合(bevy、Unreal)

Casey 的 HH 在 day 295-310 走了这条 30 年路径——从最简单的 sort_key 到拓扑排序,演示每一阶段的工程权衡。

### 性能优化的常见误区

**误区 1:过早优化**。Casey 经常说"先正确再快"。Day 301 的 sort_rule 用 f32 比较,虽然慢于 u64,但代码清晰。等真的成瓶颈(实测 >5% 帧时间),再换 u64。

**误区 2:微优化大函数**。把 sort_rule 优化 10% 没意义,如果整个 sort 只占帧时间 0.5%。优化要看 profiler 数据,不是直觉。

**误区 3:并行不是银弹**。把 sort 分到多线程,如果数据共享(同 Vec),要锁,锁比串行还慢。Casey 用 thread-local 数据 + merge,避免锁。

**误区 4:cache miss 是性能杀手**。一个 cache miss 100+ cycle,等于 50 个 mul。所以"数据紧凑 + 顺序访问"比"少指令"重要 10 倍。这是 data-oriented design 的核心。



### 深度推导:本日相关数学与算法

要真正掌握今天的内容,我们必须把背后的数学推导展开。这一节是"为什么这么做"的论证。

**算法复杂度的精确分析**。今天的代码涉及到对实体集合排序,基准复杂度:

- 最优比较排序下界:O(N log N)(决策树理论)
- Rust `sort_by`:TimSort,平均 O(N log N),最好 O(N)(已部分有序时)
- 拓扑排序:O(V + E),V 是节点数,E 是边数
- Radix sort:O(N × k),k 是 sort key 位数——非比较排序,可突破 O(N log N)

对一个 1000 sprite 的场景,TimSort 大约 10000 次比较,每次比较 5-10 个 cycle(分支少),总 50000-100000 cycle = 20-40 μs。在 60 FPS(16666 μs 预算)占 0.2%——可忽略。100K sprite 时升到 2 ms,开始有压力。Minecraft 这种规模的游戏必须用 radix sort 或 GPU sort。

**内存访问模式**。sort 是**非线性内存访问**的典型——每次比较都可能跳到 Vec 不同位置。CPU prefetcher 抓不到模式,cache miss 频繁。优化:

1. **Sort by index**:先 sort indices(u32 数组,4 字节/项,cache 友好),再 reorder 数据。indices 1000 个 = 4 KB,L1 cache 装得下。
2. **Batch reorder**:indices 排好后,顺序遍历 indices,顺序访问原数据(虽然是间接但 prefetcher 跟得上)。

```rust
let mut indices: Vec<u32> = (0..entities.len() as u32).collect();
indices.sort_by(|&a, &b| sort_rule(&entities[a as usize], &entities[b as usize]));
let sorted: Vec<&Entity> = indices.iter().map(|&i| &entities[i as usize]).collect();
```

100K sprite 的 sort 用这个技巧能加速 30%。

**位运算打包 sort key**。今天 sort_key 是 u64,可以塞进多个字段:

```
bits 63-56: layer (8 bits, 256 layers)
bits 55-40: bottom_y (16 bits, 65536 levels)
bits 39-24: x (16 bits)
bits 23-8: sub_layer (16 bits)
bits 7-0: spawn_id (8 bits, 256 entries per slot)
```

打包:`key = (layer << 56) | (bottom_y << 40) | (x << 24) | (sub << 8) | spawn`。一次 u64 比较,无分支,Rust `sort_by_key` 自动 inline。

**部分序的图论模型**。Day 304 会用拓扑排序处理真正的部分序。每个 sprite 是图节点,如果 A 应该在 B 前面,加边 A→B。拓扑排序给出节点的合法顺序(满足所有约束)。

拓扑排序算法:

1. **Kahn 算法**:每次找入度为 0 的节点,加入结果,删掉它的出边,重复。
2. **DFS 后序**:DFS 遍历,记录后序(post-order),反转得到拓扑序。

如果图有环(循环依赖),拓扑排序失败——意味着 sprite 排序无解,必须**人为拆解**(切开某个 sprite)。

**浮点数的稳定性**。Rust 的 `f32::partial_cmp` 处理 NaN 返回 `None`,sort 会 panic。所以游戏代码里要么保证所有 f32 都不是 NaN(数学保证),要么 `partial_cmp(...).unwrap_or(Equal)`(默认相等)。今天 sort_rule 用后者。

更稳妥的方法:**所有位置用整数**(像素单位)。`pos_y: i32` 没有 NaN 问题,比较无分支。但缩放和旋转要用浮点,所以游戏用 i32 + f32 混合。

### 真实代码片段

Casey 在 HH 视频里实际写的代码风格(简化伪代码):

```c
internal int32
SortRule(entity *A, entity *B)
{
    int32 Result = 0;

    // Layer 比较
    if (A->Layer != B->Layer) {
        Result = (int32)A->Layer - (int32)B->Layer;
    } else {
        // Bounds overlap 检查
        rectangle2 BoundsA = GetEntityBounds(A);
        rectangle2 BoundsB = GetEntityBounds(B);
        if (RectanglesOverlap(BoundsA, BoundsB)) {
            // Bottom y 比较
            real32 BottomA = BoundsA.Max.Y;
            real32 BottomB = BoundsB.Max.Y;
            if (BottomA != BottomB) {
                Result = (BottomA < BottomB) ? -1 : 1;
            } else {
                // x 比较
                Result = SignOf32(A->P.X - B->P.X);
            }
        } else {
            // 不重叠:任意顺序
            Result = A->SpawnID - B->SpawnID;
        }
    }

    return Result;
}
```

注意 Casey 的 C 风格:

- 直接返回 int32(而非 enum Ordering)
- 用 `SignOf32`(返回 -1/0/1)简化比较
- if-else 链而非 Rust 的 `then_with`

Rust 等价:

```rust
fn sort_rule(a: &Entity, b: &Entity) -> Ordering {
    a.layer.cmp(&b.layer)
        .then_with(|| {
            if bounds_overlap(a, b) {
                let ba = bounds(a).max.y;
                let bb = bounds(b).max.y;
                ba.partial_cmp(&bb).unwrap_or(Ordering::Equal)
                    .then(a.pos.x.partial_cmp(&b.pos.x).unwrap_or(Ordering::Equal))
            } else {
                a.spawn_id.cmp(&b.spawn_id)
            }
        })
}
```

### 跨学科联结:从游戏到数据库

sort_rule 的本质是"复杂比较函数",这在很多领域都出现:

- **数据库 ORDER BY**:SQL 的 `ORDER BY layer, y, x` 是同样的 multi-key sort,但 SQL 是声明式(数据库优化器选 sort 算法),Casey 是命令式(手写 sort_by)。
- **React diff**:React 用 sort + key 比较 virtual DOM,决定哪些 component 重渲染。Multi-key sort 决定 diff 顺序。
- **Linux kernel CFS**(Completely Fair Scheduler):按 vruntime 排序进程,选最小 vruntime 的运行。本质是 sort_rule。
- **Git log --sort**:Git commit 按 committer date + author date + subject 排序。

Casey 在 HH 反复说"很多算法本质相同,只是命名不同"。掌握一个领域的 sort_rule,就掌握所有领域。

### 阅读路径建议

如果你被今天的概念绊倒,按这个顺序补:

1. **不熟 Ordering** → 读 Rust 文档 `std::cmp::Ord`、`std::cmp::Ordering`
2. **不熟闭包** → 读 Rust Book ch.13 Closures
3. **不熟 sort_by** → 读 `slice::sort_by` 文档,自己写 benchmark
4. **不熟 sort_key** → 读 `slice::sort_by_key`,理解 cache 行为
5. **不熟 bounds** → 读 bevy 的 sprite.rs,Aabb 实现
6. **不熟拓扑排序** → 读 Introduction to Algorithms (CLRS) ch.22 graph algorithms

每一步都跑代码验证,不要只读理论。

## 9 · 延伸阅读


本仓库本地:
- [day302](day302.md) — 确认没有全序
- [day303](day303.md) — Y/Z 分离失败

外部稳定 URL:
- Topological sort: https://en.wikipedia.org/wiki/Topological_sorting
- Kahn algorithm: https://www.geeksforgeeks.org/topological-sorting-indegree-based-solution/

真实开源源码:
- petgraph toposort: https://github.com/petgraph/petgraph/blob/master/src/algo/toposort.rs
- Casey HH day304: https://github.com/HandmadeHero/handmade-hero/tree/main/code/day304
