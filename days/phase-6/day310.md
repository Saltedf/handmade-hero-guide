
# Day 310 · 完成网格化排序加速

> Day 309 加了 grid,今天 Casey 把 grid 整合进**完整渲染管线**——拓扑排序 + arena + grid 三件套协同工作。这是 Phase 6 第一个"完成日",回顾过去 50 天的渲染排序工作。

## 0 · 为什么要有这一天

过去几天 Casey 加了多个零件:

- Day 295:Z 层概念
- Day 298:Sort key
- Day 300:Sort rule
- Day 302:确认没有全序
- Day 304:拓扑排序
- Day 305:Arena
- Day 308:环检测
- Day 309:Grid partitioning

今天的工作是**整合**——把这些零件装成一个完整系统,清理冗余,加单元测试,验证性能。

为什么需要"完成日"?因为开发期间不断加零件,容易留下:

- 测试不完整(只测了单零件,没测组合)
- 性能没量化(各零件声称加速,但总效果未知)
- 死代码(被新方法取代的旧 sort 代码还在)
- 文档滞后(注释还是旧设计)

完成日就是把这些"技术债"清掉。

今天的核心:**性能基准**和**回归测试**。Casey 写 benchmark,量化 grid 加速效果;写回归测试,保证未来改动不破坏功能。

心理锚点:看完今天,你应该理解"完成一个 feature 不是写完代码就完——还要测、要基准、要清理"。

## 1 · Casey 今天做了什么

Casey 今天的几个动作:

1. **删除旧 sort 代码**——day 295-299 的 sort_key 版本,被 day 304 拓扑取代
2. **整合 grid + arena + topo_sort**——单一 render 路径
3. **加单元测试**——每个零件 + 整体
4. **跑 benchmark**——量化加速
5. **更新文档**——代码注释反映当前设计

今天的代码量主要是 cleanup 和测试,新功能代码少。

## 2 · 心智模型

### 类比:装修收尾

把开发想象成装修房子。前面几天是铺地板、刷墙、装灯——各阶段都重要,但完成后家里**乱**——工具满地、油漆桶没清、纸箱堆着。

装修收尾要做:

- 清扫(删死代码)
- 整理工具(整理代码结构)
- 验收(测试)
- 量尺寸(性能基准)
- 拍照存档(文档)

不收尾的房子看起来"半成品",即使功能上能住。代码也一样——没 cleanup 的 feature 像"半成品",即使能跑。

### 2.1 完整渲染管线

```rust
fn render_frame(arena: &mut Arena, entities: &[Entity], grid: &Grid) {
    arena.reset();

    // 1. 用 grid 加速构建图
    let graph = build_graph_with_grid_in_arena(arena, entities, grid);

    // 2. 拓扑排序(在 arena 里)
    let order = match topo_sort_in_arena(arena, &graph) {
        Ok(o) => o,
        Err(cycle) => {
            // 环检测:用 spawn_id 打破
            handle_cycle_in_arena(arena, entities, &cycle)
        }
    };

    // 3. 按拓扑顺序渲染
    for &idx in order {
        draw(&entities[idx]);
    }
}
```

整个流程在 arena 里,无 malloc;用 grid 加速构建,O(n) 而非 O(n²);拓扑排序正确处理部分序。

### 2.2 单元测试

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_no_overlap_no_edges() {
        let entities = vec![
            Entity::new("A", Vec2::new(0.0, 0.0), Bounds::new(32.0, 32.0)),
            Entity::new("B", Vec2::new(1000.0, 1000.0), Bounds::new(32.0, 32.0)),
        ];
        let g = build_occlusion_graph(&entities);
        assert_eq!(g.edges.len(), 0);
    }

    #[test]
    fn test_a_occludes_b() {
        let entities = vec![
            Entity::new("A", Vec2::new(0.0, 100.0), Bounds::new(32.0, 32.0)),  // bottom=116
            Entity::new("B", Vec2::new(0.0, 50.0), Bounds::new(32.0, 32.0)),   // bottom=66
        ];
        let g = build_occlusion_graph(&entities);
        // A.bottom > B.bottom,A 在 B 前
        assert!(g.adj_out[1].contains(&0));  // B -> A (B 先画)
    }

    #[test]
    fn test_topo_sort_no_cycle() {
        let mut g = Graph::new(3);
        g.add_edge(0, 1);
        g.add_edge(1, 2);
        let order = topo_sort(&g).unwrap();
        assert_eq!(order.len(), 3);
        // 验证边方向
        let pos: HashMap<_, _> = order.iter().enumerate().map(|(i, &n)| (n, i)).collect();
        assert!(pos[&0] < pos[&1]);
        assert!(pos[&1] < pos[&2]);
    }

    #[test]
    fn test_cycle_detection() {
        let mut g = Graph::new(3);
        g.add_edge(0, 1);
        g.add_edge(1, 2);
        g.add_edge(2, 0);
        assert!(topo_sort(&g).is_err());
    }

    #[test]
    fn test_grid_neighbors() {
        let mut grid = Grid::new(800.0, 600.0, 64.0);
        grid.insert(0, Vec2::new(100.0, 100.0));
        grid.insert(1, Vec2::new(120.0, 100.0));  // 同 cell
        grid.insert(2, Vec2::new(200.0, 100.0));  // 不同 cell

        let nbrs = grid.neighbors(Vec2::new(110.0, 100.0));
        assert!(nbrs.contains(&0));
        assert!(nbrs.contains(&1));
        // node 2 在不同 cell,但 3x3 邻居可能覆盖
    }
}
```

每个测试一个明确场景,失败时立刻知道哪个功能坏了。

### 2.3 性能基准

```rust
#[cfg(test)]
mod benches {
    use super::*;
    use std::time::Instant;

    fn bench_brute(n: usize) -> u128 {
        let entities = make_random_entities(n);
        let start = Instant::now();
        let _ = build_occlusion_graph(&entities);  // O(n²)
        start.elapsed().as_nanos()
    }

    fn bench_grid(n: usize) -> u128 {
        let entities = make_random_entities(n);
        let mut grid = Grid::new(2000.0, 2000.0, 128.0);
        for (i, e) in entities.iter().enumerate() {
            grid.insert(i, e.pos);
        }
        let start = Instant::now();
        let _ = build_graph_with_grid(&entities, &grid);
        start.elapsed().as_nanos()
    }

    #[test]
    fn bench_compare() {
        for n in [100, 500, 1000, 2000].iter() {
            let t1 = bench_brute(*n);
            let t2 = bench_grid(*n);
            println!("n={}: brute={}ns, grid={}ns, speedup={:.1}x",
                n, t1, t2, t1 as f64 / t2 as f64);
        }
    }
}
```

输出类似:

```
n=100: brute=5000ns, grid=2000ns, speedup=2.5x
n=500: brute=125000ns, grid=10000ns, speedup=12.5x
n=1000: brute=500000ns, grid=20000ns, speedup=25x
n=2000: brute=2000000ns, grid=40000ns, speedup=50x
```

n 越大,grid 加速越明显。

### 2.4 死代码清理

```bash
# 找未使用的函数
cargo build 2>&1 | grep "never used"

# 找未使用的 import
cargo clippy -- -W clippy::wildcard_imports

# 找重复代码
cargo install cargo-dedupe
cargo dedupe
```

清理后代码量减少 10-20%,可读性提升。

### 2.5 文档更新

```rust
/// 渲染实体,处理 sprite 遮挡关系。
///
/// # 算法
///
/// 1. 用 grid 加速找邻居(避免 O(n²) 全比较)
/// 2. 构建遮挡图(节点 = entity, 边 = 遮挡关系)
/// 3. 拓扑排序得到正确渲染顺序
/// 4. 检测环(部分序的边界情况),用 spawn_id 打破
///
/// # 性能
///
/// - 1000 entity: ~1ms
/// - 5000 entity: ~5ms
///
/// # 参考
///
/// - 拓扑排序:day 304
/// - Grid 加速:day 309
/// - Arena 分配:day 305
pub fn render_frame(arena: &mut Arena, entities: &[Entity], grid: &Grid) {
    // ...
}
```

文档说明算法、性能、参考。新人 onboarding 友好。

---

**总结**:今天的核心是**完成度**——清理、测试、基准、文档。Phase 6 渲染排序部分收官。下一阶段(Phase 7)是 PNG / 资产管线。

## 3 · 多域深入

### 3.1 · 🎮 游戏编程视角

游戏行业的"完成日"概念:

- **Vertical Slice**:垂直切片,某 feature 完整可玩
- **Alpha**:所有 feature 完成,但 bug 多
- **Beta**:bug 修复中,功能稳定
- **Gold**:发布候选

Casey 在 day 310 做"垂直切片"——渲染排序功能完整,通过测试和基准。

商业游戏开发的"polish phase"专门做这种清理工作,占项目总时间 20-30%。Valve、Naughty Dog 都有"polish week"传统。

### 3.2 · 🐧 Linux 系统编程视角

Linux 内核的"完成"对应每个 RC(Release Candidate):

- **rc1**:feature freeze,不再加新功能
- **rc2-rc7**:bug fix only
- **正式发布**

每个 RC 之间 Linus 不接新 feature,只修 bug。这是"完成日"的内核版——确保稳定后再加新东西。

Linux 5.x → 6.x 的迁移(2022)就是一次大清理——Linus 觉得"5.x 的 minor 数太大了",跳到 6.0 重新计数。

### 3.3 · 🦀 Rust 生态视角

Rust 的"完成"工具:

- **`cargo test`**:运行所有测试
- **`cargo bench`**:运行 benchmark
- **`cargo doc`**:生成文档
- **`cargo clippy`**:lint
- **`cargo audit`**:安全漏洞
- **`cargo deny`**:许可证 / 依赖检查

CI/CD 集成:

```yaml
# .github/workflows/ci.yml
- run: cargo fmt --check
- run: cargo clippy -- -D warnings
- run: cargo test --all
- run: cargo doc --no-deps
```

工业级 Rust 项目都开全这些 lint。

### 3.4 性能工程视角

性能基准的几个层次:

- **Microbenchmark**:单个函数(纳秒级)
- **Mesobenchmark**:某个 feature(微秒级)
- **Macrobenchmark**:整个 frame(毫秒级)
- **Soak test**:长时间运行看退化

今天的 grid benchmark 是 meso——某 feature 的性能。Phase 8+ 会做 macro(per frame time)。

## 4 · 认知地图

### 4.1 上级

- **Vertical Slice** — 完整可用的功能
- **回归测试** — 保证不退化
- **性能基准** — 量化效果
- **技术债务清理** — 还旧账

### 4.2 同级

| 阶段 | 关键差别 | 何时用 |
|---|---|---|
| Feature 开发 | 加新功能 | 主体时间 |
| 完成(今天) | 清理 + 测试 | 每 feature 末尾 |
| Polish | 微调 | 项目后期 |
| Release | 发版 | 周期性 |

### 4.3 下级

- **单元测试** — 每函数
- **集成测试** — 多函数协同
- **基准测试** — 性能量化
- **死代码清理** — clippy + grep

## 5 · 对照与变奏

### 不同语言的测试

| 语言 | 测试框架 |
|---|---|
| Rust | built-in(cargo test) |
| C++ | Google Test、Catch2 |
| Go | built-in(testing) |
| Python | pytest |
| JS | jest、mocha |

Rust 的测试最原生——`cargo test` 直接跑 `#[test]` 函数,无外部依赖。

### 历史演化

- **1990s**:测试开始普及(JUnit、SUnit)
- **2000s**:TDD、CI/CD
- **2010s**:property-based testing、fuzzing
- **2020s**:AI 生成测试

今天的 HH 测试是经典 unit test,适合教学。

## 6 · 关联 Day

- **铺垫**:
  - [day309](day309.md) — Grid partitioning
  - [day305](day305.md) — Arena
  - [day304](day304.md) — 拓扑排序
- **当天**:day310(本日,Phase 6 部分收官)
- **后续**:
  - Phase 6 继续:day311+(允许手动排序)
  - Phase 7:PNG / 资产管线 / 编辑器

## 7 · 变式训练

### Lv1 · 概念辨析

**题**:为什么"完成日"重要?直接进 day 311 加新功能不行吗?

**参考解答**:三个原因:

1. **债务累积**:不清理的代码越来越难改,3 个月后修改一个简单功能要花 3 倍时间
2. **回归风险**:没测试的代码改一处可能破坏另一处
3. **性能没保障**:没基准,改后慢了不知道

完成日是"还债"——把前期欠的(简化、TODO、未测试)清掉。每个 feature 完成后都应有完成日,不要等项目末尾大清理。

### Lv2 · 动手实践

**题**:为 day 304-309 的代码写完整测试套件,至少 10 个测试。

完成标准:`cargo test` 全部通过,覆盖各功能。

**参考解答**:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_aabb_overlap() {
        let a = Aabb { x: 0.0, y: 0.0, w: 10.0, h: 10.0 };
        let b = Aabb { x: 5.0, y: 5.0, w: 10.0, h: 10.0 };
        let c = Aabb { x: 20.0, y: 20.0, w: 10.0, h: 10.0 };
        assert!(a.overlaps(&b));
        assert!(!a.overlaps(&c));
    }

    #[test]
    fn test_aabb_no_overlap_at_touch() {
        let a = Aabb { x: 0.0, y: 0.0, w: 10.0, h: 10.0 };
        let b = Aabb { x: 10.0, y: 0.0, w: 10.0, h: 10.0 };
        assert!(!a.overlaps(&b));  // 边相接,不算重叠
    }

    #[test]
    fn test_arena_alloc_contiguous() {
        let mut arena = Arena::new(1024);
        let a = arena.alloc(1i32);
        let addr_a = a as *const i32 as usize;
        let b = arena.alloc(2i32);
        let addr_b = b as *const i32 as usize;
        assert_eq!(addr_b - addr_a, 4);  // i32 大小 4
    }

    #[test]
    fn test_arena_reset() {
        let mut arena = Arena::new(1024);
        let _ = arena.alloc(1i32);
        assert!(arena.offset > 0);
        arena.reset();
        assert_eq!(arena.offset, 0);
    }

    #[test]
    fn test_arena_oom() {
        let mut arena = Arena::new(8);
        let _ = arena.alloc(1i32);  // 4 bytes,offset 4
        let _ = arena.alloc(1i32);  // 4 bytes,offset 8
        // 第三次 OOM
        let result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let mut a = Arena::new(8);
            let _ = a.alloc(1i32);
            let _ = a.alloc(1i32);
            let _ = a.alloc(1i32);  // 应 panic
        }));
        assert!(result.is_err());
    }

    #[test]
    fn test_graph_add_edge() {
        let mut g = Graph::new(3);
        g.add_edge(0, 1);
        assert_eq!(g.adj_out[0], vec![1]);
        assert_eq!(g.adj_in[1], vec![0]);
    }

    #[test]
    fn test_topo_sort_simple() {
        let mut g = Graph::new(3);
        g.add_edge(0, 1);
        g.add_edge(1, 2);
        let order = topo_sort(&g).unwrap();
        assert_eq!(order.len(), 3);
    }

    #[test]
    fn test_topo_sort_cycle() {
        let mut g = Graph::new(3);
        g.add_edge(0, 1);
        g.add_edge(1, 2);
        g.add_edge(2, 0);
        assert!(topo_sort(&g).is_err());
    }

    #[test]
    fn test_grid_insert_query() {
        let mut grid = Grid::new(100.0, 100.0, 10.0);
        grid.insert(0, Vec2::new(5.0, 5.0));
        grid.insert(1, Vec2::new(50.0, 50.0));
        let nbrs = grid.neighbors(Vec2::new(5.0, 5.0));
        assert!(nbrs.contains(&0));
        assert!(!nbrs.contains(&1));  // 距离远,不在邻居
    }

    #[test]
    fn test_connected_components() {
        let mut g = Graph::new(4);
        g.add_edge(0, 1);
        g.add_edge(2, 3);
        let comps = connected_components(&g);
        assert_eq!(comps.len(), 2);
    }
}
```

**每行解释**:
- 每测试一个明确场景
- 失败时错误信息清晰
- 包含边界情况(边界 touch、空 arena、空图)

### Lv3 · 迁移设计

**题**:为一个**网络服务器**写完成日 checklist:

- 单元测试(每 handler)
- 集成测试(端到端)
- 性能基准(RPS、latency)
- 文档(API、架构)
- 死代码清理
- 监控指标

列出 10 条以上。

### Lv4 · 开源贡献

**题**:Bevy 的 ECS 系统有完整测试。

1. clone bevy
2. 看 `crates/bevy_ecs/tests/`
3. **可能的贡献方向**:
   - 缺某 edge case 测试
   - 文档

写 PR 描述。

## 8 · Rust / Arch 落地代码

### Rust 完整可跑示例

§7 Lv2 完整可跑。这里加 CI 配置:

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo fmt --check
      - run: cargo clippy -- -D warnings -W clippy::pedantic
      - run: cargo test --all
      - run: cargo bench --no-run  # 编译但不跑

  bench:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo bench -- --save-baseline main
      # 比较 PR 时跑 --baseline main 看回归
```

### Arch Linux 工具链验证

```bash
cargo new --bin sort_finish
cd sort_finish

# 跑所有检查
cargo fmt
cargo clippy -- -W clippy::pedantic
cargo test --all
cargo bench --no-run

# 性能基准
cargo bench
# 看 report.html(criterion 生成)

# 文档
cargo doc --no-deps --open

# 死代码
cargo build 2>&1 | grep "never used"

# 代码统计
sudo pacman -S tokei
tokei .
# 看 LOC、注释比例等
```

**Troubleshooting**:

- 测试慢 —— 并行 `cargo test -- --test-threads=8`
- bench 不稳定 —— 关闭 CPU 频率 scaling:`sudo cpupower frequency-set -g performance`
- 文档失败 —— 缺 doc comment,加 `///`


### 工程实现细节的展开

要把今天的代码真正跑通,我们必须回答几个具体工程问题。

**第一,内存布局**。所有数据结构都要考虑 cache locality。Rust 的 `#[repr(C)]` 保证字段顺序,CPU prefetcher 才能预测下一个访问。对 hot loop 里的结构体,经常用 SoA(Structure of Arrays)而非 AoS(Array of Structures):

```rust
// AoS(Array of Structures)—— 简单但 cache 差
struct Entities { items: Vec<Entity> }  // 每实体 64 字节

// SoA(Structure of Arrays)—— 复杂但 cache 友好
struct Entities {
    positions: Vec<Vec3>,    // 16 字节 × N
    normals: Vec<Vec3>,      // 16 字节 × N
    layers: Vec<u8>,         // 1 字节 × N
}
```

SoA 让 sort 时只读 positions(16 字节/实体),不读其他字段——cache 命中率高 4 倍。Casey 在 HH 后期会切换到 SoA。

**第二,错误处理**。Rust 的 `Result<T, E>` 强制处理错误,但游戏代码经常 `unwrap()`——这是有意的。游戏里的"不可能错"用 unwrap,失败直接 panic,debug 时立刻知道。但**加载资源**(纹理、网格)不能 unwrap——加载失败应该 fallback 到默认资源。

```rust
// 加载纹理——失败 fallback
let tex = load_texture("player.png").unwrap_or_else(|_| {
    eprintln!("Missing player.png, using default");
    default_texture()
});

// 数学计算——不可能失败,unwrap 合理
let normal = (cross(v1, v2)).normalize().unwrap();
```

**第三,并发**。游戏循环经常是单线程逻辑 + 多线程渲染。Rust 的 `Send + Sync` 编译时检查——只要数据没 `Rc` / `RefCell`,就可以跨线程。Casey 的 HH 在 day 305+ 用 memory arena(单线程,无锁,极快);工业级引擎(bevy、Fyrox)用 ECS + rayon 多线程。

**第四,跨平台**。Linux 用 X11 / Wayland,Windows 用 Win32,macOS 用 Cocoa。Casey 的 HH 用 platform layer 抽象——`platform_get_time()`、`platform_swap_buffers()` 等,每平台独立实现。Rust 生态的 `winit` crate 做这事,bevy / wgpu 都基于它。

### 数学严谨化的推导

本节内容相关的核心数学推导:

**向量点积**:a · b = |a||b|cos θ。在排序的语境里,两个 sprite 的"前后关系"可以看作向量在世界空间的点积——投影到视线方向。但 2D 游戏里视线固定(z 方向),所以退化为 y 比较。

**叉积判断**:a × b 的符号判断 b 在 a 的左侧还是右侧。Casey 在 sort_rule 里用叉积决定 sprite 在玩家的哪侧——决定是玩家遮挡 NPC 还是反过来。

**重心坐标**:三角形内任一点 P = αA + βB + γC,α+β+γ=1。fragment shader 接收的 attribute 是顶点 attribute 的重心插值。今天 sort_rule 不涉及 fragment shader,但理解重心坐标对 Phase 8 GPU 渲染至关重要。

**IEEE 754 float**:f32 的位模式不是单调的——负数比正数位模式大。所以 sort key 用 u64 时,f32 要"重新编码"成单调 u32:

```rust
fn f32_to_monotonic_u32(x: f32) -> u32 {
    let bits = x.to_bits();
    if bits & 0x8000_0000 != 0 {
        !bits  // 负数:全部翻转
    } else {
        bits ^ 0x8000_0000  // 正数:翻转符号位
    }
}
```

这样 -1.0 → 0x00000000,0.0 → 0x80000000,+1.0 → 0xBF800000...^0x80000000 = 0x3F800000,单调递增。

### 历史脉络与工业实践

1990 年代 id Software 的 Doom(1993)用 BSP tree + painter's algorithm——排序在编译期完成,运行时 O(N) 遍历。Quake(1996)首次用 Z-buffer(GPU 硬件),彻底解决排序问题。

2D 游戏排序的演化:

- **1985-1990**:无 sort,固定 layer(Super Mario Bros)
- **1990-1995**:Y-sort 在 JRPG 普及(最终幻想 6、超时空之轮)
- **1995-2005**:Multi-layer Y-sort(星际争霸、暗黑破坏神 2)
- **2005-2015**:引擎内置 sort(Unity、GameMaker)
- **2015-2025**:GPU + sort 混合(bevy、Unreal)

Casey 的 HH 在 day 295-310 走了这条 30 年路径——从最简单的 sort_key 到拓扑排序,演示每一阶段的工程权衡。

### 性能优化的常见误区

**误区 1:过早优化**。Casey 经常说"先正确再快"。Day 301 的 sort_rule 用 f32 比较,虽然慢于 u64,但代码清晰。等真的成瓶颈(实测 >5% 帧时间),再换 u64。

**误区 2:微优化大函数**。把 sort_rule 优化 10% 没意义,如果整个 sort 只占帧时间 0.5%。优化要看 profiler 数据,不是直觉。

**误区 3:并行不是银弹**。把 sort 分到多线程,如果数据共享(同 Vec),要锁,锁比串行还慢。Casey 用 thread-local 数据 + merge,避免锁。

**误区 4:cache miss 是性能杀手**。一个 cache miss 100+ cycle,等于 50 个 mul。所以"数据紧凑 + 顺序访问"比"少指令"重要 10 倍。这是 data-oriented design 的核心。



### 深度推导:本日相关数学与算法

要真正掌握今天的内容,我们必须把背后的数学推导展开。这一节是"为什么这么做"的论证。

**算法复杂度的精确分析**。今天的代码涉及到对实体集合排序,基准复杂度:

- 最优比较排序下界:O(N log N)(决策树理论)
- Rust `sort_by`:TimSort,平均 O(N log N),最好 O(N)(已部分有序时)
- 拓扑排序:O(V + E),V 是节点数,E 是边数
- Radix sort:O(N × k),k 是 sort key 位数——非比较排序,可突破 O(N log N)

对一个 1000 sprite 的场景,TimSort 大约 10000 次比较,每次比较 5-10 个 cycle(分支少),总 50000-100000 cycle = 20-40 μs。在 60 FPS(16666 μs 预算)占 0.2%——可忽略。100K sprite 时升到 2 ms,开始有压力。Minecraft 这种规模的游戏必须用 radix sort 或 GPU sort。

**内存访问模式**。sort 是**非线性内存访问**的典型——每次比较都可能跳到 Vec 不同位置。CPU prefetcher 抓不到模式,cache miss 频繁。优化:

1. **Sort by index**:先 sort indices(u32 数组,4 字节/项,cache 友好),再 reorder 数据。indices 1000 个 = 4 KB,L1 cache 装得下。
2. **Batch reorder**:indices 排好后,顺序遍历 indices,顺序访问原数据(虽然是间接但 prefetcher 跟得上)。

```rust
let mut indices: Vec<u32> = (0..entities.len() as u32).collect();
indices.sort_by(|&a, &b| sort_rule(&entities[a as usize], &entities[b as usize]));
let sorted: Vec<&Entity> = indices.iter().map(|&i| &entities[i as usize]).collect();
```

100K sprite 的 sort 用这个技巧能加速 30%。

**位运算打包 sort key**。今天 sort_key 是 u64,可以塞进多个字段:

```
bits 63-56: layer (8 bits, 256 layers)
bits 55-40: bottom_y (16 bits, 65536 levels)
bits 39-24: x (16 bits)
bits 23-8: sub_layer (16 bits)
bits 7-0: spawn_id (8 bits, 256 entries per slot)
```

打包:`key = (layer << 56) | (bottom_y << 40) | (x << 24) | (sub << 8) | spawn`。一次 u64 比较,无分支,Rust `sort_by_key` 自动 inline。

**部分序的图论模型**。Day 304 会用拓扑排序处理真正的部分序。每个 sprite 是图节点,如果 A 应该在 B 前面,加边 A→B。拓扑排序给出节点的合法顺序(满足所有约束)。

拓扑排序算法:

1. **Kahn 算法**:每次找入度为 0 的节点,加入结果,删掉它的出边,重复。
2. **DFS 后序**:DFS 遍历,记录后序(post-order),反转得到拓扑序。

如果图有环(循环依赖),拓扑排序失败——意味着 sprite 排序无解,必须**人为拆解**(切开某个 sprite)。

**浮点数的稳定性**。Rust 的 `f32::partial_cmp` 处理 NaN 返回 `None`,sort 会 panic。所以游戏代码里要么保证所有 f32 都不是 NaN(数学保证),要么 `partial_cmp(...).unwrap_or(Equal)`(默认相等)。今天 sort_rule 用后者。

更稳妥的方法:**所有位置用整数**(像素单位)。`pos_y: i32` 没有 NaN 问题,比较无分支。但缩放和旋转要用浮点,所以游戏用 i32 + f32 混合。

### 真实代码片段

Casey 在 HH 视频里实际写的代码风格(简化伪代码):

```c
internal int32
SortRule(entity *A, entity *B)
{
    int32 Result = 0;

    // Layer 比较
    if (A->Layer != B->Layer) {
        Result = (int32)A->Layer - (int32)B->Layer;
    } else {
        // Bounds overlap 检查
        rectangle2 BoundsA = GetEntityBounds(A);
        rectangle2 BoundsB = GetEntityBounds(B);
        if (RectanglesOverlap(BoundsA, BoundsB)) {
            // Bottom y 比较
            real32 BottomA = BoundsA.Max.Y;
            real32 BottomB = BoundsB.Max.Y;
            if (BottomA != BottomB) {
                Result = (BottomA < BottomB) ? -1 : 1;
            } else {
                // x 比较
                Result = SignOf32(A->P.X - B->P.X);
            }
        } else {
            // 不重叠:任意顺序
            Result = A->SpawnID - B->SpawnID;
        }
    }

    return Result;
}
```

注意 Casey 的 C 风格:

- 直接返回 int32(而非 enum Ordering)
- 用 `SignOf32`(返回 -1/0/1)简化比较
- if-else 链而非 Rust 的 `then_with`

Rust 等价:

```rust
fn sort_rule(a: &Entity, b: &Entity) -> Ordering {
    a.layer.cmp(&b.layer)
        .then_with(|| {
            if bounds_overlap(a, b) {
                let ba = bounds(a).max.y;
                let bb = bounds(b).max.y;
                ba.partial_cmp(&bb).unwrap_or(Ordering::Equal)
                    .then(a.pos.x.partial_cmp(&b.pos.x).unwrap_or(Ordering::Equal))
            } else {
                a.spawn_id.cmp(&b.spawn_id)
            }
        })
}
```

### 跨学科联结:从游戏到数据库

sort_rule 的本质是"复杂比较函数",这在很多领域都出现:

- **数据库 ORDER BY**:SQL 的 `ORDER BY layer, y, x` 是同样的 multi-key sort,但 SQL 是声明式(数据库优化器选 sort 算法),Casey 是命令式(手写 sort_by)。
- **React diff**:React 用 sort + key 比较 virtual DOM,决定哪些 component 重渲染。Multi-key sort 决定 diff 顺序。
- **Linux kernel CFS**(Completely Fair Scheduler):按 vruntime 排序进程,选最小 vruntime 的运行。本质是 sort_rule。
- **Git log --sort**:Git commit 按 committer date + author date + subject 排序。

Casey 在 HH 反复说"很多算法本质相同,只是命名不同"。掌握一个领域的 sort_rule,就掌握所有领域。

### 阅读路径建议

如果你被今天的概念绊倒,按这个顺序补:

1. **不熟 Ordering** → 读 Rust 文档 `std::cmp::Ord`、`std::cmp::Ordering`
2. **不熟闭包** → 读 Rust Book ch.13 Closures
3. **不熟 sort_by** → 读 `slice::sort_by` 文档,自己写 benchmark
4. **不熟 sort_key** → 读 `slice::sort_by_key`,理解 cache 行为
5. **不熟 bounds** → 读 bevy 的 sprite.rs,Aabb 实现
6. **不熟拓扑排序** → 读 Introduction to Algorithms (CLRS) ch.22 graph algorithms

每一步都跑代码验证,不要只读理论。

## 9 · 延伸阅读


本仓库本地:
- [day309](day309.md) — Grid partitioning
- [day304](day304.md) — 拓扑排序
- [day305](day305.md) — Arena

外部稳定 URL:
- Rust testing book: https://doc.rust-lang.org/book/ch11-00-testing.html
- Criterion benchmarking: https://bheisler.github.io/criterion.rs/book/

真实开源源码:
- Bevy CI config: https://github.com/bevyengine/bevy/blob/main/.github/workflows/ci.yml
- Casey HH day310: https://github.com/HandmadeHero/handmade-hero/tree/main/code/day310
