
# Day 550 · SIMD 光线投射点与法线计算

> Voxel 光照的关键操作是 **raycast**——从某点出发,沿方向走,问"沿途经过了哪些 voxel,有没有撞到墙"。如果每帧要做几百万次 raycast,串行太慢。Day 550 Casey 用 **SIMD**(单指令多数据)一次处理 4 条射线,GPU/CPU 利用率 4 倍提升。

## 0 · 为什么要有这一天

让我们把镜头贴近 CPU profile。你跟着 Handmade Hero 走到 Day 549——Casey 写了 scalar raycast,voxel 光照工作正常,但**帧率只有 30 FPS**。你打开 perf profiler,发现 raycast 占了 60% CPU 时间。具体数字:1 帧从相机发出 100 万条 ray,每条 ray 走 50 步(典型),每步 10 个 f32 操作(比较、加法、查 voxel)——总操作 5 亿次。CPU 单核 3 GHz,5 亿操作约 170 ms,**单核根本撑不住 60 FPS**。

怎么加速?**SIMD**(Single Instruction Multiple Data,单指令多数据)是答案。CPU 一次常规操作(加、乘、比较)只处理一个数,叫 **scalar(标量)**。但现代 CPU 有 **SIMD 寄存器**(SSE/AVX/AVX-512),一条指令同时处理 4/8/16 个数。

例子:4 个 f32 加 4 个 f32。

- Scalar:4 条 add 指令,每条 1 cycle,总 4 cycles
- SSE(128-bit):1 条 `addps` 指令,同时算 4 个 f32,总 1 cycle(某些 CPU 上 3-4 cycles latency 但 throughput 1)

**理论加速 4 倍**。实际加速通常 2-3 倍——因为还有数据加载、store、cache miss 等开销,以及 SIMD 的 divergence 问题(下面会讲)。

光线投射(raycast)是 SIMD 的理想场景:

- 一帧从相机发出 100 万条 ray
- 每条 ray 的算法相同(沿方向走,查 voxel)
- 数据结构相同(voxel grid)

这种"重复算法 + 独立数据"的模式,SIMD 完美匹配——把 4 条 ray 打包成"ray packet",用 SIMD 指令一次处理 4 条 ray 的同一步。这就是工业光线追踪(Embree、OptiX)的核心优化技巧。

Casey 在 Day 547-549 是 scalar 实现先把功能跑通,Day 550 用 SIMD 优化"光线 vs voxel"的核心循环。**这是性能优化的标准流程**:先写对的代码(scalar),再写快的代码(SIMD)。**不要一上来就 SIMD**——因为 SIMD 代码难 debug,先用 scalar 跑通功能,再用 SIMD 提速。

完成这一天之后,你具备:

- 理解 SIMD 的原理和适用场景——为什么"重复算法 + 独立数据"是 SIMD 友好
- 能用 Rust 的 `std::simd`(portable-simd)或 `packed_simd` 写 4-wide / 8-wide 代码
- 知道 raycast 的 Amanatides-Woo 算法,以及它的 SIMD-friendly 数据布局
- 处理 SIMD divergence——4 条 ray 撞墙时机不同,如何用 mask 高效处理

## 1 · Casey 今天做了什么

Casey 的步骤:

第一步,他识别"热点循环"——raycast 中每步算下一个 voxel 索引、查 occupied、算碰撞点。每条 ray 走 10-100 步,1M ray = 10M-100M 步操作。

第二步,他把 4 条 ray 打包,用 SSE/AVX 同时算:

- 4 个 ray origin 拼成 `__m128`(SSE 128-bit,4 个 f32)
- 4 个 ray direction 拼成 `__m128`
- 算下一步的 t 值:一次 `_mm_add_ps` 同时算 4 个

第三步,处理 divergence(分叉):如果 ray 1 在第 5 步撞墙,ray 2 还在走,要 mask 掉 ray 1,继续 ray 2。SIMD 必须齐步走,但逻辑上每条 ray 独立——用 mask 暂停"已完成"的 ray。

视频里他强调:"SIMD 优化 50% 是数据布局,30% 是指令选择,20% 是分支优化。" 数据布局最重要——如果数据不能 16 字节对齐,SIMD 加载慢。

## 2 · 心智模型

### 2.1 类比:超市 4 个收银台同时收

想象超市有 4 个收银台。每个收银员处理一个顾客(算一个 ray)。**串行**(1 个收银员处理 4 个顾客):花 4 倍时间——每个顾客 5 分钟,4 个顾客 20 分钟。**并行**(4 个收银员):4 个顾客同时处理 = 5 分钟——理论 4 倍加速。

但有个限制:4 个收银员必须**同步**——一个收银员处理完一个顾客,要等其他人也处理完,才能开始下一批。如果一个顾客买的特别多(花 10 分钟),其他 3 个收银员要等他 5 分钟——**这就是 SIMD divergence(分叉)**。SIMD 4 条 ray 必须齐步走,慢的 ray 拖累快的 ray。

**SIMD divergence 是 SIMD 编程的核心挑战**。在 raycast 里,4 条 ray 撞墙时机不同——ray 1 在第 5 步撞墙,ray 2 在第 50 步。如果齐步走,ray 1 撞墙后还要"陪跑"45 步——浪费算力。**解决方案**:用 mask 标记"已完成的 ray",继续参与运算但不更新结果。具体:每步算所有 4 条 ray 的 voxel 查询,但用 mask 选择性写入——已完成的 ray 写入被丢弃。

**类比继续**:已完成的"顾客"对应的收银员假装还在算,但他的扫描枪"关闭"——商品扫过不记录。这样收银员可以和其他人同步,但不做无用功。**这就是 SIMD mask 的工作方式**。

### 2.2 严谨展开:Amanatides-Woo voxel traversal

经典算法,1987 年 Amanatides 和 Woo 在 Eurographics 提出的论文 "A Fast Voxel Traversal Algorithm for Ray Tracing"。给定 ray 起点 origin、方向 dir、grid 大小,遍历 ray 经过的所有 voxel。**核心思想**:跟踪 ray 在每个轴上下一次穿过 voxel 边界的"参数 t 值",取最小者作为下一步——这样保证每次只跨一个 voxel 边界,不漏步。

```rust
fn amanatides_woo(
    origin: [f32; 3], dir: [f32; 3],
    grid: &VoxelGrid,
) -> Option<VoxelHit> {
    let voxel_size = grid.voxel_size;

    // 当前 voxel index(floor 找到包含 origin 的 voxel)
    let mut i = (origin[0] / voxel_size).floor() as i64;
    let mut j = (origin[1] / voxel_size).floor() as i64;
    let mut k = (origin[2] / voxel_size).floor() as i64;

    // step:+1 或 -1,看方向(ray 朝 +x 走则每步 i+1,反之 i-1)
    let step_x = if dir[0] > 0.0 { 1 } else { -1 };
    let step_y = if dir[1] > 0.0 { 1 } else { -1 };
    let step_z = if dir[2] > 0.0 { 1 } else { -1 };

    // t_max:到下一个 voxel 边界的距离(沿 ray 参数 t)
    // 物理:ray = origin + t * dir,t 是参数。t_max 是"走到下一 voxel 边界的 t 值"
    let next_boundary = |i: i64, step: i64, voxel_size| -> f32 {
        if step > 0 { (i + 1) as f32 * voxel_size } else { i as f32 * voxel_size }
    };
    let mut t_max_x = (next_boundary(i, step_x, voxel_size) - origin[0]) / dir[0];
    let mut t_max_y = (next_boundary(j, step_y, voxel_size) - origin[1]) / dir[1];
    let mut t_max_z = (next_boundary(k, step_z, voxel_size) - origin[2]) / dir[2];

    // t_delta:穿过一个 voxel 沿 t 的距离
    // 物理:voxel_size / dir 在该轴的分量 = 走完一个 voxel 需要的 t 增量
    let t_delta_x = (voxel_size / dir[0]).abs();
    let t_delta_y = (voxel_size / dir[1]).abs();
    let t_delta_z = (voxel_size / dir[2]).abs();

    loop {
        // 当前 voxel 是否占据?
        if grid.is_occupied(i, j, k) {
            return Some(VoxelHit { i, j, k /* ... */ });
        }

        // 走到最近的下一个 voxel 边界
        // 关键:取 t_max 三者最小,这保证"只跨一个边界,不漏 voxel"
        if t_max_x < t_max_y && t_max_x < t_max_z {
            i += step_x;
            t_max_x += t_delta_x;
        } else if t_max_y < t_max_z {
            j += step_y;
            t_max_y += t_delta_y;
        } else {
            k += step_z;
            t_max_z += t_delta_z;
        }

        // 出界检查
        if i < 0 || j < 0 || k < 0 ||
           i as usize >= grid.dim[0] ||
           j as usize >= grid.dim[1] ||
           k as usize >= grid.dim[2] {
            return None;
        }
    }
}
```

**算法的精妙**:每步只比较 3 个 t_max 值(3 个比较 + 3 个加法),O(1) 复杂度。整条 ray 走 N 步就是 O(N)。比 naive 算法(每步检查所有 voxel)O(N × grid_size)快几个数量级。

**为什么叫"DDA"(Digital Differential Analyzer)?** 因为 t_max += t_delta 是"差分"操作——下一步的 t 值由当前步加上一个固定 delta 得到。这是 DDA 算法的本质——用差分方程避免每步重算。

每条 ray 走 N 步,N 取决于 ray 在 grid 里走了多远。对角穿 grid 是 sqrt(3) × dim 步,沿轴穿是 dim 步。典型 N=50。

### 2.3 SIMD 4-wide 版:RayPacket

把 4 条 ray 打包成"ray packet",数据布局采用 **SoA(Structure of Arrays)** 而非 **AoS(Array of Structures)**。**为什么 SoA?** 因为 SIMD 指令要求"4 个 f32 连续在内存",SoA 让 4 条 ray 的 origin.x 连续,可以直接 load 到 `f32x4`。AoS 让 1 条 ray 的 origin.x/y/z 连续,load 需要 shuffle。

```rust
use std::simd::f32x4;

struct RayPacket4 {
    // SoA 布局:4 条 ray 的同一字段打包
    origin_x: f32x4,  // 4 条 ray 的 origin.x
    origin_y: f32x4,
    origin_z: f32x4,
    dir_x: f32x4,
    dir_y: f32x4,
    dir_z: f32x4,
    // 当前状态
    i: [i64; 4],
    j: [i64; 4],
    k: [i64; 4],
    t_max_x: f32x4,
    t_max_y: f32x4,
    t_max_z: f32x4,
    active: [bool; 4],  // 哪些 ray 还在算(mask)
}

fn step_packet(packet: &mut RayPacket4, grid: &VoxelGrid) {
    // 同时算 4 条 ray 的下一步
    for r in 0..4 {
        if !packet.active[r] { continue; }
        // 检查当前 voxel
        if grid.is_occupied(packet.i[r], packet.j[r], packet.k[r]) {
            packet.active[r] = false;  // 撞墙了,完成
            continue;
        }
        // 走到下一个 voxel
        // ...同 scalar 版
    }
}
```

注意 `i/j/k` 是 `[i64; 4]` 不是 `i64x4`——因为 SIMD int 操作硬件支持不完整(SSE/AVX 整数 SIMD 不如浮点完整),f32 SIMD 比整数 SIMD 用得多。Casey 实际用浮点 SIMD 算 t_max/t_delta,整数用 scalar。**这是工业光线追踪器的常见妥协**——Embree、OptiX 也用类似策略。

### 2.4 真正的 SIMD 完美版

如果硬件支持 AVX2(整数 SIMD),把 i/j/k 也打包:

```rust
use std::simd::i64x4;

let step_x_v = i64x4::from_array([step_x[0], step_x[1], step_x[2], step_x[3]]);
let i_v = i64x4::from_array([i[0], i[1], i[2], i[3]]);
let new_i_v = i_v + step_x_v;  // 一条指令算 4 个
```

但 portable-simd 的支持仍在演化,Casey 用了简化版。

**数据布局的另一个优化**:voxel grid 的内存布局。如果 grid 是 row-major(C-order),相邻 voxel 在内存中连续——cache 友好。但 ray 方向任意,可能沿对角走,cache miss 多。**优化**:用 Morton order(Z-order)——把 3D 坐标 interleave 成 1D 索引,相邻 voxel 在 Morton 索引下也"大致相邻"。这是 voxel raycast 的标准优化,GPUs 的 texture cache 通常 Morton 化。

**SIMD 优化的 3 个层次**(Casey 视频里强调):
1. **数据布局**(50%):SoA、对齐、Morton——决定 SIMD 能多有效
2. **指令选择**(30%):用 `addps` 而非 4 个 `addss`,用 FMA 而非 mul + add
3. **分支优化**(20%):mask 替代分支,避免 SIMD divergence

**优化顺序**:数据布局 > 指令选择 > 分支优化。如果数据布局错(如 AoS),再好的指令也救不回来。

## 3 · 四域深入

### 3.1 · 游戏编程视角

SIMD raycast 的工业用法:

- **Voxel raycast**:CryEngine LPV,GPU compute shader
- **Ray tracing(BVH)**:Embree(Intel),8/16-wide AVX-512
- **Particle physics**:每帧几万粒子,4-wide SIMD 算碰撞
- **Animation**:骨骼变换矩阵 4x4,SIMD 矩阵乘法

游戏引擎的"内层循环"几乎都是 SIMD 优化的。

### 3.2 · 图形学视角

GPU 本质是 **SIMT**(单指令多线程),是 SIMD 的"伪装"——程序员看到的是单条 ray 的代码,GPU 硬件把 32 条 ray(warp)绑一起执行。所以 GPU raycast 天然并行,不用显式 SIMD。

CPU SIMD 的价值在 **少量 ray** 的场景——比如 CPU lightmap baking,几十万 ray 不够 GPU 启动开销,CPU SIMD 反而快。

### 3.3 · Linux 系统编程视角

Linux 内核 SIMD 用得保守——因为 SIMD 寄存器要保存/恢复(上下文切换开销)。内核里只在确定不会睡眠的代码段用 SIMD。

`<linux/simd.h>` 提供包装。x86 kernel 用 SSE/AVX 做 `memcpy`、`memset`、CRC32、AES。

### 3.4 · Rust 生态视角

Rust 的 SIMD 选项:

- **`std::simd`**(portable-simd):nightly,跨平台 SIMD
- **`packed_simd2`**:稳定版,但语法稍长
- **`wide`**:跨平台,封装 f32x4 等
- **直接 intrinsic**:`core::arch::x86_64::__m128`,平台特化

```rust
// portable-simd 示例
#![feature(portable_simd)]
use std::simd::f32x4;

let a = f32x4::from_array([1.0, 2.0, 3.0, 4.0]);
let b = f32x4::from_array([5.0, 6.0, 7.0, 8.0]);
let c = a + b;  // 一次指令算 4 个
println!("{:?}", c.to_array());  // [6.0, 8.0, 10.0, 12.0]
```

如果不想用 nightly,用 `wide`:

```rust
use wide::f32x4;

let a = f32x4::from([1.0, 2.0, 3.0, 4.0]);
let b = f32x4::from([5.0, 6.0, 7.0, 8.0]);
let c = a + b;
```

## 4 · 认知地图

### 4.1 上级

- **Data parallelism** —— 数据并行(同一操作多份数据)
- **Vectorization** —— 向量化(scalar → vector)
- **Amanatides-Woo algorithm** —— 经典 voxel raycast

### 4.2 同级

| 方案 | 关键差别 | 何时用 | 本项目选了哪个 |
|---|---|---|---|
| Scalar raycast | 简单,慢 | 几百 ray | 否 |
| SIMD 4-wide | 快 4 倍,代码复杂 | 几万 ray | **是** |
| GPU raycast | 极快,需 GPU 调度 | 几百万 ray | 否 |
| BVH + raycast | 高精度,慢 | 精确碰撞 | 否 |

### 4.3 下级

- **Ray packet** —— 多条 ray 打包
- **Mask/divergence** —— 处理 ray 完成时间不同
- **Amanatides-Woo** —— voxel 遍历算法

## 5 · 对照与变奏

SIMD 演化:

- **MMX(1996)**:64-bit,整数 SIMD
- **SSE(1999)**:128-bit,4-wide float
- **AVX(2011)**:256-bit,8-wide float
- **AVX-512(2016)**:512-bit,16-wide float
- **ARM NEON**:128-bit,Raspberry Pi / 手机
- **RISC-V V**:可变长度 vector

每代翻倍宽度,但程序员的代码也复杂化。

GPU 的 SIMT 思路:让程序员不操心 SIMD,GPU 硬件自动 batch。代价:必须 1000+ ray 才划算(启动开销)。

## 6 · 关联 Day

- **铺垫**:day547(光探针框架)—— raycast 是查询探针的方式
- **铺垫**:day548(voxelization)—— grid 内容是 raycast 的"地图"
- **当天**:day550 —— SIMD raycast
- **后续**:day551(探针间传输)—— 用 raycast 检测遮挡
- **后续**:day568(调试 raycaster)—— raycast bug 排查

## 7 · 变式训练

### Lv1 · 概念辨析

**题**:为什么 SIMD 必须"齐步走"——所有 lane 同时执行同一指令?如果 ray 1 撞墙了,ray 2 还在走,怎么处理?

**参考解答**:SIMD 硬件设计是"一个控制流 + 多个数据流"——指令只有一份,数据有多份。如果允许每条 ray 走不同控制流,等于回到 scalar。处理 divergence 用 **mask**:每条 ray 有 active 位,撞墙的 ray 设 mask=false,后续指令仍然执行但结果丢弃(写到无关寄存器)。这样硬件仍然齐步走,但 mask 控制每条 ray 的"逻辑结果"是否生效。

### Lv2 · 动手实践

**题**:用 `wide` crate 写 4-wide f32 加法,验证比 scalar 快。

要求:
- 创建 10000 个 f32 数组 a 和 b
- 用 scalar 算 a[i] + b[i]
- 用 wide::f32x4 SIMD 算
- 打印两种方法的耗时

**参考解答**:

```rust
// Cargo.toml: wide = "0.7"

use wide::f32x4;
use std::time::Instant;

fn scalar_add(a: &[f32], b: &[f32], out: &mut [f32]) {
    for i in 0..a.len() {
        out[i] = a[i] + b[i];
    }
}

fn simd_add(a: &[f32], b: &[f32], out: &mut [f32]) {
    let n = a.len();
    let mut i = 0;
    while i + 4 <= n {
        let av = f32x4::from_slice(&a[i..i+4]);
        let bv = f32x4::from_slice(&b[i..i+4]);
        (av + bv).write_to_slice_unaligned(&mut out[i..i+4]);
        i += 4;
    }
    while i < n {
        out[i] = a[i] + b[i];
        i += 1;
    }
}

fn main() {
    let n = 100_000;
    let a: Vec<f32> = (0..n).map(|i| i as f32).collect();
    let b: Vec<f32> = (0..n).map(|i| (i*i) as f32).collect();
    let mut out_scalar = vec![0.0; n];
    let mut out_simd = vec![0.0; n];

    // 跑多次取平均
    let t1 = Instant::now();
    for _ in 0..100 {
        scalar_add(&a, &b, &mut out_scalar);
    }
    let dt1 = t1.elapsed();

    let t2 = Instant::now();
    for _ in 0..100 {
        simd_add(&a, &b, &mut out_simd);
    }
    let dt2 = t2.elapsed();

    println!("Scalar: {:?}", dt1);
    println!("SIMD:   {:?}", dt2);
    println!("Speedup: {:.2}x", dt1.as_secs_f64() / dt2.as_secs_f64());

    // 验证结果一致
    for i in 0..n {
        assert_eq!(out_scalar[i], out_simd[i]);
    }
}
```

### Lv3 · 迁移设计

**题**:音频处理也适合 SIMD。设计一个 audio equalizer:

- 输入:f32 数组(PCM 音频)
- 处理:对每个 sample 应用低通滤波 `y[n] = 0.5 * x[n] + 0.5 * y[n-1]`
- 输出:f32 数组

注意:这个公式有依赖(y[n] 依赖 y[n-1]),不能直接 SIMD。怎么处理?

**提示**:研究 IIR vs FIR 滤波——FIR 无依赖可以直接 SIMD,IIR 要 block processing(每块 4 个一起算)。

### Lv4 · 开源贡献

**题**:`rayon` 是 Rust 的并行库(线程版),它的 SIMD 版 `rayon-simd` 不存在。读 `rayon` 源码,看它怎么处理 work-stealing,提一个 doc 改进 PR。

repo: https://github.com/rayon-rs/rayon

## 8 · Rust / Arch 落地代码

完整的 4-wide SIMD raycast(简化):

```rust
// 用数组模拟 SIMD lane,实际可以用 wide::f32x4
struct RayPacket4 {
    origin_x: [f32; 4],
    origin_y: [f32; 4],
    origin_z: [f32; 4],
    dir_x: [f32; 4],
    dir_y: [f32; 4],
    dir_z: [f32; 4],
    i: [i64; 4],
    j: [i64; 4],
    k: [i64; 4],
    t_max_x: [f32; 4],
    t_max_y: [f32; 4],
    t_max_z: [f32; 4],
    t_delta_x: [f32; 4],
    t_delta_y: [f32; 4],
    t_delta_z: [f32; 4],
    active: [bool; 4],
    hit: [Option<(i64, i64, i64)>; 4],
}

impl RayPacket4 {
    fn new(origins: &[[f32; 3]; 4], dirs: &[[f32; 3]; 4], voxel_size: f32) -> Self {
        let mut p = RayPacket4 {
            origin_x: [origins[0][0], origins[1][0], origins[2][0], origins[3][0]],
            origin_y: [origins[0][1], origins[1][1], origins[2][1], origins[3][1]],
            origin_z: [origins[0][2], origins[1][2], origins[2][2], origins[3][2]],
            dir_x: [dirs[0][0], dirs[1][0], dirs[2][0], dirs[3][0]],
            dir_y: [dirs[0][1], dirs[1][1], dirs[2][1], dirs[3][1]],
            dir_z: [dirs[0][2], dirs[1][2], dirs[2][2], dirs[3][2]],
            i: [0; 4], j: [0; 4], k: [0; 4],
            t_max_x: [0.0; 4], t_max_y: [0.0; 4], t_max_z: [0.0; 4],
            t_delta_x: [0.0; 4], t_delta_y: [0.0; 4], t_delta_z: [0.0; 4],
            active: [true; 4],
            hit: [None, None, None, None],
        };

        for r in 0..4 {
            p.i[r] = (origins[r][0] / voxel_size).floor() as i64;
            p.j[r] = (origins[r][1] / voxel_size).floor() as i64;
            p.k[r] = (origins[r][2] / voxel_size).floor() as i64;

            let step_x = if dirs[r][0] > 0.0 { 1.0 } else { -1.0 };
            let boundary_x = (p.i[r] as f32 + if dirs[r][0] > 0.0 { 1.0 } else { 0.0 }) * voxel_size;
            p.t_max_x[r] = (boundary_x - origins[r][0]) / dirs[r][0];
            p.t_delta_x[r] = (voxel_size / dirs[r][0]).abs();
            // 同理 y, z(略)
        }
        p
    }

    fn step(&mut self, grid: &VoxelGrid) -> bool {
        let mut any_active = false;
        for r in 0..4 {
            if !self.active[r] { continue; }
            any_active = true;

            // 检查当前 voxel
            if grid.is_occupied(self.i[r], self.j[r], self.k[r]) {
                self.hit[r] = Some((self.i[r], self.j[r], self.k[r]));
                self.active[r] = false;
                continue;
            }

            // 走到下一个 voxel
            if self.t_max_x[r] < self.t_max_y[r] && self.t_max_x[r] < self.t_max_z[r] {
                self.i[r] += if self.dir_x[r] > 0.0 { 1 } else { -1 };
                self.t_max_x[r] += self.t_delta_x[r];
            } else if self.t_max_y[r] < self.t_max_z[r] {
                self.j[r] += if self.dir_y[r] > 0.0 { 1 } else { -1 };
                self.t_max_y[r] += self.t_delta_y[r];
            } else {
                self.k[r] += if self.dir_z[r] > 0.0 { 1 } else { -1 };
                self.t_max_z[r] += self.t_delta_z[r];
            }

            // 出界
            if self.i[r] < 0 || self.j[r] < 0 || self.k[r] < 0 ||
               self.i[r] as usize >= grid.dim[0] ||
               self.j[r] as usize >= grid.dim[1] ||
               self.k[r] as usize >= grid.dim[2] {
                self.active[r] = false;
            }
        }
        any_active
    }
}

struct VoxelGrid {
    occupied: Vec<bool>,
    dim: [usize; 3],
}

impl VoxelGrid {
    fn is_occupied(&self, i: i64, j: i64, k: i64) -> bool {
        if i < 0 || j < 0 || k < 0 { return false; }
        let i = i as usize; let j = j as usize; let k = k as usize;
        if i >= self.dim[0] || j >= self.dim[1] || k >= self.dim[2] { return false; }
        self.occupied[(k * self.dim[1] + j) * self.dim[0] + i]
    }
}

fn main() {
    let mut grid = VoxelGrid {
        occupied: vec![false; 16*16*16],
        dim: [16, 16, 16],
    };
    // 在 (5,5,5) 放一个墙
    grid.occupied[(5 * 16 + 5) * 16 + 5] = true;

    let origins = [[0.0; 3]; 4];
    let dirs = [
        [1.0, 0.5, 0.0],
        [1.0, 0.5, 0.0],
        [1.0, 0.5, 0.0],
        [1.0, 0.5, 0.0],
    ];
    let mut packet = RayPacket4::new(&origins, &dirs, 1.0);

    while packet.step(&grid) {}

    for r in 0..4 {
        println!("Ray {}: {:?}", r, packet.hit[r]);
    }
}
```

Arch 工具:验证 SIMD 优化效果。

```bash
# 编译 release + 自动 SIMD
RUSTFLAGS="-C target-cpu=native -C opt-level=3" cargo build --release

# 看 assembly 里有没有 SIMD 指令
cargo install cargo-asm
cargo asm --release my_crate::simd_add | grep -E '(addps|vmovups)'
# 如果看到 addps(SSE)或 vaddps(AVX),说明 SIMD 生效

# 性能对比
perf stat -e instructions,cycles ./target/release/simd_demo

# SIMD 版应该 instructions 少 4 倍(理论上)
```

如果 SIMD 没生效,常见原因:

- 数据没对齐(用 `f32x4::from_slice_unaligned` 强制)
- 编译器不知道 target CPU(`RUSTFLAGS="-C target-cpu=native"`)
- 循环里有分支(SIMD 不喜欢分支,用 mask 替代)

## 9 · 延伸阅读

- 本仓库 `days/phase-7/day547.md` —— 光探针框架
- Amanatides & Woo 1987 "A Fast Voxel Traversal Algorithm" —— 原始论文
- Rust portable-simd 文档 https://std-dev-guide.rust-lang.org/features/simd.html
