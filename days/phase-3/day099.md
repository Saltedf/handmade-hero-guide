---
day: 099
phase: 3
title_en: "Test Enviroment Maps"
title_zh: "测试环境贴图
type: coding
difficulty: 3
duration: "1.5h"
hh_url: "https://guide.handmadehero.org/code/day099/"
domains: [game, graphics, rust]
prereqs: ["day098", "day096"]
---

# Day 099 · 测试环境贴图

> Phong 光照给你**直接光**——光从光源直线到达表面。但现实里还有**间接光**——光被环境(天空、墙壁、其他物体)反射后才到达表面。比如金属球反射出蓝天白云,玻璃杯折射出后面的图案。这就是**环境贴图**(environment map)。今天 Casey 加上环境贴图,让 sprite 表面反射出周围环境——立体感和真实感立刻上一个台阶。

## 0 · 为什么要有这一天

考虑一个抛光金属球。它**反射**周围环境——你看到球上有蓝天、有地面、有你自己的倒影。Phong 模型算不出这个——Phong 只算「光从光源直线到达」,不算「光被周围反射到达」。

**环境贴图**(environment map,也叫 reflection map 或 cube map)的思想:
1. 用一张图存储「**周围 360° 的全景**」(可以是 cube map 6 面或 equirectangular 全景图)
2. 渲染时,根据表面**反射向量**查这张图
3. 反射向量指向哪,就从图上采哪个像素

举例:球面上一点的反射向量指向天空 → 查到蓝天色;指向地面 → 查到棕色;指向相机 → 查到你的脸(如果有)。

这就是**环境反射**(environment reflection),让金属/玻璃/水面看起来真实。

今天 Casey 还不做完整 cube map(那需要 6 张图,Day 200+ 加),他先做简化版:
- 一张**球面环境贴图**(sphere map)——把 360° 环境压成一张 2D 图
- 用 Day 096 的反射向量 + 简单 uv 变换采样

这给你 80% 视觉效果,20% 工作量。是教学经典做法。

心理锚点:看完今天,你的 sprite 有了**反射感**——光照时表面闪出环境色,不再是「哑光」的。你能向队友解释「环境贴图让金属像金属」。

## 1 · Casey 今天做了什么

Casey 今天做的事情:

1. 加载一张测试用的环境贴图(简单的 256×256,程序化生成的「天空 + 地面」图案)
2. 计算**反射向量** `R = reflect(-V, N)`(V 是视线,N 是法线)。R 指向「视线被反射的方向」
3. 把 R 转换成 uv 坐标——`u = 0.5 + R.x * 0.5`、`v = 0.5 - R.y * 0.5`(把 [-1, 1] 范围映射到 [0, 1])
4. 采样环境贴图,得到**反射色**
5. 把反射色加到 Phong 结果上(简单加,或按材质 reflectivity 混合)

Casey 强调这只是「**测试**」——明天 Day 100 会写更精确的反射向量(尤其是法线贴图下的),Day 200+ 切 cube map。今天是「让流程跑通」。

## 2 · 心智模型

### 费曼类比:镜子 vs 鸡蛋盒

**镜子**(完美反射):你站在镜子前,镜子把你的影像反射回你眼睛。这就是「视线被反射」——入射方向 V,反射方向 R = reflect(-V, N),N 是镜面法线。

**金属球**(部分反射):不是完美镜子,但表面光滑度足够反射环境——只是反射被「**模糊**」了一些。Phong 高光其实是一种环境反射——只不过把整个环境简化成一个「光源点」。

**环境贴图** = 把镜子里的图像预先存好,渲染时按反射方向查表。这样不需要真正去渲染周围的物体(那需要 ray tracing,贵得多)。

### 严谨原理

#### 2.1 反射向量

回顾 Day 044 / Day 096 的反射公式:

```
R = I - 2(I · N) N
```

I 是入射方向(从光源 / 相机指向表面)。**但环境反射里,I 是视线**,而且**反向**——从表面指向相机。

约定:`V` = view direction(从表面指向相机),那么入射 = -V。反射:
```
R = reflect(-V, N) = -V - 2(-V · N) N = 2(V·N)N - V
```

R 是「视线被法线反射后的方向」。指向「如果你是相机,你能看到的反射方向」。

#### 2.2 反射向量到环境贴图 uv

Sphere map(球面贴图)约定:把 360° 环境投影到一张 2D 图。常见映射:

```
M = 2 * sqrt((R.x + 1)²/8 + (R.z + 1)²/8 + (R.y + 1)²/8 * ...)
# 太复杂,简化版:
u = 0.5 + R.x * 0.5  # 把 R.x ∈ [-1, 1] 映射到 [0, 1]
v = 0.5 - R.y * 0.5  # 注意 y 取反(因为图像 v 朝下)
```

简化版忽略 z(深度),只用 xy 平面投影。**不严格正确**,但视觉效果接近。

#### 2.3 Cube map(简介)

真正的环境贴图用 **cube map**——6 张图代表立方体 6 面。反射向量 R 直接当 3D 方向,选最大分量的轴对应的面,再从那张图采样。

```
fn sample_cube(cube: &CubeMap, r: Vec3) -> Color {
    let abs_x = r.x.abs();
    let abs_y = r.y.abs();
    let abs_z = r.z.abs();
    let (face, u, v) = if abs_x >= abs_y && abs_x >= abs_z {
        if r.x > 0 { (Face::PosX, r.z, -r.y) } else { (Face::NegX, -r.z, -r.y) }
    } else if abs_y >= abs_x && abs_y >= abs_z {
        // 类似
    } else {
        // 类似
    };
    cube.faces[face].sample_uv(u, v)
}
```

复杂但严格正确。Casey HH Day 200+ 切 cube map。

#### 2.4 加入 Phong

完整公式:

```
final = ambient
      + diffuse * surface * max(0, N · L)
      + specular * pow(max(0, R · V), shininess)
      + env_color * reflectivity   # 新增
```

`reflectivity` 是材质的「反射率」——金属 0.8,塑料 0.1,木头 0.0。`env_color` 是环境贴图采样结果。

**注意**:env_color 也在 linear 空间。

#### 2.5 实现

```rust
struct EnvMap {
    bitmap: LinearBitmap,
}

impl EnvMap {
    fn sample_reflection(&self, reflect_dir: Vec3) -> Color {
        // 简化 sphere map 映射
        let u = 0.5 + reflect_dir.x * 0.5;
        let v = 0.5 - reflect_dir.y * 0.5;
        let texel = self.bitmap.sample(u.clamp(0.0, 1.0), v.clamp(0.0, 1.0));
        Color { r: texel[0], g: texel[1], b: texel[2] }
    }
}

#[derive(Copy, Clone)]
struct Material {
    diffuse_color: Color,
    shininess: f32,
    reflectivity: f32,  // 0..1, 0 = 不反射, 1 = 完全反射
}

fn shade_with_env(
    surface_normal: Vec3,
    view_dir: Vec3,
    light: Light,
    material: Material,
    env: &EnvMap,
    ambient: Color,
) -> Color {
    // Phong(同 Day 096)
    let n = surface_normal;
    let v = view_dir;
    let l = light.direction;
    let h = l.add(v).normalized();
    let ndotl = n.dot(l).max(0.0);
    let ndoth = n.dot(h).max(0.0);
    let specular = ndoth.powf(material.shininess);

    let diffuse_color = material.diffuse_color;
    let phong = Color {
        r: ambient.r + light.intensity * light.color.r * (diffuse_color.r * ndotl + specular),
        g: ambient.g + light.intensity * light.color.g * (diffuse_color.g * ndotl + specular),
        b: ambient.b + light.intensity * light.color.b * (diffuse_color.b * ndotl + specular),
    };

    // 环境反射
    let r = reflect_vector(v, n);
    let env_color = env.sample_reflection(r);

    // 混合:phong + env_color * reflectivity
    let refl = material.reflectivity;
    Color {
        r: phong.r * (1.0 - refl) + env_color.r * refl,
        g: phong.g * (1.0 - refl) + env_color.g * refl,
        b: phong.b * (1.0 - refl) + env_color.b * refl,
    }
}

fn reflect_vector(v: Vec3, n: Vec3) -> Vec3 {
    // R = 2(V·N)N - V
    let d = v.dot(n);
    n.scale(2.0 * d).sub(v)
}

fn main() {
    // 测试环境贴图:简单的 1x1 蓝色
    let env_bytes = vec![0, 100, 200, 255];
    let env_bitmap = LinearBitmap::from_srgba_straight(1, 1, &env_bytes);
    let env = EnvMap { bitmap: env_bitmap };

    let mat = Material {
        diffuse_color: Color { r: 0.5, g: 0.5, b: 0.5 },
        shininess: 30.0,
        reflectivity: 0.5,
    };
    let normal = Vec3::new(0.0, 0.0, 1.0);
    let view = Vec3::new(0.0, 0.0, 1.0);
    let light = Light {
        direction: Vec3::new(0.0, 0.0, 1.0),
        color: Color { r: 1.0, g: 1.0, b: 1.0 },
        intensity: 0.0,  // 暂时关闭,看 env 反射
    };
    let ambient = Color { r: 0.0, g: 0.0, b: 0.0 };

    let lit = shade_with_env(normal, view, light, mat, &env, ambient);
    println!("lit color: r={:.3} g={:.3} b={:.3}", lit.r, lit.g, lit.b);
    // diffuse 是灰,reflectivity 0.5,env 是蓝(0, 100, 200 的 linear)
    // 预期:lit 应该有一定蓝分量
}
```

## 3 · 四域深入

### 3.1 · 🎮 游戏编程视角

环境贴图在游戏的应用:

1. **金属 / 玻璃物体**:剑、盾、汽车漆面、水面
2. **湿润表面**:雨后街道、湿润石头
3. **眼睛**:角色眼睛的角膜反射(让眼睛「活」起来)
4. **汽车 / 飞机 simulator**:整个车体 / 机身的反射

**坑:静态 vs 动态环境贴图**:
- **静态**:预渲染的 cube map(美术做),运行时不变。简单快,但物体移到别处还反射原来环境(假)
- **动态**:每帧重渲染 cube map(从物体中心 6 个方向)。真,但慢 6 倍
- **屏幕空间反射 (SSR)**:用当前 framebuffer 做近似反射。中等质量,中等开销

工业做法:**静态 cube map + 局部 SSR**——大部分环境用 cube map,附近的物体用 SSR 增强。

### 3.2 · 🎨 图形学视角

环境贴图的几种格式:

1. **Sphere map**:2D,1998 PlayStation 时代。简单,但有 singularity(极点)。Casey HH 用这个。
2. **Cube map**:6 张 2D,OpenGL 3.0+ 标准。最常见。
3. **Dual paraboloid**:2 张 2D,各自半球。比 sphere map 好,但少用。
4. **Equirectangular**:1 张 2D,全景图(经纬度展开)。HDR 全景图常用。

**Prefilter environment map**:PBR 用法——预先对 cube map 做不同 roughness 的模糊版本,运行时按材质 roughness 采样。这是 PBR IBL(image-based lighting)的核心。Casey HH Day 200+ 详细讲。

### 3.3 · 🐧 Linux 系统编程视角

环境贴图加载的 Linux 工具:

```bash
# 1. HDR 全景图(.hdr / .exr 格式)
sudo pacman -S openexr
# 或 Rust crate:
# [dependencies]
# openexr = "0.9"

# 2. 把全景图转 cube map
# 用 HDRI-to-CubeMap 工具:
# https://github.com/danjohnson/HDRI-to-CubeMap
# 或 Blender 操作

# 3. 在线下载免费 HDRI
# https://hdrihaven.com/  (CC0)
```

### 3.4 · 🦀 Rust 生态视角

Rust 处理环境贴图的 crate:

1. **`glam`**:向量数学(反射、变换)
2. **`image`**:加载 2D 图
3. **`openexr`**:HDR 图像
4. **`wgpu`**:GPU cube texture

**Rust trait 抽象**:

```rust
trait EnvironmentMap {
    fn sample(&self, direction: Vec3) -> Color;
}

struct SphereMap { bitmap: LinearBitmap }
struct CubeMap { faces: [LinearBitmap; 6] }

impl EnvironmentMap for SphereMap { /* ... */ }
impl EnvironmentMap for CubeMap { /* ... */ }
```

这让 shade 函数可以接受 `&dyn EnvironmentMap` 或 `impl EnvironmentMap`,以后切换格式不需要改 shade 函数。

## 4 · 认知地图

### 4.1 上级

- **Image-Based Lighting (IBL)** — 用图像代替解析光源的整套技术
- **Indirect illumination** — 间接光(被环境反射的光)
- **Reflection model** — 完整 BRDF 的反射部分

### 4.2 同级

| 反射方法 | 来源 | 真实度 | 性能 |
|---|---|---|---|
| Phong specular | 解析(单一光源) | 低 | 极快 |
| Sphere map | 预渲染 2D | 中 | 快 |
| Cube map | 预渲染 6 张 2D | 高 | 快 |
| Screen-space reflection | 当前帧 buffer | 高(局部) | 中 |
| Ray tracing | 实时光线追踪 | 最高 | 慢 |

### 4.3 下级

- **反射向量 (reflection vector)** — `2(V·N)N - V`
- **环境贴图映射** — R → uv 的变换
- **reflectivity** — 材质反射率
- **cube map 6 面** — posX/negX/posY/negY/posZ/negZ
- **预过滤 (prefilter)** — 按 roughness 预先模糊

## 5 · 对照与变奏

### Sphere map vs Cube map

| | Sphere Map | Cube Map |
|---|---|---|
| 内存 | 1 张 2D | 6 张 2D |
| 采样复杂度 | 简单 | 选面 + uv |
| 极点问题 | 有(在两极压缩) | 无 |
| GPU 支持 | 旧 API | 现代标准 |
| Casey HH 用 | Day 099 | Day 200+ |

### Static vs Dynamic env map

| | 静态 | 动态 |
|---|---|---|
| 更新频率 | 一次 | 每帧 |
| 真实度 | 低(物体移动不更新) | 高 |
| 性能 | 免费 | 重渲染 6 倍 |
| 适合 | 静态场景 | 角色装备 / 水面 |

### 历史

- 1976 Blinn & Newell:first environment map(sphere map)
- 1998 PlayStation 1:大量用 sphere map
- 2000s cube map 成标准
- 2010s IBL + PBR
- 2018+ RTX 实时光追

## 6 · 关联 Day

- **铺垫**:
  - [day098](day098.md) — 代码清理(本日基于清理后代码)
  - [day096](day096.md) — Phong 光照(反射向量基础)
  - [day100](day100.md) — 完整反射向量(明天)
- **当天**:day099(本日,环境贴图)
- **后续**:
  - [day100](day100.md) — 反射向量精确版
  - [day101](day101.md) — 逆矩阵(法线变换需要)
  - [day103](day103.md) — 完整环境反射
  - [day200+](../phase-6/) — cube map + PBR IBL

## 7 · 变式训练

### Lv1 · 概念辨析

**题**:Phong 的 specular 和环境贴图有什么关系?

**参考解答**:Phong 的 specular 本质上是**环境反射的特例**——把整个环境简化成一个「光源点」。环境贴图是 specular 的「**升级**」——把单一光源扩展成整个环境的图像。

数学上,Phong specular = 「环境贴图所有方向积分,但只算反射方向的高斯权重」——而完整环境贴图 = 「积分整个环境贴图,按 BRDF 权重」。PBR 把这个关系严格化。

### Lv2 · 动手实践

**题**:在 sprite 渲染器里加 env map 支持。生成一个简单的 256×256 环境贴图(上半蓝下半绿),让一个有 reflectivity=0.3 的 sprite 渲染时上半部分带蓝色反射、下半带绿色反射。

完成标准:sprite 顶部边缘偏蓝,底部边缘偏绿,中间过渡。

**参考解答**:见 §2 完整代码,生成环境贴图:

```rust
fn gen_test_env() -> Vec<u8> {
    let mut bytes = vec![0u8; 256 * 256 * 4];
    for y in 0..256 {
        for x in 0..256 {
            let idx = (y * 256 + x) * 4;
            if y < 128 {
                // 上半:蓝天
                bytes[idx] = 100; bytes[idx+1] = 150; bytes[idx+2] = 220; bytes[idx+3] = 255;
            } else {
                // 下半:绿地
                bytes[idx] = 100; bytes[idx+1] = 180; bytes[idx+2] = 100; bytes[idx+3] = 255;
            }
        }
    }
    bytes
}
```

### Lv3 · 迁移设计

**题**:你想做**水面反射**——湖面反射出岸边树木。怎么实现?

提示:
- 水面法线:大致朝上(0, 1, 0),但有 ripple 让它略变
- 反射向量:R = reflect(-V, N),V 是视线
- 用 cube map(预渲染岸边)或 SSR(实时反射 framebuffer)
- 水的颜色 + 反射色按 fresnel 混合(角度越大反射越强)

### Lv4 · 开源贡献

**题**:`rend3` 或 `bevy_pbr` 有 cube map 支持。

```bash
gh repo clone BVE-Reborn/rend3
cd rend3
grep -rn "cube_map\|CubeMap" rend3/src/ | head
```

读 cube map 实现。

**可能的贡献方向**:
1. **doc**:cube map 函数没解释采样公式,补一段
2. **test**:cube map 边界(face 接缝)没测试,补一个
3. **example**:加动态 cube map(实时反射)示例

## 8 · Rust / Arch 落地代码

完整 sprite + env 渲染(片段):

```rust
// [Vec3 / Color / Light / Material / EnvMap / shade_with_env / reflect_vector 同 §2]

fn render_sprite_with_env(
    fb: &mut LinearFramebuffer,
    sprite: &Sprite,
    env: &EnvMap,
    material: Material,
    origin: Vec2,
    m: Mat2,
    light: Light,
    view: Vec3,
    config: RenderConfig,
) {
    let inv = m.inverse();
    let (min_x, max_x, min_y, max_y) = compute_aabb(&origin, &m, fb.width, fb.height);
    for py in min_y..=max_y {
        for px in min_x..=max_x {
            let (u, v) = match pixel_to_uv(PixelCoord { px, py }, origin, inv) {
                Some(uv) => uv,
                None => continue,
            };
            let sample = sprite.sample_surface(u, v);
            if sample.alpha < config.alpha_threshold { continue; }
            // 用 material(注意:surface_color 从 sprite.diffuse 来,但 reflectivity 是 sprite 属性)
            let mut mat = material;
            mat.diffuse_color = sample.color;
            let lit = shade_with_env(sample.normal, view, light, mat, env, config.ambient);
            let bg = fb.pixels[py * fb.width + px];
            fb.pixels[py * fb.width + px] = blend_premul_linear(bg, lit, sample.alpha);
        }
    }
}
```

### Arch Linux 工具

```bash
# 1. 下载免费 HDRI 全景图(测试用)
# https://hdrihaven.com/hdris/category/?c=skies&hdr=questkeep
# 或自己用 Blender 渲染 cube map

# 2. 转换 HDR 到 LDR(用 openexr + ImageMagick)
sudo pacman -S openexr
oiiotool --pattern constant 256x256 4 -d uint8 -o env.png

# 3. 性能分析
perf record -g ./target/release/env_demo
perf report
# 热点应该在 sample_reflection + shade_with_env
```

### Troubleshooting

- **反射看起来反了** — 反射向量公式错了。确认是 `2(V·N)N - V`(从表面指向相机方向被反射)
- **反射太强** — reflectivity 太高,降到 0.2-0.3
- **反射和环境不一致** — sphere map 映射不严格,边界有问题。Day 200+ 切 cube map 解决
- **没有反射效果** — 检查 reflectivity 是不是 0;检查 normal 是不是单位向量

## 9 · 延伸阅读(可选补充,非必需)

本仓库本地:
- [day098.md](day098.md) — 代码清理
- [day096.md](day096.md) — Phong 光照
- [day100.md](day100.md) — 完整反射向量(明天)
- [deep-dives/texture-mapping.md](deep-dives/texture-mapping.md) — 纹理映射全集

外部稳定 URL(可选):
- LearnOpenGL Cubemaps:https://learnopengl.com/Advanced-OpenGL/Cubemaps
- PBR Book IBL:https://www.pbr-book.org/3ed-2018/Light_Transport_I_Surface_Reflection/Sampling_Reflection_Functions
- Scratchapixel Environment Mapping:https://www.scratchapixel.com/lessons/3d-basic-rendering/environment-map

真实开源源码:
- `bevy_pbr` cube map:https://github.com/bevyengine/bevy/blob/main/crates/bevy_pbr/src/lighting.rs
- HH 原版 day099 C 代码:https://github.com/HandmadeHero/handmade-hero/tree/main/code/day099
