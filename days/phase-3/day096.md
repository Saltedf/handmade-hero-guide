---
day: 096
phase: 3
title_en: "Introduction to Lighting"
title_zh: "光照入门
type: concept
difficulty: 4
duration: "2h"
hh_url: "https://guide.handmadehero.org/code/day096/"
domains: [game, graphics, math, rust]
prereqs: ["day095", "day091", "phase-2/day041"]
---

# Day 096 · 光照入门

> 你的 sprite 现在能旋转、有纹理、半透明混合正确。但它看起来「**平**」——像个剪纸贴在屏幕上。现实世界不是这样的:物体朝着光的面亮,背光的面暗,光从某个角度照过来时表面有明暗变化。**这就是光照**。今天 Casey 引入**Phong 光照模型**——游戏和图形学最经典的光照模型,把「光的方向、表面朝向、视线方向」结合起来,算出每个像素应该多亮。

## 0 · 为什么要有这一天

考虑你画一个 3D 立方体在屏幕上。立方体有 6 个面,每个面贴一张纹理(一样的砖墙图)。没有光照,所有面看起来一样亮——立方体像个**纸折的盒子**,没有立体感。

加上光照后:
- 朝光的面**亮**
- 背光的面**暗**
- 侧光的面**有从亮到暗的渐变**
- 立刻看起来是**立体的**

这就是光照的魔法。具体怎么做?

观察现实世界:**光照强度取决于光的方向和表面朝向的夹角**。
- 光**直射**表面(垂直)→ 100% 光照
- 光**斜射**(45°)→ ~70% 光照(`cos 45° = 0.707`)
- 光**平行掠过**表面(0°)→ 0% 光照
- 光**从背面**来 → 0%(表面没受光)

数学上,**光向量和表面法线(垂直于表面的向量)的点积**告诉你光强:

```
diffuse = max(0, N · L)
```

N 是法线,L 是指向光源的向量(都归一化)。这就是 **Lambert 漫反射**(Lambert's cosine law),图形学最古老也最重要的光照公式。

Lambert 解释了**漫反射**(matte 表面)——但没解释**高光**(specular highlights,金属/塑料的反光)。Phong 1975 年加上一个 specular 项:

```
specular = pow(max(0, R · V), shininess)
```

R 是反射光向量,V 是视线向量。`pow(..., shininess)` 让高光集中在反射方向附近。

完整 Phong 模型:

```
final = ambient + diffuse * tex_color + specular * spec_color
```

ambient 是「环境光」(常数,模拟间接光),diffuse 是 Lambert,specular 是 Phong。三部分加起来就是 1975-2010 年代游戏的标准光照。

心理锚点:看完今天,你能向队友解释「为什么 3D 物体需要光照才有立体感」。你能用 Rust 写 Phong 光照。你能用 30 秒说清「点积在光照里的角色」。

## 1 · Casey 今天做了什么

Casey 今天做的事情:

1. 在白板上画**光照几何**——光源 L、表面法线 N、视线 V、反射 R 四个向量
2. 推导 Lambert 漫反射:`diffuse = max(0, N · L)`
3. 推导 Phong 镜面反射:`specular = pow(max(0, R · V), n)`,其中 `R = reflect(-L, N)`
4. 介绍 Blinn-Phong 优化:`specular = pow(max(0, N · H), n)`,H 是半角向量 `(L + V) / |L + V|`——更快,质量接近
5. 把光照整合到 sprite 渲染:每个 sprite 的每个像素查「表面法线」,算光照,加到颜色上
6. 强调:**所有光照在 linear 空间做**(Day 094 的预备)

Casey 强调,光照的**几何直觉**比公式更重要——你必须能在脑子里画出 N、L、V 的关系。这是后面 Day 097 法线贴图、Day 200+ PBR 的基础。

## 2 · 心智模型

### 费曼类比:正午太阳 vs 日落

中午太阳当头(光从上方来),地面(法线朝上)被**直射**——光强最大。

日落时太阳在地平线(光从侧面来),地面被**斜射**——同样面积的光散到更大区域,光强减弱(实际是 `cos θ`,θ 是光和法线的夹角)。

这就是 Lambert 余弦定律:**单位面积接收的光强 = 光强 × cos(入射角)**。

游戏里:你可以让一个表面的法线朝任意方向,N·L 自动算出光强。这就是为什么 3D 模型的每个面有不同的法线——它们决定了面在光照下的亮度。

### 严谨原理

#### 2.1 法线 (Normal)

**定义**:法线是垂直于表面的**单位向量**(长度为 1)。平面有 2 个法线方向(正面 / 背面),约定选「朝外的」那个。

2D 平面的法线:旋转 90°。`(1, 0)` 的法线是 `(0, 1)` 或 `(0, -1)`。

3D 三角形的法线:用叉积 `cross(v1 - v0, v2 - v0)`,然后归一化。叉积给出垂直于两个向量的第三个向量(右手定则)。

#### 2.2 Lambert 漫反射

**几何**:光源 L,法线 N,夹角 θ。光照强度:

```
intensity = max(0, cos θ) = max(0, N · L)  (假设 N 和 L 都是单位向量)
```

`max(0, ...)` 因为负数意味着光在背面,不应贡献光强。

```
final_diffuse = light_color × surface_color × max(0, N · L)
```

每个分量(R, G, B)独立算。`light_color` 是光的颜色(比如红色光源 = `(1, 0, 0)`),`surface_color` 是材质颜色(从纹理采样)。

#### 2.3 Phong 镜面反射

漫反射模拟的是**粗糙表面**(磨砂、墙壁、木头)。光滑表面(金属、玻璃)有**镜面高光**(specular highlight)——光从某个角度看,表面反射出一团亮斑。

**Phong 模型**:
1. 算反射光向量:`R = reflect(-L, N) = 2(N·L)N - L`
2. 算视线向量 V(从表面指向相机)
3. 镜面反射 = `pow(max(0, R · V), shininess)`

`shininess` 是「光滑度」——金属 ~100,塑料 ~10,磨砂 ~2。

**问题**:`R · V` 计算需要先算 R,涉及一次反射变换。**Blinn 1977 年优化**:

```
H = normalize(L + V)  # 半角向量
specular = pow(max(0, N · H), shininess)
```

H 是 L 和 V 的「中间」方向——它代表「如果表面绕 H 法线转一点,刚好让 L 反射到 V」。Blinn-Phong 用 N·H 代替 R·V,**少算一次反射**。质量几乎一样,速度快。今天 GPU 标准都用 Blinn-Phong。

#### 2.4 环境光 (Ambient)

Lambert + Phong 模拟直射光。但现实里有**间接光**——光被环境反射,照亮背光面。完全模拟间接光需要「全局光照」(Global Illumination, GI),计算量巨大。

**简化**:加一个常数环境光 `ambient`,模拟「背光面也能看见一点东西」。

```
final = ambient_constant + diffuse + specular
```

ambient 通常很小(0.1-0.2),只为了让背光面不是纯黑。

#### 2.5 完整 Phong 公式

```
final_color = ambient
            + diffuse_intensity * light_color * surface_color * max(0, N · L)
            + specular_intensity * light_color * spec_color * pow(max(0, N · H), shininess)
```

每个 RGB 通道独立计算。所有颜色在 linear 空间。

#### 2.6 实现要点

**1. 法线来源**:
- 2D 矩形:法线固定 (0, 0, 1) 朝相机(因为 2D)
- 3D 三角形:用顶点法线插值(平滑表面),或面法线(平面表面)
- 法线贴图(Day 097):每像素查纹理得到法线

**2. 归一化**:`N`、`L`、`V`、`H` 都必须是单位向量。**插值后归一化**——线性插值破坏单位长度。

**3. Linear 空间**:`light_color * surface_color` 必须在 linear 空间做(见 Day 094)。

**4. 半向量优化**:`H = (L + V) / |L + V|`。如果 L 和 V 都单位,`L + V` 的长度可计算,但归一化需要 `1 / |L+V|`。

### Rust 实现

```rust
#[derive(Copy, Clone, Debug)]
struct Vec3 { x: f32, y: f32, z: f32 }
impl Vec3 {
    fn new(x: f32, y: f32, z: f32) -> Self { Self { x, y, z } }
    fn dot(self, o: Self) -> f32 { self.x*o.x + self.y*o.y + self.z*o.z }
    fn add(self, o: Self) -> Self { Self::new(self.x+o.x, self.y+o.y, self.z+o.z) }
    fn sub(self, o: Self) -> Self { Self::new(self.x-o.x, self.y-o.y, self.z-o.z) }
    fn scale(self, k: f32) -> Self { Self::new(self.x*k, self.y*k, self.z*k) }
    fn length(self) -> f32 { self.dot(self).sqrt() }
    fn normalized(self) -> Self {
        let l = self.length();
        if l > 1e-6 { self.scale(1.0 / l) } else { Self::new(0.0, 0.0, 0.0) }
    }
}

#[derive(Copy, Clone)]
struct Color { r: f32, g: f32, b: f32 }  // linear, 0..1+

#[derive(Copy, Clone)]
struct Light {
    direction: Vec3,  // 指向光源(单位向量)
    color: Color,
    intensity: f32,
}

fn reflect(incident: Vec3, normal: Vec3) -> Vec3 {
    // R = I - 2(I·N)N
    let d = incident.dot(normal);
    incident.sub(normal.scale(2.0 * d))
}

fn phong_lighting(
    surface_color: Color,
    normal: Vec3,
    view_dir: Vec3,    // 指向相机(单位向量)
    light: Light,
    ambient: Color,
    shininess: f32,
) -> Color {
    let n = normal;  // 假设已归一化
    let l = light.direction;
    let v = view_dir;
    let h = l.add(v).normalized();  // Blinn-Phong 半角

    let ndotl = n.dot(l).max(0.0);
    let ndoth = n.dot(h).max(0.0);
    let specular = ndoth.powf(shininess);

    Color {
        r: ambient.r
            + light.intensity * light.color.r * surface_color.r * ndotl
            + light.intensity * light.color.r * specular,
        g: ambient.g
            + light.intensity * light.color.g * surface_color.g * ndotl
            + light.intensity * light.color.g * specular,
        b: ambient.b
            + light.intensity * light.color.b * surface_color.b * ndotl
            + light.intensity * light.color.b * specular,
    }
}

fn main() {
    // 表面:红色,法线朝相机 (0,0,1)
    let surface = Color { r: 1.0, g: 0.0, b: 0.0 };
    let normal = Vec3::new(0.0, 0.0, 1.0);
    let view = Vec3::new(0.0, 0.0, 1.0);

    // 光从右上方斜照下来
    let light = Light {
        direction: Vec3::new(0.7, 0.3, 0.7).normalized(),
        color: Color { r: 1.0, g: 1.0, b: 1.0 },
        intensity: 1.0,
    };
    let ambient = Color { r: 0.1, g: 0.1, b: 0.1 };

    let result = phong_lighting(surface, normal, view, light, ambient, 50.0);
    println!("final color: r={:.3} g={:.3} b={:.3}", result.r, result.g, result.b);
    // 红通道应该有 N·L + specular 的贡献
    // N·L = (0,0,1) · (0.7,0.3,0.7)/|...| = 0.7/sqrt(0.49+0.09+0.49) ≈ 0.662
    // 预期:r ≈ 0.1 + 0.662 + specular
}
```

**逐行解释**:
- `Vec3` — 3D 向量,跟 Day 041 介绍的 Vec2 类似,加一个 z
- `dot` — 点积,光照的核心运算
- `normalized` — 归一化,除零保护
- `reflect` — 反射公式 `I - 2(I·N)N`(同 Day 044 的 2D 版本,3D 同理)
- `powf(shininess)` — `^n`,控制高光锐度
- `max(0.0, ...)` — Lambert 定律要求光强非负

## 3 · 四域深入

### 3.1 · 🎮 游戏编程视角

光照在游戏里的实际应用:

1. **静态光照**(预计算):地图设计时烘焙(bake)光照到纹理。运行时无开销,但不能动态改
2. **动态光照**(实时):每帧重算。开销大,但光源可以动(火把、手电筒、子弹)
3. **多光源**:每个光源独立算 Phong,最后加起来。性能 N 倍
4. **光照衰减**:光强随距离减弱 `1/d²`(物理)。但游戏常简化成线性 `1 - d/range`(更可控)

**坑:法线必须归一化**。插值破坏单位长度,光照结果错。每个像素都要 `normalize()`。

**坑:Half-vector 在 view 接近表面平行时**:`L+V` 接近 0,归一化爆炸。加 epsilon 保护。

**Casey 后续哪天会用到**:
- Day 097 — 法线贴图(每像素查法线,光照算)
- Day 099 — 环境贴图(用 cube map 做反射)
- Day 100 — 反射向量(高光)
- Day 200+ — PBR(Cook-Torrance BRDF,Phong 的物理化升级)

### 3.2 · 🎨 图形学视角

光照模型演化:

| 模型 | 年代 | 复杂度 | 何时用 |
|---|---|---|---|
| Lambert | 1760 | 极简 | 漫反射(纸张、墙壁) |
| Phong | 1975 | 简单 | 高光(塑料、玻璃) |
| Blinn-Phong | 1977 | 简单 | Phong 的优化版(主流) |
| Cook-Torrance | 1982 | 复杂 | PBR(金属、生锈) |
| Oren-Nayar | 1994 | 中等 | 粗糙表面(月球、黏土) |
| PBR (Disney) | 2012 | 复杂 | 现代 3D 游戏/电影 |

**BRDF**(Bidirectional Reflectance Distribution Function):双向反射分布函数,所有光照模型的数学框架。给定入射光 L、出射方向 V、法线 N,BRDF 返回「这个方向反射多少光」。Phong 是 BRDF 的一个特例。

**全局光照 (Global Illumination, GI)**:考虑光的多次反射。完整 GI 需要 path tracing(光线追踪),慢得吓人。游戏用各种近似(SSAO、SSGI、Light Propagation Volumes、Voxel GI)。Day 600+ Casey 会讨论。

### 3.3 · 🐧 Linux 系统编程视角

光照计算本身是纯数学,但性能上 Linux 相关:

1. **SIMD**:Vec3 dot / add / mul 是 3 个 f32 独立运算,SIMD `_mm_mul_ps` 一次算 4 个像素(把每个像素的 RGB 打包)。Casey Day 112+ 大量用。
2. **多线程**:每个光源独立,可以并行算。或者按屏幕分块(tile-based),每块独立。
3. **GPU 计算光照**:Day 235+ 切 OpenGL 后,光照搬到 fragment shader,GPU 几千核心并行算。

```bash
# Linux 上看 GPU 信息
lspci | grep -i vga
# 或
lshw -C display
# 看 GPU 核心数、显存

# glxinfo 看 OpenGL 支持
sudo pacman -S mesa-demos
glxinfo | grep "OpenGL renderer"
```

### 3.4 · 🦀 Rust 生态视角

Rust 实现光照:

**选择 1:CPU 软件光照(今天)**
- 优势:完全控制,易调试
- 劣势:慢(单核几十兆光照/秒)

**选择 2:GPU shader(Day 235+)**
- 用 `wgpu` crate 或 `glow` crate 写 GLSL/WGSL
- 性能 ×1000

**选择 3:混合(Casey HH Day 600+)**
- 光照计算用 GPU compute shader
- 结果合并用 CPU

**`glam` 提供 `Vec3::dot`、`Vec3::normalize`、`Vec3::reflect`**——直接用比手写快(SIMD 优化)。

**`rend3` / `bevy_pbr`** 是 Rust PBR 渲染器,基于 wgpu。


### 3.5 · 🐧 Linux 系统编程视角(内核空间 / 硬件加速)

Linux 系统里"碰撞 / 相交检测"的核心思想在多处出现,理解这些能让你看 Casey 的 2D / 3D collision 算法在更广的语境里。

**内核的 page fault 处理**。虚拟内存到物理内存的映射,本质是"地址空间相交检测"。CPU 访问虚拟地址,MMU 查 page table,如果地址不在已映射范围内 → page fault。内核接 fault,检查(1)地址合法吗(进程的 VMA 区间)、(2)权限对吗(read / write / exec)、(3)page 在物理内存还是 swap。这套检查就是"区间相交 + 属性匹配",和 Casey 在 day 050 的 Minkowski collision 在抽象上一致——"用几何空间判断两个东西是否接触"。

```bash
# 看你进程的 VMA(虚拟内存区域)
cat /proc/$$/maps
# 输出形如:
# 56001234-56005678 r-xp ...  /usr/bin/bash
# 7f1234560000-7f1234780000 r--p ... /usr/lib/libc.so.6

# 触发 page fault(故意访问非法地址)
python3 -c "import ctypes; ctypes.cast(0xdeadbeef, ctypes.POINTER(ctypes.c_int))[0] = 0"
# 输出:Segmentation fault (core dumped)
```

**epoll / poll / select** 是 IO 多路复用,本质是"时间区间相交"。你给内核 N 个 fd,内核检查"哪个 fd 当前可读 / 可写"。这是 N 路并行等待,内核用红黑树 + ready list 高效管理。和 collision detection 的 broad phase 类似——快速排除不可能相交的对,只在 narrow phase 详细检查。

**文件锁(flock / fcntl)**。Linux 文件锁检测"两个进程是否同时持有冲突锁"——这是"锁区间相交"。flock 用 inode-level 锁,fcntl 用 byte-range 锁(`fcntl(F_SETLK, F_WRLCK, {100..200})` 锁字节 100-200)。内核维护锁列表,新锁请求时遍历检查冲突——这就是 interval intersection 检测。

**网络栈的 TCP / IP header 校验**。每个 TCP/IP 包带 checksum,内核收到包校验——本质是"数据完整性检测"。和 collision detection 不直接相关,但**思想相同**——"用一个数学函数快速判断数据是否合法"。

**cgroup 的资源限制**。`cpu.max = 50000 100000` 表示 cgroup 在每 100ms 周期内最多用 50ms CPU。内核定期检查"当前周期已用多少 CPU",超了 throttle。这是"时间区间累积 + 阈值检测",和 Casey 的"span duration + frame budget"逻辑相同。

**netfilter / iptables** 用 BPF 风格的规则匹配,检查每个网络包。"规则匹配"本质是"包属性 vs 规则区间"——"src IP in 192.168.0.0/16 && dst port == 22"是区间检测。BPF (Berkeley Packet Filter) 让用户写过滤器,内核执行——这种"用户定义函数 + 内核执行"模式是 eBPF 的基础。

**kernel scheduling**。Linux CFS(Completely Fair Scheduler)维护红黑树 of runnable tasks,按 vruntime 排序。每个 tick(1ms / 4ms),CFS 检查当前 task 是否该让出 CPU——"时间片用完了吗"。这是"时间累积 + 阈值切换",和游戏 update 循环的 frame budget 检查同构。

### 3.6 · 🦀 Rust 生态视角

Rust 在游戏物理 / 碰撞领域有几个值得关注的 crate 和模式。

**`rapier`** 是 Rust 的 2D / 3D 物理引擎,由 Dimforge 开发。Rapier 提供 rigid body dynamics、collision detection、joints、rays、sensors——是 Bevy 生态的物理默认选项。Rapier 用 ECS 风格 API,内部用 SOA(struct of arrays)布局,cache 友好。

```rust
use rapier3d::prelude::*;

let mut rigid_body_set = RigidBodySet::new();
let mut collider_set = ColliderSet::new();

let rigid_body = RigidBodyBuilder::dynamic().translation(vector![0.0, 5.0, 0.0]).build();
let handle = rigid_body_set.insert(rigid_body);
let collider = ColliderBuilder::ball(0.5).build();
collider_set.insert_with_parent(collider, handle, &mut rigid_body_set);
```

Rapier 内部用 **broad phase(AABB tree)+ narrow phase(GJK / SAT)**,和工业 C++ 引擎(Box2D、Bullet、PhysX)思路一致。Casey HH 手写简化版,Rapier 是 production-grade。

**`parry`** 是 Rapier 抽出来的纯 collision detection 库。parry 提供 shape(ball、cuboid、convex hull、triangle mesh)、ray cast、point projection、pair collision——不涉及 dynamics。如果你只想要碰撞不想物理,parry 是答案。

**`nalgebra`** 是 Rust 的线性代数库,Dimforge 出品(同 Rapier / parry)。`Vector3`、`Matrix4`、`Quaternion`、`Isometry3`——3D 游戏需要的所有数学类型。比 `cgmath` 现代、比 `euclid` 全面。

**Rust ECS 的 collision pattern**。Bevy + Rapier 的典型代码:

```rust
use bevy::prelude::*;
use bevy_rapier3d::prelude::*;

fn setup(mut commands: Commands) {
    commands.spawn((
        RigidBody::Dynamic,
        Collider::ball(1.0),
        Transform::from_translation(Vec3::new(0.0, 5.0, 0.0)),
    ));
}

fn on_collision(
    mut events: EventReader<CollisionEvent>,
    query: Query<&Name>,
) {
    for event in events.read() {
        if let CollisionEvent::Started(e1, e2, _) = event {
            println!("{:?} collided with {:?}", query.get(*e1), query.get(*e2));
        }
    }
}
```

ECS 风格——entity 加 Collider component,系统处理 CollisionEvent。这是 Rust 风格的 collision 设计——数据导向、零成本、类型安全。Casey HH 用 OOP 风格(`struct Player { collider: Collider }`),简单但不够通用。

**Rust 的 zero-cost abstraction** 让 collision detection 可以既高级又快:

```rust
// 高级 API
let hits = colliders.iter()
    .filter(|c| c.aabb_intersects(&target_aabb))
    .filter(|c| c.shape_intersects(&target_shape))
    .collect::<Vec<_>>();

// LLVM 把这优化成手写 C 的等价代码
```

Rust 的 iterator chain + 闭包,被 LLVM inline + vectorize,生成和手写循环一样快的代码。这是 Rust 在性能敏感领域的杀手锏。

**Rust 的 SIMD** 对 collision 特别有用。AABB 测试 8 个浮点比较,用 SIMD 一次处理 4-8 个 AABB。Rust 的 `std::simd`(unstable)或 `wide` crate(stable)提供 SIMD API。Rapier 内部用 SIMD 加速 broad phase。

**Rust 的多线程 collision**。collision detection 是 embarrassingly parallel——N 个 object pair 互相独立。Rust 的 `rayon` 提供 parallel iterator,`colliders.par_iter().filter(...)` 自动并行。Bevy 用 ECS 的 `query.par_iter()` 同样效果。

**Rust 的 `Send + Sync`**。collision data 跨线程共享,要 `Send + Sync`。Rust 编译时检查——如果你的 Collider 包含 `Rc<...>`,编译失败,因为 Rc 不是 Send。这种类型系统保证的多线程安全,是 Rust 比 C++ 优势的地方。

**Rust 的 `smallvec`** 在 collision 里有用。pair list 经常是空的或只有 1-2 个 pair,用 `SmallVec<[Pair; 4]>` 避免堆分配,大部分情况栈上。

**Rust 的 `ahash`** 在 spatial hash 里替代 `DefaultHasher`——3-5 倍速度。Casey HH 的 hash chunk storage(Phase 2)用 `DefaultHasher`,Rust 版应该用 `ahash` 或 `fxhash`。

## 4 · 认知地图

### 4.1 上级

- **BRDF (Bidirectional Reflectance Distribution Function)** — 所有光照模型的数学框架
- **光照模型 (lighting model)** — 描述光如何与表面交互的数学模型
- **光照管线 (lighting pipeline)** — 一帧内光照计算的完整流程

### 4.2 同级

| 模型 | 漫反射 | 镜面反射 | 物理 |
|---|---|---|---|
| Lambert | ✅ N·L | ❌ | 部分(只漫反射) |
| Phong | ✅ N·L | ✅ (R·V)^n | 不严格 |
| Blinn-Phong | ✅ N·L | ✅ (N·H)^n | 不严格,更快 |
| Cook-Torrance | 微表面 | 微表面 | ✅ PBR |

### 4.3 下级

- **法线 (normal)** — 表面朝向,决定光照分布
- **半角向量 (half vector)** — Blinn-Phong 优化
- **环境光 (ambient)** — 间接光的常数近似
- **shininess** — 高光锐度
- **light direction / view direction** — 光照输入向量

## 5 · 对照与变奏

### Lambert 余弦定律的物理推导

考虑光从 L 方向来,照射表面。光的能量流过某面积 A。如果表面法线和 L 同向,光「正好打在」A 上——光强最大。如果表面斜着,L 方向上的光散到更大面积 `A / cos θ`(投影原理)——单位面积光强变弱 `cos θ`。

这就是 Lambert 余弦定律——简单几何,深刻结果。

### 2D 光照 vs 3D 光照

2D 游戏通常**不用真实光照**——sprite 是预渲染的(美术已经画好阴影)。Casey HH 是 2.5D,所以他需要光照。2D 游戏「fake 3D 光照」的方法:
- 全屏 normal map(描述 sprite 表面朝向)
- 在 sprite 上算光照
- 这就是 Day 097 法线贴图的思想

### CPU vs GPU 光照

CPU 光照慢——单核几千万光照计算/秒,1920×1080 屏幕约 2M 像素,每像素一次光照 = 几十帧/秒极限。

GPU 光照是 fragment shader 的一部分,几千核心并行,每秒几十亿光照计算,完全免费(对现代 GPU 是小菜一碟)。

## 6 · 关联 Day

- **铺垫**:
  - [day091](day091.md) — 矩阵(法线变换需要)
  - [day094](day094.md) — sRGB(光照必须 linear)
  - [day095](day095.md) — 预乘 alpha(混合必须 linear)
  - [phase-2/day041](../phase-2/day041.md) — 数学基础(点积)
- **当天**:day096(本日,Phong 光照)
- **后续**:
  - [day097](day097.md) — 法线贴图(每像素法线)
  - [day099](day099.md) — 环境贴图(反射)
  - [day100](day100.md) — 反射向量
  - [day200+](../phase-6/) — PBR(物理光照)

## 7 · 变式训练

### Lv1 · 概念辨析

**题**:为什么 Blinn-Phong 比 Phong 快?给出 2 个理由。

**参考解答**:
1. **少算反射向量**。Phong 要 `R = reflect(-L, N)`,涉及一次点积和两次向量乘加。Blinn-Phong 直接 `H = (L + V).normalized()`,只需一次加法 + 一次归一化。
2. **`N·H` 比 `R·V` 更稳定**。当 V 接近表面平行时,`R·V` 变化剧烈;`N·H` 平滑过渡。所以 Blinn-Phong 用更低的 shininess 就能达到同样效果,实际工作量更少。

### Lv2 · 动手实践

**题**:实现完整 Phong 光照,在 sprite 上应用。给一个法线贴图(全 (0,0,1),即朝相机),光源从右上 45° 角照。sprite 是纯红 (1, 0, 0)。预期:sprite 的右上部分变亮,左下部分变暗。

完成标准:画面上看到 sprite 有明显的从右上到左下的亮度渐变。

**参考解答**:见 §2 Rust 实现。把 `phong_lighting` 应用到 sprite 每个像素。

### Lv3 · 迁移设计

**题**:你的游戏是 2D 俯视角,玩家在地图上走。你想做一个**手电筒**——玩家朝向方向的锥形光。算法是什么?

提示:
- 手电筒是「有方向的光源」(directional light 的一种)
- 锥形光:除了 Lambert,还要算「光的方向和手电筒方向的夹角」
- 夹角小于某阈值(比如 30°)才有光,大于的全黑
- 平滑边缘:`smoothstep` 函数让边缘渐变

### Lv4 · 开源贡献

**题**:`rend3` 是 Rust 的 PBR 渲染器,GitHub: https://github.com/BVE-Reborn/rend3

```bash
gh repo clone BVE-Reborn/rend3
cd rend3
grep -rn "specular" rend3/src/ | head
```

读它的 PBR shader,对比 Phong 的差别。

**可能的贡献方向**:
1. **doc**:某个 BRDF 函数没有解释「物理意义」,补一段
2. **test**:边界 case(法线为 0、光在背面、shininess=0)没测试,补一个
3. **example**:加 Blinn-Phong vs PBR 对比示例

**PR 示例**:
```
标题:example: add Blinn-Phong vs PBR comparison
文件:examples/lighting_comparison.rs
动机:新用户经常问「为什么 PBR 比 Blinn-Phong 好」,加一个可视化对比。
改动:加 example,并排渲染两个球,一个 Blinn-Phong 一个 PBR。
验证:cargo run --example lighting_comparison,确认两个球正常渲染。
```

## 8 · Rust / Arch 落地代码

### 完整 Phong 光照演示

```rust
// [Vec3 / Color / Light / reflect / phong_lighting 同 §2]

fn render_sphere_with_lighting(width: usize, height: usize) -> Vec<u8> {
    let mut bytes = vec![0u8; width * height * 4];
    let cx = width as f32 / 2.0;
    let cy = height as f32 / 2.0;
    let radius = width as f32 * 0.3;

    let light = Light {
        direction: Vec3::new(0.7, -0.3, 0.7).normalized(),
        color: Color { r: 1.0, g: 1.0, b: 1.0 },
        intensity: 1.0,
    };
    let ambient = Color { r: 0.1, g: 0.1, b: 0.1 };
    let view = Vec3::new(0.0, 0.0, 1.0);  // 视线朝相机
    let surface_color = Color { r: 0.7, g: 0.2, b: 0.2 };  // 暗红

    for y in 0..height {
        for x in 0..width {
            let dx = x as f32 - cx;
            let dy = y as f32 - cy;
            let r2 = dx*dx + dy*dy;
            if r2 < radius * radius {
                // 球面投影:Z = sqrt(r^2 - x^2 - y^2)
                let z = (radius * radius - r2).sqrt();
                let normal = Vec3::new(dx, dy, z).scale(1.0 / radius);  // 法线 = (dx, dy, dz) / r
                let lit = phong_lighting(surface_color, normal, view, light, ambient, 50.0);
                // Clamp + 转 sRGB
                let s = |c: f32| -> u8 {
                    let l = if c <= 0.0031308 { c * 12.92 } else { 1.055 * c.powf(1.0/2.4) - 0.055 };
                    (l * 255.0).round().clamp(0.0, 255.0) as u8
                };
                let idx = (y * width + x) * 4;
                bytes[idx]     = s(lit.r);
                bytes[idx + 1] = s(lit.g);
                bytes[idx + 2] = s(lit.b);
                bytes[idx + 3] = 255;
            }
        }
    }
    bytes
}

fn main() {
    let bytes = render_sphere_with_lighting(400, 400);
    // 写出 PPM(无依赖)
    // 或用 image crate 写 PNG
    println!("rendered sphere");
}
```

### Arch Linux 工具

```bash
# 1. 性能分析
perf record -g ./target/release/lighting_demo
perf report
# 热点应该在 phong_lighting

# 2. SIMD 是否激活
cargo rustc --release -- --emit asm -- --target-cpu=native
# 编译时启用所有 SIMD 指令

# 3. 多线程版本对比
cat >> Cargo.toml << 'EOF'
[dependencies]
rayon = "1.5"
EOF
# 用 rayon 把 y 循环并行化,性能 ×N(N 是核数)
```

### Troubleshooting

- **球看起来扁** — 法线计算错。检查 `dx, dy, z` 是否正确归一化
- **没有高光** — `view_dir` 必须指向相机,不是表面到光源
- **暗部全黑** — ambient 太小或为 0
- **色彩失真** — 没 linear 空间计算,或 sRGB 转换错

## 9 · 延伸阅读(可选补充,非必需)

本仓库本地:
- [day091.md](day091.md) — 矩阵
- [day094.md](day094.md) — sRGB
- [day095.md](day095.md) — 预乘 alpha
- [day097.md](day097.md) — 法线贴图(明天)

外部稳定 URL(可选):
- LearnOpenGL Basic Lighting:https://learnopengl.com/Lighting/Basic-Lighting
- 3D Math Primer ch.10 Lighting:https://gamemath.com/book/lighting.html
- Scratchapixel Shading:https://www.scratchapixel.com/lessons/3d-basic-rendering/introduction-to-shading/diffuse-lambertian-shading

真实开源源码:
- `glam` Vec3 reflect:https://github.com/bitshifter/glam/blob/main/src/f32/vec3.rs
- HH 原版 day096 C 代码:https://github.com/HandmadeHero/handmade-hero/tree/main/code/day096
