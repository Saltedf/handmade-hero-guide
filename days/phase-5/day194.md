---
day: 194
phase: 5
title_en: "Organizing Debug Variables into Hierarchy"
title_zh: "调试变量组织为层级"
type: debug
difficulty: 2
duration: "1h"
hh_url: "https://guide.handmadehero.org/code/day194/"
domains: [game, debug, rust]
prereqs: ["day193"]
---

# Day 194 · 调试变量组织为层级

> profiler 后端能记 cycle,但游戏有大量"debug 变量"(玩家速度、重力、AI 视野角),你想在 UI 里调整它们。今天 Casey 把这些变量组织成树形层级(玩家 → 移动 → 速度),便于浏览。

## 0 · 为什么要有这一天

游戏开发一段时间后,会有大量可调参数:

- 玩家相关:移动速度、跳跃力、攻击范围、HP
- AI 相关:视野角、追击距离、攻击间隔
- 物理相关:重力、摩擦、弹性
- 渲染相关:光源颜色、阴影强度、相机 FOV
- 音频相关:主音量、音效音量、BGM 音量

几百个变量,平铺在一个 UI 里看不过来。**层级组织**:`Player/Movement/Speed`、`AI/Vision/Fov`,树状浏览。

今天 Casey 设计层级:

1. 每个 debug 变量有"完整路径"(像文件路径:`Player/Movement/Speed`)
2. 自动从路径构建树
3. UI 渲染成 tree view(可折叠)

心理锚点:看完今天,你能实现"从 `A/B/C` 字符串构建树"的算法。

## 1 · Casey 今天做了什么

Casey 设计层级:

1. debug 变量 GUID 用 hash("Player/Movement/Speed") 生成
2. 从 GUID 反推路径(用反向 map)
3. 自动构建树(类似 radix tree / prefix tree)
4. UI 渲染 expandable tree view

## 2 · 心智模型

### 类比:操作系统的文件系统

文件系统是经典的层级组织:`/home/user/docs/file.txt`。每段是目录,最后是文件。debug 变量一样:`Player/Movement/Speed` 是"目录 Player → 子目录 Movement → 变量 Speed"。

文件系统用 **inode + dentry** 实现:每级目录有 inode(元数据),dentry 链接父目录和子文件。debug 变量树可以借鉴——用 HashMap<String, Node> 表示目录,Node 是 leaf 变量或子目录。

### 2.0 Trie / Radix tree 的核心思想

把多个字符串**共享前缀**地存:

```
"Player/Movement/Speed"  ┐
"Player/Movement/Jump"   ├─ 共享前缀 "Player/Movement/"
"Player/Combat/HP"       ┘
```

树形:

```
root
└── Player
    ├── Movement
    │   ├── Speed (leaf)
    │   └── Jump (leaf)
    └── Combat
        └── HP (leaf)
```

每个内部节点 = 共享前缀,叶子 = 完整路径。**空间效率**:N 个长度 L 的字符串,共享前缀让总节点数 < N×L。

应用:
- **DNS 服务器**:域名树
- **路由表**:URL 路径
- **auto-complete**:单词前缀
- **debug 变量**:Casey 这里

### 2.0.1 构建 Trie 的算法

```rust
#[derive(Default)]
struct TrieNode {
    children: HashMap<String, TrieNode>,
    value: Option<f32>,  // leaf 才有
}

impl TrieNode {
    fn insert(&mut self, path: &str, value: f32) {
        let mut node = self;
        for part in path.split('/') {
            node = node.children.entry(part.to_string()).or_default();
        }
        node.value = Some(value);
    }
}
```

每个变量调用 `insert("Player/Movement/Speed", 5.0)`。N 个变量 = N 次插入,每次 O(path 长度)。

### 2.0.2 tree view 的渲染

immediate-mode tree view:

```rust
fn draw_tree(ui: &mut Ui, node: &TrieNode, path: &str, expanded: &mut HashSet<String>) {
    for (name, child) in &node.children {
        let full_path = format!("{}/{}", path, name);
        let is_leaf = child.value.is_some();

        if is_leaf {
            if let Some(v) = &child.value {
                ui.slider(name, v);  // 调整值
            }
        } else {
            let is_expanded = expanded.contains(&full_path);
            if ui.tree_node(name, is_expanded) {
                if !is_expanded { expanded.insert(full_path.clone()); }
                draw_tree(ui, child, &full_path, expanded);  // 递归
            } else {
                expanded.remove(&full_path);
            }
        }
    }
}
```

每帧重画,expanded 状态用 ID(路径字符串)持久化。这就是 immediate-mode 的精髓——UI 结构跟着数据走,改了变量自动反映在树上。

### 2.1 反射式变量注册

Casey 用 macro 自动注册变量:

```rust
#[derive(Debug)]
struct PlayerVars {
    speed: f32,
    jump: f32,
    hp: i32,
}

// macro 自动生成
impl PlayerVars {
    fn debug_vars(&self) -> Vec<(String, DebugValue)> {
        vec![
            ("Player/Speed".to_string(), DebugValue::F32(self.speed)),
            ("Player/Jump".to_string(), DebugValue::F32(self.jump)),
            ("Player/HP".to_string(), DebugValue::I32(self.hp)),
        ]
    }
}
```

macro 自动生成 `debug_vars`,UI 自动遍历。这就是**反射**(reflection)的简化版——程序运行时知道自己的字段。

## 3 · 四域深入

`/usr/share/doc/rust/html/std/index.html` 是路径,自动分目录。文件系统的树是天然层级。

debug 变量层级一样:每个"路径段"是一个文件夹,变量是叶子。

### 2.1 路径设计

```rust
const SPEED_GUID: u64 = hash("Player/Movement/Speed");
const FOV_GUID: u64 = hash("AI/Vision/Fov");
```

用斜杠分隔的路径,hash 后得到 u64(同时保留原始路径字符串用于 UI)。

### 2.2 自动构建树

```rust
#[derive(Default)]
pub struct DebugNode {
    pub name: String,
    pub children: HashMap<String, DebugNode>,
    pub variables: Vec<(u64, DebugValue)>,
}

impl DebugNode {
    pub fn insert(&mut self, path: &str, guid: u64, value: DebugValue) {
        let parts: Vec<&str> = path.split('/').collect();
        let mut current = self;
        for part in &parts[..parts.len() - 1] {
            current = current.children.entry(part.to_string()).or_default();
        }
        let last = parts.last().unwrap();
        // 最后一段是变量名
        current.variables.push((guid, value));
        // 但为了 UI 显示变量名,要存 last
        // 简化:把 name 存到变量里
    }
}
```

### 2.3 tree 渲染

```rust
fn draw_node(ui: &mut Ui, node: &DebugNode, depth: usize) {
    let indent = "  ".repeat(depth);
    for (name, child) in &node.children {
        let open = ui.tree_header(&format!("{}📁 {}", indent, name));
        if open {
            draw_node(ui, child, depth + 1);
        }
    }
    for (guid, value) in &node.variables {
        ui.draw_debug_value(&indent, guid, value);
    }
}
```

(immediate-mode 假设 API)

### 2.4 tree state

immediate-mode 的 tree 需要"哪些节点 expand"的状态:

```rust
pub struct TreeState {
    expanded: HashSet<String>,  // path
}

impl TreeState {
    pub fn is_expanded(&self, path: &str) -> bool {
        self.expanded.contains(path)
    }
    pub fn toggle(&mut self, path: &str) {
        if self.expanded.contains(path) {
            self.expanded.remove(path);
        } else {
            self.expanded.insert(path.to_string());
        }
    }
}
```

每帧 render 时,如果节点 expand,递归渲染子节点。

## 3 · 四域深入

### 3.1 · 游戏编程视角

游戏的 inspector / property tree:
- Unreal:Details Panel,层级 + 类型自适应 editor
- Unity:Inspector,层级 + 类型
- Casey:手写 tree,简单但够用

### 3.2 · Rust 生态视角

Rust 的树表达:
- `Box<Node>` — 简单,但 ownership 严格
- `Rc<RefCell<Node>>` — 灵活,但复杂
- `id-based`(arena)— Bevy ECS 用这个,避免循环引用

```rust
// arena 模式
pub struct Arena<T> {
    nodes: Vec<T>,
}
pub type NodeId = usize;  // index into arena
```

casey 用 `HashMap<String, Node>` 简化,适合 UI 树(不深)。

### 3.3 · Linux 系统编程视角

Linux 的 procfs(`/proc`)就是树:

```
/proc/
├── cpuinfo
├── meminfo
├── self/
│   ├── status
│   ├── maps
│   └── fd/
│       ├── 0
│       └── 1
└── [pid]/
```

debug 变量树和这结构一样。

## 4 · 认知地图

### 4.1 上级

- **Tree data structure** — 树
- **Hierarchical naming** — DNS / 文件路径 / URL
- **Property editor** — GUI 模式

### 4.2 同级

| 组织方式 | 用途 |
|---|---|
| Flat list | 小数据集 |
| Tree | 层级 |
| Tag | 多维分类 |
| Search | 海量数据 |

### 4.3 下级

- `split('/')` — 路径分段
- `HashMap<String, Node>` — 子节点
- `HashSet<String>` — expand state
- `tree_state` — 状态保留

## 5 · 对照与变奏

**不同 UI 的层级组织**:

| UI 类型 | 层级表示 |
|---|---|
| 文件浏览器 | 路径字符串 `/a/b/c` |
| Inspector(Unity) | 反射字段名 `Player.speed` |
| Registry(Windows) | 树状 key `HKEY_CURRENT_USER\Software\...` |
| JSON | 嵌套对象 `{"a": {"b": 1}}` |
| ImGui | stack-based ID `PushID("Player"); PushID("Movement");` |

所有这些都是**树形结构的不同表示**。

### Unreal 的 Reflection 系统

Unreal 用 UHT(Unreal Header Tool)预处理头文件,生成 reflection 代码:

```cpp
// .h
UCLASS()
class APlayer : public AActor {
    GENERATED_BODY()
public:
    UPROPERTY(EditAnywhere, Category = "Movement")
    float Speed = 5.0f;
};
```

UHT 看到 `UPROPERTY`,生成 `Player::StaticClass()`、`PropertyIterator` 等。Editor 用这些 API 列字段、序列化、修改。

Rust 没有 UHT 等价物。`#[derive(Debug)]` 是简版,但没有"枚举所有字段"的 API。Casey 用 macro 自己生成,工程量大但灵活。

### Bevy Reflect crate

`bevy_reflect` 提供 Rust 的运行时反射:

```rust
use bevy_reflect::Reflect;

#[derive(Reflect)]
struct Player { speed: f32, hp: i32 }

let p = Player { speed: 5.0, hp: 100 };
// 运行时遍历字段
for (name, value) in p.iter_fields() {
    println!("{}: {:?}", name, value);
}
```

`#[derive(Reflect)]` 自动生成 `Reflect` trait 实现。但**有 overhead**(vtable 调用),hot loop 不用。

### Inspector / Toolbar / Property Tree

游戏 editor 的标准 UI 组件:
- **Inspector**:选中对象,显示所有字段(Unity / Unreal / Godot)
- **Toolbar**:顶栏按钮
- **Property Tree**:左侧树(Outliner)
- **Viewport**:中间 3D 视图

Casey 的 debug UI 是 inspector 的简化版——选中"调试分类",看里面所有变量。

## 6 · 关联 Day

**层级 UI 的不同实现**:

| 库 | 模式 |
|---|---|
| Dear ImGui | immediate,recursive |
| egui | immediate |
| Qt | retained,QTreeView |
| Cocoa | retained,NSOutlineView |

immediate-mode 树渲染是递归调用,简洁。

## 6 · 关联 Day

- **铺垫**:[day193](day193.md)(编译时变量)
- **当天**:day194(本日)
- **后续**:[day195](day195.md)(交互式树视图)

## 7 · 变式训练

### Lv1 · 概念辨析

**题**:为什么用路径字符串("Player/Movement/Speed")而不是 struct(`Player.Movement.Speed`)?字符串有什么好处?

**参考解答**:
- 字符串可以 hash 成 GUID(编译时 / 运行时一致)
- 字符串可以 split 成段,自动构建树
- 字符串容易序列化(JSON / toml)
- 字符串可拼接(`format!("{}/{}", parent, child)`)
- 缺点:运行时分配,弱类型

struct 的优缺点反过来:强类型,但加新节点要改代码。

### Lv2 · 动手实践

**题**:写一个 DebugNode 树,插入几个路径,然后 print 树。

**参考解答**:

```rust
use std::collections::HashMap;

#[derive(Default, Debug)]
struct DebugNode {
    name: String,
    children: HashMap<String, DebugNode>,
    variables: Vec<String>,
}

impl DebugNode {
    fn insert(&mut self, path: &str) {
        let parts: Vec<&str> = path.split('/').collect();
        let mut current = self;
        for part in &parts[..parts.len() - 1] {
            current = current.children.entry(part.to_string()).or_insert_with(|| DebugNode {
                name: part.to_string(),
                ..Default::default()
            });
        }
        if let Some(leaf) = parts.last() {
            current.variables.push(leaf.to_string());
        }
    }

    fn print(&self, depth: usize) {
        let indent = "  ".repeat(depth);
        for (_, child) in &self.children {
            println!("{}{}/", indent, child.name);
            child.print(depth + 1);
        }
        for var in &self.variables {
            println!("{}{}", indent, var);
        }
    }
}

fn main() {
    let mut root = DebugNode::default();
    root.insert("Player/Movement/Speed");
    root.insert("Player/Movement/Jump");
    root.insert("Player/Combat/HP");
    root.insert("AI/Vision/Fov");
    root.insert("AI/Vision/Range");

    root.print(0);
}
```

**预期输出**:

```
Player/
  Movement/
    Speed
    Jump
  Combat/
    HP
AI/
  Vision/
    Fov
    Range
```

### Lv3 · 迁移设计

**题**:给每个变量加类型(int / float / bool / string),tree 里 print 时显示类型。怎么设计 `DebugValue` enum?**提示**:`#[derive(Clone, Debug)] enum DebugValue { Int(i64), Float(f32), ... }`。

### Lv4 · 开源贡献

**题**:`egui::collapsing_header` 是树视图组件。GitHub: https://github.com/emilk/egui

读 collapsing_header.rs。

## 8 · Rust / Arch 落地代码

完整见 Lv2。

```bash
cargo new --bin debug-tree
cd debug-tree
cargo run
```

**Arch Linux**:

```bash
# 看真实 tree UI:用 htop
htop
# 按 F5 进入 tree 模式,看进程树

# 或用 pstree
pstree -p
```

**Troubleshooting**:

- name 是空 — `or_insert_with` 里要设 name
- 树排序乱 — HashMap 无序,改成 BTreeMap

## 9 · 延伸阅读(可选补充,非必需)

本仓库:
- [day193](day193.md) — 编译时变量

外部稳定 URL:
- Trie / prefix tree — https://en.wikipedia.org/wiki/Trie
- egui — https://github.com/emilk/egui

真实源码:
- Casey HH day194 — https://github.com/HandmadeHero/handmade-hero/tree/main/code/day194
