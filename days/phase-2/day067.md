---
day: 067
phase: 2
title_en: "Conditional Update: Per-State Behavior"
title_zh: "条件更新:按状态决定行为"
type: coding
difficulty: 2
duration: "1h"
hh_url: "https://guide.handmadehero.org/code/day067/"
domains: [game, rust]
prereqs: ["day060", "day066"]
---

# Day 067 · 条件更新:按状态决定行为

> 一个 entity 有三种状态:活着、垂死、死亡。今天 Casey 让 update 函数根据状态决定"要不要 update AI / 物理 / 动画"。听起来一行 if,但它能省下 80% 的 CPU——因为大部分 entity 在大部分时间是死的或静止的。

## 0 · 为什么要有这一天

朴素 update:

```rust
fn update(e: &mut Entity, dt: f32) {
    e.update_ai(dt);       // AI 思考(50 µs)
    e.update_physics(dt);  // 物理积分(10 µs)
    e.update_animation(dt); // 动画帧(5 µs)
}
```

每帧每个 entity 花费 ~65 µs。1000 个 entity = 65 ms/帧 = 15 FPS。

但仔细想:
- **死的 entity**(state=Dead)等被清理,什么都不应该做
- **垂死的 entity**(state=Dying)在播倒地动画,不该 AI 思考(已死)也不该物理移动(已倒)
- **静止的 entity**(墙)永远不动,AI / 物理都无意义
- **暂停的 entity**(被冰冻 / 眩晕)AI 暂停,但渲染继续

一个 entity 在大部分时间处于这些"不需要完整 update"的状态。**统一 update 浪费了 80% CPU**。

**条件 update**:

```rust
fn update(e: &mut Entity, dt: f32) {
    match e.state {
        EntityState::Alive => {
            e.update_ai(dt);
            e.update_physics(dt);
            e.update_animation(dt);
        }
        EntityState::Dying { timer } => {
            // 只 update 动画(倒地 / 渐隐)
            e.update_animation(dt);
        }
        EntityState::Dead => {
            // 什么都不做
        }
    }
}
```

垂死 / 死亡的 entity 跳过 90% 工作。1000 个 entity 中假设 800 个是 dead,只 200 个完整 update = 13 ms/帧 = 77 FPS。

这就是 **conditional update**——根据状态决定 update 内容。今天 Casey 加这个分支。

读完今天,你要做到:
- 看到任何 update 函数都问"是不是所有状态都需要这个工作"
- 能用 Rust 的 match 实现条件 update,编译器验证完整性
- 理解"90% 工作"和"100% 工作"对 FPS 的指数级影响
- 知道这种优化叫"early exit"——CPU 分支预测友好

如果你卡在"为什么不直接给 Dead entity 删了",回去看 Day 060(两阶段删除)——Dead 状态的 entity 还在等待 cleanup,不能立即删。

## 1 · Casey 今天做了什么

Casey 第 67 集做的事:

1. 把 update 函数改成 match self.state
2. Alive:全 update(AI / 物理 / 动画)
3. Dying:只 update 动画(dying_timer 减,timer 到 0 转 Dead)
4. Dead:不 update(等 retain 清理)
5. 加更多状态(Phase 5+ 会加 Stunned / Frozen / Paused)

视频里 Casey 用了 Casey 喜欢的"早退(early return)"风格:

```c
// Casey C 风格
void update(Entity *e, f32 dt) {
    if (e->state == DEAD) return;
    if (e->state != ALIVE) goto update_animation_only;
    
    update_ai(e, dt);
    update_physics(e, dt);
    
update_animation_only:
    update_animation(e, dt);
}
```

C 风格用 goto。Rust 用 match,更安全。

## 2 · 心智模型

### 2.1 类比:医院的不同病区

医院根据病人状态分病区,每个病区做不同事:

- **急诊病区(Alive)**:全面治疗——检查 / 用药 / 手术 / 监测
- **ICU 病区(Dying)**:只监测生命体征,不做主动治疗
- **太平间(Dead)**:什么都不做,等家属认领

为什么不所有病人都进急诊?因为急诊医生时间宝贵,不能花在"已经死亡"的病人身上。**按状态分病区 = 按状态分 update 强度**。

### 2.2 严谨:状态驱动的 update

```rust
#[derive(Clone, Copy, PartialEq, Debug)]
enum EntityState {
    Alive,
    Dying { timer: f32 },
    Dead,
    Stunned { timer: f32 },    // Phase 5+ 加
    Frozen { timer: f32 },
}

impl Entity {
    fn update(&mut self, world: &World, dt: f32) {
        match self.state {
            EntityState::Dead => {
                // 完全跳过,等 retain 清理
                return;
            }
            EntityState::Dying { ref mut timer } => {
                // 只 update 动画
                self.update_animation(dt);
                *timer -= dt;
                if *timer <= 0.0 {
                    self.state = EntityState::Dead;
                }
                return;
            }
            EntityState::Stunned { ref mut timer } => {
                // 不 update AI(被眩晕),但 update 物理 / 动画
                self.update_physics(dt);
                self.update_animation(dt);
                *timer -= dt;
                if *timer <= 0.0 {
                    self.state = EntityState::Alive;
                }
                return;
            }
            EntityState::Frozen { ref mut timer } => {
                // 完全冻结,只 timer 减
                *timer -= dt;
                if *timer <= 0.0 {
                    self.state = EntityState::Alive;
                }
                return;
            }
            EntityState::Alive => {
                // 完整 update
                self.update_ai(world, dt);
                self.update_physics(dt);
                self.update_animation(dt);
            }
        }
    }
}
```

**Rust match 的强大**:
- 编译器强制覆盖所有 variant(漏一个就编译错)
- variant 带数据(timer),destructure 直接用
- `ref mut timer` 借用可变引用,不 move 出 enum

### 2.3 早退 vs 完整 match

两种风格:

**早退(early return)**:

```rust
fn update(&mut self, dt: f32) {
    if self.state == EntityState::Dead { return; }
    if let EntityState::Dying { ref mut timer } = self.state {
        self.update_animation(dt);
        *timer -= dt;
        if *timer <= 0.0 { self.state = EntityState::Dead; }
        return;
    }
    // 默认:Alive,完整 update
    self.update_ai(dt);
    self.update_physics(dt);
    self.update_animation(dt);
}
```

**完整 match**:

```rust
fn update(&mut self, dt: f32) {
    match self.state {
        EntityState::Dead => {},
        EntityState::Dying { .. } => { /* ... */ },
        EntityState::Alive => { /* ... */ },
    }
}
```

**Casey 偏爱早退**(C 风格)。Rust 项目偏爱 match(编译器强制完整)。

### 2.4 性能:80/20 法则

条件 update 利用了 **80/20 法则**(Pareto principle):

- 80% 的 entity 是 Dead / 静止的(墙 / 已死怪 / 静态装饰)
- 20% 的 entity 是活跃的(玩家 / 活怪 / 投射物)
- 把 80% 的 entity 跳过,只对 20% 做完整 update

收益:
- 1000 个 entity,800 个 Dead
- 朴素 update:1000 × 65 µs = 65 ms
- 条件 update:200 × 65 µs + 800 × 1 µs(几乎跳过)= 13.8 ms
- **4.7× 加速**

更大的好处:**分支预测友好**。`if e.state == Dead { return }` 在 CPU 分支预测器看来是"几乎总是 true"(因为 80% 是 Dead),预测正确率 99%。**指令流水线无 stall**。

### 2.5 加新状态

条件 update 的另一个好处:**加新状态容易**。

举例:加 `Frozen` 状态(被冰冻法术命中,3 秒不动):

```rust
// 加 state
enum EntityState { Alive, Dying { timer: f32 }, Dead, Frozen { timer: f32 } }

// update 里加一个 arm
EntityState::Frozen { ref mut timer } => {
    *timer -= dt;
    if *timer <= 0.0 { self.state = EntityState::Alive; }
    // 不 update AI / 物理 / 动画
}

// 法术命中时设置
fn freeze(e: &mut Entity) {
    if e.state == EntityState::Alive {
        e.state = EntityState::Frozen { timer: 3.0 };
    }
}
```

如果不用状态机,而是 `if e.is_alive && !e.is_dying && !e.is_dead && !e.is_frozen && ...`,逻辑会指数爆炸。状态机干净。

### 2.6 AI 内部也有状态机

EntityState 是 entity 的"宏观状态"(活 / 死)。但活的怪内部也有"微观状态"——巡逻 / 追击 / 攻击 / 逃跑:

```rust
enum MonsterAiState {
    Patrol { waypoint_idx: usize },
    Chase { target_id: EntityId },
    Attack { target_id: EntityId, cooldown: f32 },
    Flee { from_id: EntityId },
}

struct Monster {
    state: EntityState,           // 宏观
    ai_state: MonsterAiState,     // 微观
}

impl Monster {
    fn update_ai(&mut self, world: &World, dt: f32) {
        if self.state != EntityState::Alive { return; }
        match self.ai_state {
            MonsterAiState::Patrol { waypoint_idx } => { /* ... */ },
            MonsterAiState::Chase { target_id } => { /* ... */ },
            // ...
        }
    }
}
```

这是 **嵌套状态机**。Casey 第 67 集只做宏观(EntityState),微观(Phase 4+ 的 brain 系统)后面加。

## 3 · 四域深入

### 3.1 · 🎮 游戏编程视角

游戏的状态机无处不在:

- **EntityState**(今天):活 / 死 / 状态
- **AnimationState**:idle / walk / run / attack / die
- **CombatState**:idle / windup / active / recovery / hit
- **GameState**:menu / loading / playing / paused / cutscene / game_over
- **AIState**:patrol / chase / attack / flee / dead

每个状态机用 match 实现,加状态容易。这是**有限状态机(FSM)**的最简形态。

更复杂的状态机工具:
- **Hierarchical FSM**(分层 FSM):状态可以嵌套(攻击下面有 windup / active / recovery)
- **Behavior Tree**(行为树):分支 / 序列 / 并行节点,比 FSM 灵活
- **GOAP / Planner**:定义目标和动作,AI 自动规划路径

Casey 用最简 FSM,Phase 8 才可能升级。

### 3.2 · 🐧 Linux 系统编程视角

Linux 进程状态机:

- **Running**:在 CPU 上跑
- **Sleeping**:等事件(interruptible / uninterruptible)
- **Stopped**:被信号暂停(SIGSTOP)
- **Zombie**:已 exit,等 parent wait
- **Dead**:被 reap,从进程表移除

`top` 命令的 S 列显示这些状态。kernel scheduler 根据**状态决定 update**——只调度 Running / Sleeping,不调度 Zombie / Dead。**这就是条件 update**。

```
top - 15:30:00 up 1 day
  PID USER      PR  NI    VIRT    RES    SHR S  %CPU %MEM
1234 user       20   0  100000   5000   3000 R   5.0  0.1   <- Running
1235 user       20   0  100000   5000   3000 S   0.0  0.1   <- Sleeping
1236 user       20   0       0      0      0 Z   0.0  0.0   <- Zombie
```

Zombie 进程在进程表里但不调度。和 Casey 的 Dead entity 在列表里但不 update 完全一致。

### 3.3 · 🎨 图形学视角

渲染管线里也有"条件 update":

- **视锥外物体**:跳过 vertex shader(剔除)
- **背面**:背向相机的三角形跳过光栅化(backface culling)
- **深度更远**:被前面物体挡的 fragment 跳过(z-test)
- **alpha = 0**:透明 fragment 跳过(blend skip)

GPU 大量用"早退"——大部分 fragment / vertex 被跳过,只对真正需要计算的做完整 pipeline。这就是 GPU 性能的来源。

CPU 端的 conditional update 是同样思想:**大部分 entity 不需要完整 update,跳过它们**。

### 3.4 · 🦀 Rust 生态视角

Rust 的 match 让条件 update 极其优雅:

```rust
match self.state {
    EntityState::Alive => { /* full update */ }
    EntityState::Dying { timer } if timer > 0.5 => { /* early dying */ }
    EntityState::Dying { timer } => { /* late dying */ }
    EntityState::Dead => {}
}
```

**match guard**(`if timer > 0.5`):在 pattern 后加 if 条件,进一步细分。Casey 在 C 里写 `if (state == DYING && timer > 0.5)`,Rust 写 `Dying { timer } if timer > 0.5`,更紧凑。

**编译时完整性检查**:加新 variant,所有 match 都要更新。这是重构的护栏——不会漏一个分支。

#### 数据导向设计的角度

条件 update 的另一面:**数据布局应该匹配访问模式**。

如果 80% 是 Dead,把 Dead entity 分到单独 Vec:

```rust
struct World {
    alive_entities: Vec<Entity>,    // 完整 update
    dead_entities: Vec<EntityId>,   // 等清理
}
```

每帧 update 只扫 alive_entities(200 个),dead_entities 只在 retain 时一次扫。**比"所有 entity 在一个 Vec + 条件 update"还快**。

这是 **SoA(struct of arrays)** 的思想——按访问模式分组。Phase 4+ 会深入。


### 3.5 · 🐧 Linux 系统编程视角(内核空间 / 硬件加速)

Linux 系统里"碰撞 / 相交检测"的核心思想在多处出现,理解这些能让你看 Casey 的 2D / 3D collision 算法在更广的语境里。

**内核的 page fault 处理**。虚拟内存到物理内存的映射,本质是"地址空间相交检测"。CPU 访问虚拟地址,MMU 查 page table,如果地址不在已映射范围内 → page fault。内核接 fault,检查(1)地址合法吗(进程的 VMA 区间)、(2)权限对吗(read / write / exec)、(3)page 在物理内存还是 swap。这套检查就是"区间相交 + 属性匹配",和 Casey 在 day 050 的 Minkowski collision 在抽象上一致——"用几何空间判断两个东西是否接触"。

```bash
# 看你进程的 VMA(虚拟内存区域)
cat /proc/$$/maps
# 输出形如:
# 56001234-56005678 r-xp ...  /usr/bin/bash
# 7f1234560000-7f1234780000 r--p ... /usr/lib/libc.so.6

# 触发 page fault(故意访问非法地址)
python3 -c "import ctypes; ctypes.cast(0xdeadbeef, ctypes.POINTER(ctypes.c_int))[0] = 0"
# 输出:Segmentation fault (core dumped)
```

**epoll / poll / select** 是 IO 多路复用,本质是"时间区间相交"。你给内核 N 个 fd,内核检查"哪个 fd 当前可读 / 可写"。这是 N 路并行等待,内核用红黑树 + ready list 高效管理。和 collision detection 的 broad phase 类似——快速排除不可能相交的对,只在 narrow phase 详细检查。

**文件锁(flock / fcntl)**。Linux 文件锁检测"两个进程是否同时持有冲突锁"——这是"锁区间相交"。flock 用 inode-level 锁,fcntl 用 byte-range 锁(`fcntl(F_SETLK, F_WRLCK, {100..200})` 锁字节 100-200)。内核维护锁列表,新锁请求时遍历检查冲突——这就是 interval intersection 检测。

**网络栈的 TCP / IP header 校验**。每个 TCP/IP 包带 checksum,内核收到包校验——本质是"数据完整性检测"。和 collision detection 不直接相关,但**思想相同**——"用一个数学函数快速判断数据是否合法"。

**cgroup 的资源限制**。`cpu.max = 50000 100000` 表示 cgroup 在每 100ms 周期内最多用 50ms CPU。内核定期检查"当前周期已用多少 CPU",超了 throttle。这是"时间区间累积 + 阈值检测",和 Casey 的"span duration + frame budget"逻辑相同。

**netfilter / iptables** 用 BPF 风格的规则匹配,检查每个网络包。"规则匹配"本质是"包属性 vs 规则区间"——"src IP in 192.168.0.0/16 && dst port == 22"是区间检测。BPF (Berkeley Packet Filter) 让用户写过滤器,内核执行——这种"用户定义函数 + 内核执行"模式是 eBPF 的基础。

**kernel scheduling**。Linux CFS(Completely Fair Scheduler)维护红黑树 of runnable tasks,按 vruntime 排序。每个 tick(1ms / 4ms),CFS 检查当前 task 是否该让出 CPU——"时间片用完了吗"。这是"时间累积 + 阈值切换",和游戏 update 循环的 frame budget 检查同构。

### 3.6 · 🦀 Rust 生态视角

Rust 在游戏物理 / 碰撞领域有几个值得关注的 crate 和模式。

**`rapier`** 是 Rust 的 2D / 3D 物理引擎,由 Dimforge 开发。Rapier 提供 rigid body dynamics、collision detection、joints、rays、sensors——是 Bevy 生态的物理默认选项。Rapier 用 ECS 风格 API,内部用 SOA(struct of arrays)布局,cache 友好。

```rust
use rapier3d::prelude::*;

let mut rigid_body_set = RigidBodySet::new();
let mut collider_set = ColliderSet::new();

let rigid_body = RigidBodyBuilder::dynamic().translation(vector![0.0, 5.0, 0.0]).build();
let handle = rigid_body_set.insert(rigid_body);
let collider = ColliderBuilder::ball(0.5).build();
collider_set.insert_with_parent(collider, handle, &mut rigid_body_set);
```

Rapier 内部用 **broad phase(AABB tree)+ narrow phase(GJK / SAT)**,和工业 C++ 引擎(Box2D、Bullet、PhysX)思路一致。Casey HH 手写简化版,Rapier 是 production-grade。

**`parry`** 是 Rapier 抽出来的纯 collision detection 库。parry 提供 shape(ball、cuboid、convex hull、triangle mesh)、ray cast、point projection、pair collision——不涉及 dynamics。如果你只想要碰撞不想物理,parry 是答案。

**`nalgebra`** 是 Rust 的线性代数库,Dimforge 出品(同 Rapier / parry)。`Vector3`、`Matrix4`、`Quaternion`、`Isometry3`——3D 游戏需要的所有数学类型。比 `cgmath` 现代、比 `euclid` 全面。

**Rust ECS 的 collision pattern**。Bevy + Rapier 的典型代码:

```rust
use bevy::prelude::*;
use bevy_rapier3d::prelude::*;

fn setup(mut commands: Commands) {
    commands.spawn((
        RigidBody::Dynamic,
        Collider::ball(1.0),
        Transform::from_translation(Vec3::new(0.0, 5.0, 0.0)),
    ));
}

fn on_collision(
    mut events: EventReader<CollisionEvent>,
    query: Query<&Name>,
) {
    for event in events.read() {
        if let CollisionEvent::Started(e1, e2, _) = event {
            println!("{:?} collided with {:?}", query.get(*e1), query.get(*e2));
        }
    }
}
```

ECS 风格——entity 加 Collider component,系统处理 CollisionEvent。这是 Rust 风格的 collision 设计——数据导向、零成本、类型安全。Casey HH 用 OOP 风格(`struct Player { collider: Collider }`),简单但不够通用。

**Rust 的 zero-cost abstraction** 让 collision detection 可以既高级又快:

```rust
// 高级 API
let hits = colliders.iter()
    .filter(|c| c.aabb_intersects(&target_aabb))
    .filter(|c| c.shape_intersects(&target_shape))
    .collect::<Vec<_>>();

// LLVM 把这优化成手写 C 的等价代码
```

Rust 的 iterator chain + 闭包,被 LLVM inline + vectorize,生成和手写循环一样快的代码。这是 Rust 在性能敏感领域的杀手锏。

**Rust 的 SIMD** 对 collision 特别有用。AABB 测试 8 个浮点比较,用 SIMD 一次处理 4-8 个 AABB。Rust 的 `std::simd`(unstable)或 `wide` crate(stable)提供 SIMD API。Rapier 内部用 SIMD 加速 broad phase。

**Rust 的多线程 collision**。collision detection 是 embarrassingly parallel——N 个 object pair 互相独立。Rust 的 `rayon` 提供 parallel iterator,`colliders.par_iter().filter(...)` 自动并行。Bevy 用 ECS 的 `query.par_iter()` 同样效果。

**Rust 的 `Send + Sync`**。collision data 跨线程共享,要 `Send + Sync`。Rust 编译时检查——如果你的 Collider 包含 `Rc<...>`,编译失败,因为 Rc 不是 Send。这种类型系统保证的多线程安全,是 Rust 比 C++ 优势的地方。

**Rust 的 `smallvec`** 在 collision 里有用。pair list 经常是空的或只有 1-2 个 pair,用 `SmallVec<[Pair; 4]>` 避免堆分配,大部分情况栈上。

**Rust 的 `ahash`** 在 spatial hash 里替代 `DefaultHasher`——3-5 倍速度。Casey HH 的 hash chunk storage(Phase 2)用 `DefaultHasher`,Rust 版应该用 `ahash` 或 `fxhash`。

## 4 · 认知地图

### 4.1 上级

- **State machine(FSM)** —— 状态驱动行为
- **Early exit optimization** —— 早退优化
- **Pareto principle(80/20)** —— 少数 entity 占多数 CPU

### 4.2 同级

| 优化类型 | 例子 | 加速比 |
|---|---|---|
| Conditional update(today) | Dead entity 跳过 | 5× |
| Spatial culling(Day 058) | 屏幕外不画 | 50× |
| Sim region(Day 063) | 远处不模拟 | 1000× |
| LOD | 远处低精度 | 10× |
| Lazy evaluation | 用时才算 | 视情况 |

### 4.3 下级

- **EntityState enum** —— 状态定义
- **match arm** —— 每状态的 update 逻辑
- **Dying { timer }** —— variant 带数据
- **early return** —— Casey 风格
- **Branch prediction** —— CPU 友好

## 5 · 对照与变奏

### 状态机在不同领域

| 领域 | 状态机 | 状态 |
|---|---|---|
| HH entity | EntityState | Alive / Dying / Dead |
| OS process | task_state | Running / Sleeping / Zombie |
| TCP connection | TCP state | LISTEN / SYN_SENT / ESTABLISHED / ... |
| Vending machine | (典型 FSM 例子) | Idle / Coin inserted / Dispensing |
| Regex | NFA / DFA | 字符状态 |
| HTTP request | request lifecycle | idle / parsing / processing / responding |

都是"少数状态 + 状态间转移"。

### 历史演化

- **1950s**:FSM 形式化理论
- **1980s**:游戏 AI 用 FSM(Pac-Man 鬼)
- **1990s**:游戏引擎内置 FSM 编辑器
- **2000s**:行为树在 Halo 2 普及
- **2010s**:GOAP / Utility AI 流行
- **2020s**:RL / ML AI 在某些场景

Casey 用最简 FSM,够用。

## 6 · 关联 Day

- **铺垫**:
  - [day060](day060.md) —— HP + EntityState 引入
  - [day066](day066.md) —— spatial / non-spatial 分离
- **当天**:day067(本日)
- **后续**:
  - [day068](day068.md) —— 速度 clamp(另一种条件 update)
  - [day287](../phase-6/day287.md)+ —— brain 系统,完整 AI FSM
  - [day180](../phase-5/day180.md)+ —— debug overlay 显示 entity state

## 7 · 变式训练

### Lv1 · 概念辨析

**题**:垂死(Dying)状态的 entity 为什么 update 渲染,但不 update AI?

**参考解答**:
- **update 渲染**:倒地 / 渐隐动画要播。如果不 update,entity 突然从屏幕消失,玩家觉得"它瞬间蒸发了",游戏感很差
- **不 update AI**:已经死了,不需要思考"去哪 / 攻击谁"。AI 计算是浪费 CPU
- **不 update 物理**:已经倒地,不需要重力 / 碰撞(它会和地面 / 其他 entity 不必要地交互)
- **timer 减**:倒计时到 0 转 Dead,触发清理

这是状态机的本质——**不同状态做不同事**。所有状态都做同样的事,等于没状态机。

### Lv2 · 动手实践

**题**:实现 conditional update。验证:
1. 1000 个 entity,800 是 Dead,200 是 Alive
2. 模拟 60 帧,测量总耗时
3. 用条件 update 应该比"全部完整 update"快 4×

**完成标准**:打印两种实现的耗时对比。

**参考解答**:

```rust
use std::time::Instant;

#[derive(Clone, Copy, PartialEq, Debug)]
enum EntityState { Alive, Dying { timer: f32 }, Dead }

#[derive(Clone, Debug)]
struct Entity {
    state: EntityState,
    pos_x: f32,
    pos_y: f32,
}

impl Entity {
    fn update_full(&mut self, dt: f32) {
        // 模拟 AI / 物理 / 动画(无脑做)
        self.pos_x += 1.0 * dt;
        self.pos_y += 1.0 * dt;
        // 模拟一些 CPU 工作
        let mut sum = 0.0;
        for i in 0..100 {
            sum += (i as f32).sqrt();
        }
        let _ = sum;
    }

    fn update_conditional(&mut self, dt: f32) {
        match self.state {
            EntityState::Dead => { return; }
            EntityState::Dying { ref mut timer } => {
                *timer -= dt;
                if *timer <= 0.0 { self.state = EntityState::Dead; }
                return;
            }
            EntityState::Alive => {
                self.update_full(dt);
            }
        }
    }
}

fn main() {
    let dt = 1.0 / 60.0;
    let mut entities: Vec<Entity> = Vec::new();
    for i in 0..1000 {
        let state = if i < 800 { EntityState::Dead }
                    else { EntityState::Alive };
        entities.push(Entity { state, pos_x: 0.0, pos_y: 0.0 });
    }

    // 全部完整 update
    let start = Instant::now();
    for _ in 0..60 {
        for e in &mut entities {
            e.update_full(dt);
        }
    }
    let full_time = start.elapsed();
    println!("Full update (all entities): {:?}", full_time);

    // 重置
    for e in &mut entities {
        e.pos_x = 0.0;
        e.pos_y = 0.0;
    }

    // 条件 update
    let start = Instant::now();
    for _ in 0..60 {
        for e in &mut entities {
            e.update_conditional(dt);
        }
    }
    let cond_time = start.elapsed();
    println!("Conditional update: {:?}", cond_time);
    println!("Speedup: {:.1}x", full_time.as_secs_f64() / cond_time.as_secs_f64());
}
```

### Lv3 · 迁移设计

**题**:加一个新状态 `Stunned { timer: f32 }`(被眩晕,1 秒不动但能被攻击)。如何修改 update 函数和 take_damage 函数?

**提示**:
- update 里:Stunned 跳过 AI / 物理,但 timer 减,timer 到 0 转 Alive
- take_damage 里:Stunned 状态的 entity 仍可被攻击(扣血)
- 写出关键代码,考虑"被眩晕时被攻击死"的情况(应该转 Dying)

### Lv4 · 开源贡献

**题**:`rust-fsm` 是 Rust 的状态机库。

GitHub: https://github.com/eugene-babichenko/rust-fsm

1. clone 仓库
2. 看它的宏 / DSL
3. **可能的贡献方向**:
   - doc 加"游戏 entity 状态机示例"
   - examples 加"游戏 entity 状态机"
   - 测试覆盖某个未测的状态转移

**示例**:
```
PR 标题:examples: add game entity state machine
文件:examples/game_entity.rs
动机:rust-fsm 适合游戏 entity,但没有现成示例。
验证:cargo run --example game_entity,看到状态转移输出。
```

## 8 · Rust / Arch 落地代码

### 关键 Rust 特性

#### match 完整性

```rust
match self.state {
    EntityState::Alive => { /* ... */ }
    EntityState::Dying { timer } => { /* ... */ }
    EntityState::Dead => { /* ... */ }
    // 漏任意一个就编译错
}
```

Rust 强制 match 完整。C 的 switch 不强制(可漏 case),bug 来源。

#### ref mut pattern

```rust
match self.state {
    EntityState::Dying { ref mut timer } => {
        *timer -= dt;  // 修改 enum 内的字段
    }
}
```

`ref mut` 借用可变引用,不 move 出 enum。如果没有 `ref`,会尝试 move `timer` 出去,Rust 不允许(enum 还在用)。

替代写法:`if let &mut EntityState::Dying { ref mut timer } = &mut self.state { ... }`。但 match 更清晰。

#### Match guard

```rust
match self.state {
    EntityState::Dying { timer } if timer > 0.5 => { /* early */ }
    EntityState::Dying { timer } => { /* late */ }
}
```

`if condition` 进一步过滤 pattern。Casey 在 C 用 `if (state == DYING && timer > 0.5)`,Rust 用 match guard。

### Arch Linux 工具链验证

```bash
cargo new --bin hh_day067
cd hh_day067
# 把代码粘进 src/main.rs
cargo run --release

# 输出类似:
# Full update (all entities): 8.5ms
# Conditional update: 1.8ms
# Speedup: 4.7x

# (具体数字看 CPU,但 conditional 应该明显快)

# 用 perf 看 cache / branch 状态
sudo pacman -S perf
perf stat -e branches,branch-misses,cache-misses ./target/release/hh_day067
# conditional 版应该 branch-miss 少(早退预测准)
```

**Troubleshooting**:
- "无法 move timer out of enum" —— 加 `ref mut`
- "match 不完整" —— 漏了某个 variant,加 _ => 或显式列出
- "性能没差" —— release 模式跑(debug 不优化 match)

## 9 · 延伸阅读(可选补充,非必需)

本仓库本地:
- [day060](day060.md) —— EntityState
- [day066](day066.md) —— spatial / non-spatial

外部稳定 URL(可选):
- Game Programming Patterns "State": https://gameprogrammingpatterns.com/state.html
- Rust Book ch.6(Enums + match): https://doc.rust-lang.org/book/ch06-00-enums.html

真实开源源码:
- rust-fsm: https://github.com/eugene-babichenko/rust-fsm
- Casey HH day067: https://guide.handmadehero.org/code/day067/
